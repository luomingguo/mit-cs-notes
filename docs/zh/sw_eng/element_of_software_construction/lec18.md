# Lec 18 消息传递 & 网络





## 总结 

消息传递系统通过**共享通信通道**（如数据流或队列）而非共享可变数据，从根本上规避了同步操作引发的并发问题。其核心设计原则为：

1. **隔离可变状态**
   - 各并发模块（线程/进程/独立机器）仅通过传递**不可变消息**通信
   - 所有状态变更被限制在模块内部（如之前的《互斥锁》阅读材料中安全可变ADT的设计不再必要）
2. **客户端-服务器模式的必然并发**
   - 多客户端与多服务器在网络中同时收发消息
   - 服务端若因单个慢客户端阻塞，将导致其他客户端请求延迟（违背及时响应原则）