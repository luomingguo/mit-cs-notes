# Lec 2 有符号数 & 补码 & 指针





## 指针

指针是32bit的地址，不同的数据类型有不同的字节大小，因此需要指定指针的类型，这样它就能知道如何读取 & 解释有多少字节。函数中常用来更改原始数据的值，函数默认是值拷贝。

指针的错误用法；

```c
int *x = 0; // x 存储的内存地址为 0; 或者说x指向地址0； 或者x的值为0
*x = 5; // Segmentment fault ❌，地址 0 被保留用于表示空指针（null）并且不能被更改
```

```c
int a = 1;
int *x; // 没有指向特别的地址
*x = a; // Segmentment fault ❌， x存储的地址不合法
```



指针的作用：

1. 指向一个变量，
