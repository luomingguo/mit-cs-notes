# Lec 20 网络与OS

谈谈网络，以及它与操作系统的关系，

## 总览

- 数据包格式与协议
- 内核中软件栈
- 过载行为——这节课的论文

阅读材料

[Eliminating Receive Livelock in an Interrupt-driven Kernel, 1996](https://pdos.csail.mit.edu/6.828/2023/readings/mogul96usenix.pdf)



## 数据包格式与协议

### 以太网

![image-20240908233432902](http://47.115.50.83:49153/i/66ddc42397929.png)

- （同步前导位，例如 10101010）
- （开始符号，例如 10101011）
- **目标以太网地址**——48位
- **源以太网地址**——48位
- **以太类型**——16位
- **有效负载**
- （CRC-32）
- （结束符号，例如 8b/10b 或曼彻斯特编码）

xv6中定义在kernel/net.h

> Tcpdump捕捉和现实网络数据包
>
> 通过tcpdump可以``tcpdump -t -xx -n``
>
> - `-t`：在输出中不显示时间戳。这会导致 `tcpdump` 只显示数据包内容，而不包括时间戳。
> - `-xx`：以十六进制格式显示数据包的内容，以及每个字节的 ASCII 码表示。这会让输出包括数据包的完整字节内容和每个字节的 ASCII 字符显示。
> - `-n`：不解析主机名。使用 IP 地址而不是域名进行显示，这可以加快解析速度并避免 DNS 查询。

包的开头会有一些东西，由硬件在非常低的级别上识别，表示数据包的开始，而接收主机需要知道包结束，所以在结束会有另一个特殊的位模式，表示数据包的结束。这两个开始和结束标志永远不会被软件看到，但是剩下的以太网帧到 H2 的网卡是由软件完成的。主机实际上负责设置和解析以太网使用的报头

#### 以太网地址

- 简单的以太网局域网广播：所有主机网卡看到所有数据包
  - 主机使用目标地址决定数据包是否真正发给它
- 当今的以太网局域网使用交换机，每台主机都有一条电缆
  - 交换机使用目标地址决定将数据包发送到哪个主机
- 每个网卡都有唯一的以太网地址，由网卡制造商分配
  - <24位制造商 ID，24位序列号>

>  我们为什么要用以太网地址代替IP地址？

1. 以太网不是唯一的底层地址
2. “平坦”地址使广域网路由变得困难

- 因此：32位 IP 地址，高位包含路由提示
- 但如何从 IP 地址转换为本地以太网地址？

#### ARP

- 用于将 IP 地址转换为以太网地址的请求/响应协议
- “嵌套”在以太网数据包中，类型 0x0806
- ARP 头部指示请求与响应
- 请求包含所需的 IP 地址
  - 请求包广播到交换机上的每台主机
  - 所有主机处理该包，但只有拥有该 IP 地址的主机会响应
- 响应包含对应的以太网地址

- [tcpdump -t -n -xx arp]

注意：

- 通常将高层数据包“嵌套”在低层数据包中
- 例如 ARP 嵌套在以太网中
- [图示：eth | ip | udp | DNS]
- 所以你经常看到一系列头部（以太网头部，然后是 ARP）
- 一层的有效负载以下一层的头部开始

- 以太网头部足以将数据包送到本地局域网的主机
  - 但需要更多的信息才能将数据包路由到远程互联网主机



### IP 头部

- [kernel/net.h, struct ip]
  - 以太网类型 0x0800
  - 包含大量内容，但地址最为关键
  - 32位 IP 地址足以路由到任何互联网计算机
  - 高位包含“网络号”，帮助路由器理解如何在互联网中转发
  - 协议号告诉目的地如何处理数据包
    - 即，数据包应交给哪个高层协议处理（通常是 UDP 或 TCP）
- [tcpdump -t -n -xx ip]

### UDP 头部

- [kernel/net.h, struct udp]
  - 一旦数据包到达正确的主机，应该送到哪个应用？
  - UDP 头部位于 IP 数据包内部
  - 包含源和目标端口号
  - 应用程序使用“套接字 API”系统调用告诉内核希望接收发送到特定端口的所有数据包
  - 一些端口是“公认的”，例如端口 53 预留给 DNS 服务器
  - 其他端口根据需要分配给连接的客户端端
  - UDP 头部后是有效负载，例如 DNS 请求或响应
- [tcpdump -t -n udp]

### TCP

- 类似于 UDP，但显式连接
- 包含序列号字段，以便可以重新传输丢失的数据包
- 以及将发送速率匹配到网络和目的地容量的能力

**典型内核中的网络代码是什么样的？**

- 典型的 UNIX/Linux 设置
- 广泛用于防火墙和服务器
- 设计对性能和过载至关重要





在实验里，xv6 最终会与模拟的以太网协议交互，通过模拟以太网协议发送 IP 数据包，在你运行 QEMU 的任何主机上运行。所以，你可以看到这些 ARP 报文，在 xv6 和你们的主机之间，我们在这里看到的是我的主机想知道我的 xv6 的 IP 地址，想要知道它在 QEMU 模拟的局域网中的以太网地址。第二个包是我的 xv6 ，你可以看到生成的代码是我的 xv6 看到了这个请求，意识到它是请求中 IP 地址的所有者，并且正在发回响应，tcpdump 可以很好地解析出 ARP 数据包中的字段，并打印在这里，我想这是发送者的 IP 地址，这是，抱歉，这是发送者的 IP 地址，这是发送者感兴趣的 IP 地址，这些东西大概会放在这里和这里，这是一个响应，是具有这个 IP 地址的所有者的以太网地址，并且这个以太网地址可能最终在这个字段中，如果我们足够聪明，我们可以把这些数据包拆开，看看其中的一些字段，如我们所知，这个部分是以太网报头，目的以太网地址，源以太网地址和数据包类型 0806 ，向后看，这是 tip 字段，它是发送者想要查找以太网地址的 IP 地址，如果你把这个拆开，这里有一个字节，每个 IP 地址有四个字段，抱歉，它是在找 10.0.2.15 ，这是十六进制 10 ，十六进制 0 ，十六进制 2 ，十六进制 15 ，然后是目标以太网地址，它是未知的，然后是发送者的 IP 地址 10.0.2.2 ，发送者的以太网地址，这里还有一堆东西在说，我们对以太网和 IP 地址格式感兴趣。并且这是请求，这是回应，关于 ARP ，有什么问题吗？嗯，是的，有个问题，为什么发送者需要包括它的 IP 地址，如果它已经包含在数据包中的以太网地址，想要响应它，那么接收者是不是只需要使用那个地址？是的，我不知道为什么里面会有这些东西，我想如果你想的话，你可以把这件事做得更简单，比如，发送，好吧，也许答案是，这个协议被设计成在网络上使用而不是在以太网，所以它被设计成相当独立的，所以它不依赖于其他任何东西，所以， ARP 报头具有以太网地址的复制，事实上，如果你通过以太网发送 ARP ，以太网数据包还要包含所有的以太网地址，正如你在这里看到的，所以，在以太网上运行 ARP 是多余的，但如果你在其他地方运行 ARP ，你需要这些字段，因为可能还有其他数据包格式，没有包括这些地址。我明白了，好的，谢谢。哦，抱歉，右边那部分是什么？好的，这个，这不是很有趣，但这是这些字节的 ASCII 码解释，这个点对应没有 ASCII 码的字节，我猜这个可能是 52 或 55 ，在 ASCII 码中， 52 可能是 R ， 55 可能是 U ，所以，这会更有趣，当我们开始发送包含 ASCII 文本的数据包时，而不是二进制字段。好的，我明白了，谢谢。好的。好的，我给你看这个，因为你会在实验里看到这些包。好吧，有件事我想。好的，我有件事想确认一下，你能从这个讨论中获得，这是嵌套协议和嵌套报头的格式化数据包的[习惯]，所以我们刚才看到一个数据包，以太网报头和以太网有效载荷，以太网有效负载的第一部分是 ARP 报头，剩下的部分是 ARP 有效载荷，但是还有其他的，我们稍后会看到更复杂的结构，在以太网数据包中包含 IP 数据包，在 IP 包内部是 UDP 包，UDP 是另一种可以在 IP 上运行的协议，所以这里有一个 UDP 报头，它也是，不一定要理解这些首字母缩写，但作为 UDP 报头，UDP 数据包也包含报头和有效载荷，有时会在 UDP 中携带另一个协议，比如域名系统，包含另一种适合在 UDP 内部的数据包格式定义，所以你看到的是，主机发送数据包将构建一个数据包，DNS 软件会说我想通过 UDP 发送数据包，UDP 软件会准备 UDP 报头，我们通过 IP 发送，IP 软件会添加 IP 报头，以太网软件会添加以太网报头，并在发送时逐渐在软件中建立数据包。类似地，当系统接收到数据包时，它首先获得整个数据包，检查第一个报头，注意到是以太网，因为它从以太网卡接收数据，校验有效性，去除这个报头，查看下一个报头，这里总有一个类型，或者在这种情况下，有一个协议字段，告诉软件在以太网报头之后希望发生什么。所以，有一个类型字段表示 IP 或 ARP ，所以，软件将查看每个报头，验证它，剥离它，获取下一个报头，检查报头，解释它，理解它，再把它去掉，暴露出下一层，并将其传递给下一层软件。我会更多地讨论这一点，但这是一种看待嵌套数据报头的通用方式。好的，以太网数据包，以太网报头，足以将数据包发送到局域网上的主机，特别是当你想在本地发送 IP 数据包时，你可以使用 ARP ，但 IP 是更通用的，IP 是一种协议层，帮助你将数据包传送到互联网的任何位置，基于 IP 地址。所以，这是 IP 数据包的格式，再次，你可以在我们给你的 net.h 源码中找到它，通过以太网[]，你可以看到，这是，在以太网数据包中，有目的地址、源地址和类型，以太网类型等于 0800 ，然后是 IP 报头，以及 IP 有效载荷，当你将数据包发送到远程网络时，在世界的另一边的网络，IP 报头传递，在离开本地以太网后，以太网报头被剥离，也许在路由数据包的每一跳上都会放上一个新的，但是 IP 报头保持不变，从你的计算机上的原始源主机，一直到目的主机，这个报头具有全局意义，而以太网报头仅用于单个局域网，所以，这里必须有足够的信息将数据包一路传送到互联网的另一端。对于我们的目的来说，关键字段是，这个数据包格式中的三个非常有趣的字段，目的地址字段，是主机的 32 位 IP 地址，我们将数据包发送到，特别是在它的高位，其中包含网络编号，用于帮助路由器，当数据包被递送时，这个 p 协议字段告诉目的地主机如何处理数据包，在它剥离 IP 报头之后下一步该如何处理。如果你见过麻省理工的 IP 地址，你会看到有几个不同的，但是如果你看到互联网地址以 18 开头，在过去的几年里，事情发生了变化，但在很长一段时间里，这是麻省理工的网络编号，所以，大多数托管麻省理工的 IP 地址的高位字节是 18 ，世界各地的路由器都会有一张表，它们会查到 18 ，然后说，我知道如何路由这个数据包到麻省理工又近一步。所以，让我再次展示 tcpdump 的输出，同样取自实验，其中包括 IP 报头。好的，我们可以解析这个数据包，因为它是通过以太网发送的，它以以太网报头开始。它是一，我认为这些 tcpdump 是有问题的，这些生成的包，现在我看它，我不确定问题出在哪里，它们不应该开头，以太网报头不应该以全部 f 开始，因为它是广播地址，这会导致数据包到达每台主机，你不会看到在两台单独的主机之间发送的数据包，因为这台主机位于真实网络上，在 QEMU 的网络实验中我的解决方案中有些东西有点滑稽，不管怎样，我们有以太网目的地址、以太网源地址，而以太网类型是 0800 ，表示剩余的字节是 IP 数据包，IP 数据包报头长度我认为是 20 字节，找到结尾，二，十五，所以这一定是 IP 报头的结尾。往回看一下，因为这些是我们真正关心的字段，目的 IP 字段是， a 是 10 ， 10.0.2.2 ，我认为，在 QEMU 中，有趣的模拟网络是我运行 QEMU 的真实计算机的地址，在这之前是 IP 源地址，是 10.0.2.15，是发送方，是 QEMU 的地址，是 QEMU 内运行的 xv6 的地址，然后在这之前的这些东西，有一个 16 位的校验和，你的软件应该检查它，意识到数据包已经损坏，应该丢弃，这就是校验和，这里有一个字节，好的，这里的东西乱了。哦，抱歉，这是 16 位校验和，这个 11 是协议号，这一点特别重要，十六进制 11 是 16 加 1 或者 17 ，意味着这是一个 UDP 数据包，基于协议字段。然后所有这些东西，我们并不关心，比如数据包的长度。关于 IP 报头，有什么问题吗？好的，再说一遍，这里的关键是，IP 报头具有 IP 地址的源地址和目的地址，而这个协议字段告诉目的主机网络堆栈，这个分组应该由 UDP 软件处理，我现在会谈到这一点。好的，这个 IP 报头，它足以将数据包发送到互联网上的任何主机，但我们想做得更好，每台主机都在运行很多需要使用网络的不同程序，它们需要在网络中发送和接收数据包，所以我们需要一种不在 IP 字段中的方法，为了决定哪个应用程序需要，应该将包移交给目标主机上的哪个应用程序。有几个协议可以完成这项工作，一个是相当复杂的 TCP ，还有其他是 UDP 。TCP 主要用于 web 之类的东西，TCP 是一种非常复杂的协议，它不仅帮助将数据包传送到正确的应用程序，还有许多东西，比如序列号，为了检测丢失的包并重新发送它们，确保包或数据按顺序而且不间断地传送，防止出现什么问题。UDP 是一种简单得多的协议，它只是传递，尽力而为的传递包到特定应用，但是，没有任何纠错，或者没有任何其他东西，对我们来说，关键字段是这两个端口号，这里的游戏是，当你的应用程序想要发送或接收数据包时，它使用所谓的套接字 API ，在 Unix 上，这是一组系统调用，其中进程说，我对寻址到特定端口的数据包感兴趣，它会说感兴趣的端口号，抱歉，特定的包，我要接收具有特定目的端口的数据包，操作系统进行系统调用，它在操作系统中设置，返回一个文件描述符，每次数据包到达应用程序请求的端口，这个数据包将出现在文件描述符上，应用程序就可以读取它。这两个端口实际上是两种端口，一些是知名端口号，比如端口号 53 是 DNS 名称服务器的官方知名普遍同意的端口号，所以，如果你要向 DNS 名称服务器发送请求，你可以通过 dport 53 发送 UDP 包，还有许多其他知名的端口，可以提供普遍可用的服务，使用普遍同意的编号，然后剩余的 16 位端口号空间，用于匿名客户端支持，所以，如果我想要向 DNS 服务器发送数据包，它的 dport 是 53 ，但它的 sport 是我这边随机选择的数字，所以，它与我的应用程序的套接字相关联，所以，当 DNS 服务器发送回复时，将对其进行寻址，DNS 服务器会将请求源端口复制到回复的目的端口字段中，并将其发回我的机器，我的机器将使用这个端口号来确定哪个应用程序应该得到响应。好的，这里的主要功能是拥有这两个端口号，为了分发，可以在这台机器上分发各个应用程序的数据包。所以，请随意提问。我也有 UDP 的 tcpdump 输出，也是从实验里拿来的。同样，我们有一个以太网报头，20 字节的 IP 报头可能在这里结束，11 是 IP 协议号 17 ，也就是 UDP ，所以接收主机知道用 UDP 软件来处理它，接下来的 8 个字节是这里显示的 UDP 报头。那么谁知道这些端口号是什么？我是说，这是一个由实验软件生成的数据包，任何特殊的号码，这是恰巧选择的端口号，这一定是包的长度，1b 是二十多，这是我的，这是我们的软件，xv6 中的 UDP 还很差劲，它没有填写校验和字段，这就是报头。然后在 UDP 报头之后是 UDP 包的有效载荷，在这种情况下，应用程序正在发送 ASCII 文本，而 ASCII 文本就在这里。所以，这是在 UDP 数据包中 ASCII 文本，在 IP 数据包中，又在以太网数据包中，通过模拟以太网发送。抱歉，我有个问题，当你说给某人寄数据包的时候，你不知道它们的以太网地址，那么是否只是将它发送到你的路由器，然后路由器通过计算得出。对于大多数在互联网上发送到其他地方的数据包，让我们看看，如果你将数据包发送到特定的 IP 地址，你的主机软件将查看目标地址，确定目标主机是否与你在同一局域网上，如果是，它将使用 ARP 将 IP 地址转换为以太网地址，然后通过以太网将数据包发送到目标主机，所以在这种情况下，目标主机在同一网络中。在更一般的情况下，我们将数据包发送到全国各地的互联网上的其他地方，你将把数据包发送到同一局域网上的路由器，那个路由器将查看目的 IP 地址，选择下一台路由器，以确定对于数据包连接到哪台路由器，数据包的逐跳通过路由器，离目标越来越近。这回答了你的问题吗？好的，我明白了，是的，非常感谢。有人问数据的长度是否有限制，答案是肯定的，有几个不同的限制，每个底层网络技术，比如以太网，但还有其他东西，比如以太网，有它自己的最大数据包长度，所以，当今天的论文写的时候，以太网上的最大数据包长度是 1500 字节，我认为现代以太网允许 9000 或 10000 字节的数据包，但这是我听说过的最大数据包大小。这个原因，你不希望无限长的单个包有几个原因，其中之一是，你通过线路发送这些数据包，可能会相当长，而且会受到噪音和干扰的影响，所以，当你发送数据包时，可能会出现位损坏，基本上，每种网络技术都是某种校验和或纠错码，伴随着每个数据包，但是校验和和纠错码仅能可靠地检测一定数量的位上的错误，随着位数的增加，出现不常见错误的概率会越来越高，所以，这限制了，对于合理大小的校验和，比如 16 位或 32 位，它限制了数据包的最大大小。另一个限制是，如果你发送巨大的数据包，这意味着主机中的所有路由器都必须有巨大的数据包缓冲区，才能准备好接收巨大的数据包，这开始变得非常昂贵，因为很难有可变长度的缓冲区，所以最方便的是只有单一长度的缓冲区，这样效果最好，如果最大数据包长度不是太大。不管怎样，对于你来说，所以以太网有 1500 , 9000 字节限制，此外，对于所有 IP 协议都具有 16 位的长度字段，所以，即使你愿意让以太网有更大的数据包大小，IP 本身也有最大数据包大小 64 千字节。好的。好的，对 UDP 来说挺多了。希望当你完成实验后，你会看到非常类似的输出，具体地说，来自 xv6 的消息，以及来自你正在运行的 QEMU 的主机的消息。事实上，在实验的最后，你将使用，你将运行我们提供的软件，软件将向 Google 的 DNS 服务器发送 DNS 查询，把回应拿回我们的软件，并打印响应，你的软件将完成以太网级设备驱动程序交互。好的，这就是关于网络上的数据包报头和协议的事。所以，与这些数据包格式对应的是，让我们查看在主机上运行的网络软件堆栈，所以，如果你想一想主机里有什么，从现在开始，我要说的主要是一些典型的软件安排，人们构建网络软件有不同的方法，和我要说的有点不同，我要讨论一下，我认为至少是一种标准的方法。让我们假设运行的是 Linux 或者是 xv6 ，现在我们有一堆应用程序，可能是网络浏览器，也可能是 DNS 服务器，谁知道是什么样的应用程序，它们都使用套接字 API ，来打开套接字层中的文件描述符，所以在内核里会有这样的东西，称为套接字层的软件层，它记住表，它记住对应的文件描述符，应用程序用来读取，UDP 端口号或 TCP 端口号，是这些文件描述符引用的端点，所以，套接字层有关于端口号的文件描述符表，并且它通常还具有已到达的数据包队列，并等待每个套接字或文件描述符读取。我们提供的软件是一个非常原始的套接字层，在它下面是 UDP 和 TCP 协议层。UDP 几乎什么都没发生，它查看传入的数据包，提取目的端口号，并将数据包传递给套接字层，所以有效载荷在正确的文件描述符入站队列上排队。TCP 实际上要复杂得多，它保持每个 TCP 连接状态，它会记住所有序列号和数据包，那些没有确认和需要重发的，以及 TCP 的大量状态和所谓的协议控制块，而在 UDP 层中没有状态。这些通常称为传输层， UDP 和 TCP ，我们为你提供一个简单的 UDP 层，而不是 TCP 层，在 TCP 和 UDP 下面是 IP 层，这是相当简单的，与 IP 层并行，我不确定否应该画在同一层面或下一层面，那就是 ARP 层，在它们下面，我们可以认为是以太网层，但实际上不会有单独的以太网层，通常在最低层有一个或多个网卡驱动程序，并且这些设备与实际的网络接口硬件通信，它本身有与局域网的连接，或任何连接的网络，在这个层面上，发生的情况是数据包到达网络，网卡会把它从网络上拉下来，把它交给驱动，并且驱动将数据包向上推送到网络堆栈，在堆栈中的每一层，这一层的报头，IP 层会查看 IP 报头，验证报头，从报头剥离出 UDP ，UDP 将找出文件描述符，找出数据并放到队列中，数据包进入并解析，报头在向上的过程中被剥离。当应用程序发送数据包时，会发生相反的事情，随着分组在各层中向下移动，越来越多的报头被添加上，直到你到达最底层，然后数据包交给网卡进行传输。所以，当然软件，人们思考和设计网络软件的方式，内核通常由数局包内协议的嵌套来驱动。对这个结构，有什么问题吗？实际上，有一个重要的事情，我留在这里，它在旁边，这里有缓冲区，所有都通过这里，当一个包到达时，它被复制到包缓冲器中，数据包缓冲器在堆栈中上下发送，而且通常有相当多的数据包缓冲器，这些层之间经常有队列，这里肯定有队列，数据包等待应用程序处理，这里是链表缓冲区，所以这里有一个缓冲区分配器，这是一个缓冲区方案和一个缓冲区分配器，在整个堆栈中使用，在软件中，我们给出了一个缓冲区方案，叫做 MBUF ，它是一种 MBUF 方案，这不是一个层，而是所有这些层都在使用。好的，这是典型的网络堆栈分层图。对于论文来说，理解控制流是如何工作的很重要，这可能与图中的有所不同。关于网络堆栈，需要了解的一件事是，通常有多个独立的参与者，处理数据包并接受输入，考虑这些数据包并产生输出，由于各种原因，这些不同的参与者是分离的，所以它们可以并发运行，并具有连接它们的数据包队列。所以从论文的角度来看，这是非常重要的，所以在内核中，同样，我们有一个网卡，然后我们有内核，经典安排是让网卡以某种方式获取数据包，对于网卡，当它接收到数据包时会生成中断，这里有一个中断例程，当中断来时就会触发，中断例程的任务是从网卡中获取数据包。因为我们不想专门拿出 CPU 时间用于完成数据包的处理，中断例程通常仅将数据包附加到队列，以便稍后处理，然后返回，所以，所需的最少工作是从网卡获取数据包并将其放入队列中，我们要转移到传统的网络堆栈中的原因，我们希望快速将数据包从网卡中移出并移入这个软件队列，因为网卡通常只有非常有限的用于数据包队列的内存，而在主内存，计算机的 RAM 中，我们可能有千兆字节的内存，所以这里有大得多的空间，所以，网卡，所以，如果出现数据包突发，网卡可能没有空间对它们进行排队，所以，我们将它们复制到这里的队列，以避免网卡空间不足。然后可能在单独的线程中，这里有我所说的 IP 处理线程，有时它不是一个线程，有时它是一种不同的实体，但它的基本工作是读取这些传入队列的数据包，而且可能有多个网卡，将数据包追加到这些队列，所以 IP 线程运行，它会查看在这里排队的数据包，并决定如何处理它们。一种可能是通过 UDP 将它们向上发送到套接字层，排队等待某个应用程序，通常在这里只是一个函数调用，在这个线程的上下文中。另一种可能，这也是论文关心的可能性是，这个主机是一台路由器，它的数据包从一个网卡进入，然后从一个或多个其他网卡路由出去，因为在普通操作系统上构建路由器是很常见的，比如 Linux ，如果你购买 WiFi 盒子或有线调制解调路由器或其他设备，它很有可能在内部运行 Linux ，并使用标准 Linux 堆栈，它有完整的路由实现，它很可能使用标准 Linux 堆栈来进行路由。所以，如果 IP 线程查看目标 IP 地址，决定我应该把这个发送出去，我应该把这个包从另一个网络接口转发出去，它会将数据包添加到这个传出接口的传出数据包队列中，几乎可以肯定的是，这是一个接收中断， RX 表示接收，传出网卡通常有某种传输中断方案，网卡将会中断，每当发送完一个数据包，并准备好接收更多数据包时，这些传出中断也很重要。这里的重点是，这里有一堆并发的实体，有各种不同方式的独立调度，这些中断由网卡触发，当数据包到达或发送是，发生中断，这个线程可能是我们在 xv6 中拥有的内核线程，在单个处理器中，比如在今天论文中的情况，这个线程不能同时运行，中断有绝对优先级，在多核计算机上，可能会有更多的并行性，然后，重要的是应用程序要能够读取数据包，应用程序是另一个独立调度的实体，我们希望得到机会在 CPU 上执行。所以这些是在调度游戏中的所有玩家。有一件经常出现的事是缓冲区，所以这里有三个队列，通过缓冲，我指的是，这些结构附加输入数据包，并且一些其他实体轮询队列前面的数据包，这些队列在网络系统中无处不在。其中一个原因是为了应对临时性的突发，这个 IP 线程可能只能以每秒多少的速度处理数据包，但是网卡可能能够更快地传送数据包，所以这里可能会有一些临时的数据包突发，我们希望有一个地方存放它们，等待 IP 线程开始处理，这是队列的一种用途，在输出端，我们希望使用队列的另一种方式，特别是如果数据包突发，我们希望能够在这里存放一些数据包，准备网卡发送，保持输出网卡忙，因为根据速度的不同，它很重要，能够在这里以 100% 利用网络。另一个原因是，也许与排队的原因不同，能够将软件组织成单独调度的独立部分，不需要 IP 线程或应用程序知道必须进行的其他事情，比如中断处理，所以 IP 线程是一种传统的网络系统，它不需要知道中断何时发生或应用程序何时运行，虽然我们将在这篇论文中看到，在那里有一点了解可能是有好处的。对于这个调度控制图，有什么问题吗？哦，我有个问题，同一个网卡能不能同时用于发送和接收？它可以，实际上我的笔记本电脑只有一个网卡，它连接到 WiFi ，当它收到一个数据包，在我的笔记本电脑上，这个网卡是一个 WiFi 无线电接口，数据包在同一网卡上到达和传出。两个网卡的情况用于路由器，所以你家里的 WiFi ，我不知道，也许我有 WiFi 和有线，还有一个路由器盒子，它有两个网卡，一个连接到我的电缆调制解调器，剩下的是互联网，还有一个是我的 WiFi 接口，所以这个小盒子，电缆调制解调器发送，作为一个路由器有两个网络接口。实际上很多服务器也有多个接口，尤其是那些 web 服务器，你想要与外部世界交互，一个接口发送到你的私有敏感数据库机器或其他什么，在具有另一个网络接口的完全独立的网络上，这种安排是相当常见的。所以，有多个网卡的原则是想与不同的网络通信。是的，如果你想与不同的网络通信，那么你有多个网卡，是的。好的，像我所说的，多谈一点关于网卡的事情，网卡在数据包到达时如何处理它们，这与实验有关系。网卡内部是什么样子，它有一根电缆或无线电从外面的世界通向那里，当电子进来的时候，它查看它们，把它们变成数据包。然后这里是主机，主机里面有某种驱动软件，不管怎样，我们需要将在网卡中解码的数据包放入内存，其中 IP 软件和主机可以解析这个数据包，所以这些年来设计了很多不同的方案，论文的方案是网卡具有很多内存，当数据包到达时，唯一立即发生的事情是网卡将数据包放入自己的缓冲存储器中，就是这样。并且中断主机，网卡有一个内部的数据包队列和一堆内存，然后在中断，在主机驱动程序中，主机驱动中有一个循环，主机驱动程序会与它们交互，它们可以说任何缓冲的数据包，如果是这样，主机有一个循环，它会逐个字节或逐个字地复制，将这个数据包复制到主机的内存中，并附加到主机内部的队列中，所以这就是论文中网卡的运作方式，驱动程序负责执行从网卡内存复制到主机内存。这在 30 年前很有意义，今天，在 CPU 中复制的循环与外部硬件或在总线上的硬件交互非常非常慢，在微处理器设计的宏观方案中，CPU 和外部设备之间的距离，即使它在同一台计算机上，这是一段非常远的距离，今天的每一次交互都需要很长的时间，互相交互，所以你不想有太多的字节交互，所以人们不再设计这样的高速接口了。所以一个更现代的安排看起来是这样的，所以，现在我要讨论一种安排，在 E1000 网卡上的一种安排，你将在实验中使用或模拟它。所以， E1000 网卡的工作方式，有这根电线，它查着电子，但当数据包到达时，网卡写入它们，网卡没有显著的内部缓冲，尽管它有一点，它将数据包直接复制到主机内存中，在那里，它们在主机内存中，等待驱动把已经复制好的它们取走，但这意味着网卡必须知道它应该将每个数据包放在哪里，所以， E1000 网卡的工作方式是主机软件格式化所谓的环，DMA 环，一个数据包指针，所以 DMA 环是指向数据包缓冲区的指针数组。所以，当主机驱动程序初始化时，网卡分配比如 16 1500 字节数据包缓冲区，创建一个由 15 个或 16 个指针组成的数组，并使这些指针指向那里，然后它会在配置时告诉网卡，看，这是环，所以这叫 DMA 环，因为在你走出终点之后，你又回到了起点。驱动程序软件会告诉网卡，这里有一个指针指向 RAM 中的地址， DMA 环，你可以用来寄存进来的数据包，当数据包到达时，网卡会记住下一个环条目，这是一个指针到这里，这允许它记住下一个条目，它[应该] DMA ，当数据包到达时，网卡从这里提取指针，从主机 RAM 中获取缓冲区指针，将数据包字节复制到这个缓冲区中，然后推进其内部索引到这里，指向下一个环的槽，我们将在下一个包中使用它。还有一个类似的，所以这是 RX 环，为了接收，驱动程序设置了一个类似的环，驱动程序可以放入数据包，它想让网卡发送，所以，网卡还有一个指向 TX 环的指针。所以你会学到，你在实验的工作是编写处理这些环的驱动软件。对于这个安排，有什么问题吗？是的， E1000 与生产级别的网卡相比，可能在高性能环境中使用的。当 E1000 出现时，它绝对是最好的网卡，它是在生产环境中使用的网卡，但那是很多年前的事了，现代网卡更聪明，变化不大的是这个 DMA 环结构，你仍会发现网卡使用 DMA 传输数据包，它们找到递送数据包的地方的方式，通过缓冲区指针环。有几件事是现代网卡更聪明的，一个是现代的网卡，你可以给它们设置很多很多的队列，我认为 E1000 只有单个接收队列，但是你可以告诉现代网卡，我想让你把我的数据包分成 32 个不同的传入队列，这里是如何为每个数据包做出决定，查看这个字段，并使用这个字段选择数据包 DMA 环，有很多聪明的方式让人们使用这种能力，比如，如果你有多个虚拟机，你的亚马逊，并且你正在运行多个客户虚拟机，你可以使用这个能力，将每个数据包定向到与虚拟机对应的队列，那个数据包应该被读（的虚拟机）。另一种现代网卡更聪明的方式，它们在网卡上执行一些 TCP 处理，我想最多的可能是通常的校验和计算。无论如何，是的，现代网卡类似 E1000 ，但是更多。好的，谢谢。哦，抱歉，好的。哦，是的，我想问一下，在我们实验的方案中，在 IP 层和驱动之间没有队列，是吗？是的，实验网络堆栈被精简到绝对最小，结构比真正的网络堆栈更简单。但就性能而言，这是否更糟糕？哦，我不知道，我从来没有在现实生活中运行过它，我敢肯定，我们肯定没有注意性能，当编写网络实验时，所以如果有高性能是令人惊讶的，但大多数情况下，这不是性能限制的问题，它不能达到你需要的 95% 的网络堆栈，比如处理多个网卡或使用 TCP 。对，对，好的，我明白了，谢谢。嗯，抱歉，对于整个系统有没有硬件变动，需要让网卡可以直接访问内存，像上一张图那样，是否所有内容都通过 CPU ，或者网卡是否也可以直接到达内存。在我之前展示的那张图片中，网卡访问不到内存。好的。我实际上不知道，我的意思是，也许最重要的问题是，虚拟内存转换是否以及如何工作，当网卡要使用引用主机内存的地址时，我不知道它是怎么工作的，我不知道它是怎么工作的，我猜这里有一个转换，网卡实际在总线上，它连接到，通过一些智能的硅连接到 DRAM 系统，我相信在现代机器中，你可以在网卡内设置转换表，可以使用虚拟地址，或者由这个位于它和 RAM 之间的硬件转换的地址，在某些情况下，这可能是非常有价值的。另一件事，我是说，我意识到的另一件事是，如果网卡要写入一些内存或读取一些内存，并且内存被高速缓存在 CPU 上，你想让网卡读取，如果软件只是写入一个数据包缓冲区，但是 CPU 没有，CPU 只是缓存了写，因为在之后，所有内存被写回，这意味着内存的最新版本位于 CPU 缓存中，而不是在 RAM 中，在这种情况下，我们希望它读取 CPU 缓存，而不是 RAM ，如果它执行 DMA ，当然，在英特尔的机器上，也可能在其他机器上，有一些相当复杂的机制，如果网卡读取一些内存，但是内存的最新副本位于 CPU 缓存中，CPU 缓存将产生数据，而不是 RAM 。好的。是的，那实际上是一种设施，一些聪明的软件来获得高性能，也就是说，对于写入也会发生这种情况，网卡直接在高速缓存中写入高速缓存线，CPU 可以非常快速地获取数据。谢谢。简单，但它们在现实生活却相当复杂。还有别的问题吗？好的，我现在想换个话题看今天的论文。我只想说，因为我们已经讨论了很多这篇论文的背景，我要直接转到论文中的第一个图，为了推动对论文图表的讨论，我们现在看到的是路由器的性能图，在 x 轴上，这里有一个带有两个网卡的路由器，它的工作从一个网卡接口数据包，并把它们从另一个网卡发送出去。x 轴是到达率，数据包到达输入网卡，y 轴输出率，观察到数据包离开输出网卡，我们关心的线是，是填充的圆，向上然后向下。所以即使对这里发生的事情一无所知，我们可以看看这张图，我们可以问问自己，为什么它上升，为什么它下降，这一点有什么特别之处，这是一个拐点，是什么决定了它上升的速度或者下降的速度。所以即使是零知识，我们也有很好的线索知道该问什么问题，那么为什么它会上升，为什么它会上升？这不是一个很深奥的问题。它们上去，对不起，请继续。哦，我想直到它饱和时，你可以处理更多的输入包并生成更多的输出包。当然，在事情开始出错之前，对于进入的每个包，路由器只是将它转发出去，所以，直到事情出了问题，如果数据包以每秒 2000 个的速度到达，它只是将每个输入数据包复制到输出，这意味着输出速率正好等于输入速率，所以这是为什么 y 等于 x ，因为每个输入包都被发送出去，所以有一段时间 y 等于 x 。那么为什么它停止上升？这是不是他们在论文上提到的一件事，它们无法以需要的速率处理中断。这是它为什么会下降的问题的答案。我的问题是，为什么它停止上升，是什么，这个线在设计良好的系统中，如果他们没有搞乱设计的话，你和我会设计一个没有问题的系统，我们的系统会不会跟上这条线，继续往上走。我想不会，因为在某一时刻，数据包到达的速率将会，如果你可以足够快地处理数据包，那么在某一点上，瓶颈将是数据包到达的速率。对，我们现在说的这个系统是有一些限制，你知道 CPU 不是无限快的，无论 CPU 每秒执行多少条指令，不会更多，所以通常这些数据包必须被处理，IP 软件必须查看报头并检查校验值，并查找目地的址和它使用的表，每个数据包需要数百或数千个 CPU 周期。所以我们不能指望这个线永远会上升，它肯定会在某个地方停下来，我们可以确定，我们可以猜测，这个系统最高可达 5000 ，但不会更多，这向我们表明，在这台计算机上处理每个数据包大约需要 200 微秒，这就是这一点，拐点在这里意味着处理包的总成本大约是 200 微秒的 CPU 时间。这是个猜测，但很可能接近正确，所以我们不可能得到，也许我们可以把软件做得更有效率一些，我们可以将其降低到每个包 150 微秒，所以也许我们可以把拐点往上移一点，但是我们确实面临着一些点，就是这个系统可以处理的数据包的数量。这并不一定是这里正在发生的事情，这是这里会发生的事情，但它不是写在[]上，事实上，还有其他瓶颈可能是限制，除了 CPU 时间，值得考虑的最明显的是网速，它们使用的网络仅以每秒 10 兆比特的速度运行，这就是低级网络硬件发送比特的速度，所以，每秒传输的位永远不会超过一万，一千万位。所以，这也可能构成一个限制，所以我们考虑，这是否真的是决定这个 5000 的原因。我不知道，论文上说的不够，知道是 CPU 还是网卡是这里的限制因素，但事实是，它们的 10 兆网络，如果你发送小数据包，那就是 10 兆比特转换成某些东西，比如每秒 10000 或 15000 个数据包，这是网线对输入速率的限制，所以，这远远低于网络所能达到的每秒 10000 或 15000 个数据包，这个网络的能力，所以，几乎可以肯定的是，限制与 CPU 或内存或其他因素有关，而不是网络本身。好的。我们希望在一个设计良好的路由器中看到的是，它需要 200 微秒，来转发数据包，处理数据包，我们希望看到的是，无论如何，路由器实际上每秒可以转发 5000 个数据包，即使负荷很高，所以我们想要的是这条线。这里有一个输出速率与输入速率相匹配，直到你达到系统的容量，每秒 5000 个数据包，然后它继续以每秒 5000 个数据包的速度转发，很可能丢弃其他的。这就是我们想要设计的东西，但实际发生的情况远比这更糟糕，当你将速率提高到 5000 以上时，转发的数据包数量将降至 0 。那么为什么这条线往下走，之前有人提到过。好的，作者想出了一个原因，当你提高输入速率时，每个输入数据包都会生成中断，而中断使用时间，实际上在它们的系统上，中断是相当昂贵的，因为它们涉及将数据包从网卡复制到主内存中，这花了很长时间，因为 CPU 正在做，所以我们知道数据包以每秒 10000 个的速度到达，我们当然不能转发 10000 个，这意味着我们最多只能希望转发 5000 ，然后简单地丢弃剩下的 5000 ，但实际上额外的 5000 个包，每个包都会产生非常昂贵的中断，所以，对于每秒 5000 个以上的额外数据包，我们产生了越来越昂贵的中断，它们有优先权，中断不管你在做什么，它会停止机器，接受中断，因为机器优先考虑这些中断，这意味着每秒每个额外的数据包占用转发代码的 CPU 时间，直到最终 100% 的 CPU 时间在输入中断例程中用完，并且没有 CPU 时间可以用在转发数据的线程上。大家对这个解释满意吗？这个下降，就是所谓的中断活锁。这条线下降，而不是直线，人们所说的中断活锁，这是一个在许多系统中都会发生的现象，我的意思是驱动它的东西是，这里有两个不同的任务，比如输入中断任务和转发任务，并且由于调度问题，优先考虑输入任务，会使包处理任务饥饿，在几乎任何具有多个独立任务或一系列独立任务的系统中，这需要对每个输入完成操作，而输入率不一定是可控的，许多这样的系统都会显示活锁，如果太过用力。你会遇到活锁，可能因为多种资源，不仅是使用 CPU，它可能是网卡，网卡 DMA 耗尽了 RAM 周期来执行 DMA ，如果网卡正在使用 RAM ，CPU 就不能使用 RAM ，所以另一种获得活锁的方法，即使你有很多 CPU 时间，一些其他的设计，你也可能会得到活锁，由于网卡正在耗尽 RAM 资源，所以 CPU 使用 RAM 的能力变差。不管怎样，这条线下降就是他们所说的活锁。你可能会问，多余的数据包会发生什么情况，如果你回想一下他们的软件结构，它们有网卡，处理接收中断，接收中断软件复制每个数据包到一个队列，然后有某个网络线程，将数据包从队列中拉出，数据包丢失的确切位置就在这里。接下来会发生的是中断例程，一旦因为活锁，下降到这里，中断例程将填充这个队列，这里会有最大队列长度，至少所有的内存，但可能要少得多。中断例程从网卡上拉出一个数据包，看到这个队列已经达到了允许的长度，中断例程将丢弃数据包，当然在那之后，还会有另一次中断，中断例程将再次丢弃下一个数据包，因为网络线程总是被中断，并且永远不允许运行。关于这张图，有什么问题吗？这是论文中最重要的图。好的，我们快没时间了，所以我会试着把这个问题的答案压缩到一分钟内，作者提出了一个解决方案，关于这个解决方案最直接的好消息是，这是他们的解决方案的性能，也就是说，输入率上升到 5000 ，然后是平坦的 5000 ，无论输入率是多少。所以这是一条完美的非活锁性能线，当然会是平坦的，因为你每秒只能处理 5000 个数据包，CPU 的速度，他们得到这个的方式，他们仍然有这个网络线程，而且他们仍然有一个中断例程。所以，当第一次网卡中断时，运行中断例程，但是中断例程不从网卡复制数据包，它唤醒网络线程，然后使网卡上的中断处于禁用状态，所以我们不会再被中断，唤醒网络线程，然后中断例程返回，所以现在这个网卡中断关闭，网络线程有一个循环，它检查网卡，从网卡中提取几个数据包，5 个，我想这是他们最终使用的，然后处理这些数据包。如果没有的话，如果这个检查，这个网络线程从网卡读取数据包，而不是中断例程，如果没有等待的数据包，它会启用中断，然后进入休眠状态，因为它启用了中断，下次数据包到达时，中断例程将唤醒这个线程，从睡眠中醒来，回到循环的开端。这就是他们解决方案的结构，看待这个的一种方式是，他们将中断方案转变为轮询方案，在高负荷的情况下，它们只是在这个循环里，它们在中断关闭的情况下读取包并处理，读取包并处理，因为中断是关闭的，所以永远不会有这个效果，中断从主线程窃取时间，而在低负载下，它们启用中断，在数据包到达之前可能需要一段时间，但是，它们会立即被中断例程唤醒，如果一个数据包到达。这就是我要说的全部。有什么问题吗？我有个问题，这个循环是查看所有设备，还是只查看生成中断的设备？如果有多个网卡，这是一个好问题，如果有多个网卡，这个循环，我其实不知道循环是如何工作的，一个非常合理的设计是让这个网络线程跟踪，对于每个网卡，无论是中断模式还是轮询模式，然后它只会，实际上，因为中断例程不再从网卡读取数据包，这意味着在这一点上循环可能会检查每个网卡，在这一点上，可能检查每个网卡，然后拉出，对于每个网卡，它都有几个数据包，如果网卡有数据包在等待，循环将从网卡中取出几个并对其进行处理，如果所有的网卡都没有，如果你检查了所有网卡，但没有任何数据包在等待，这个循环将在所有网卡上启用中断，然后休眠，任何网卡中断都会唤醒它，这是我的猜测。好的，听起来很合理。我有一个简短的问题，当循环运行时，数据包是如何进入要拉取的队列的，我觉得一次只有一个。数据包最初在网卡内部的私有内存中排队，然后这个循环，当它回到循环的开端时，它查看每个网卡，并与网卡硬件通信，询问内存里有没有什么输入包在等待，如果是这样，那么这个循环将在 RAM 中分配一个数据包缓冲区，并将数据包中的字节从网卡复制到数据包缓冲区，然后处理那个数据缓冲区。所以，它可以复制多个数据包。是的，我想它们是以 5 个一组的，为了，即使这里有一百个包在等待，它只处理接下来的 5 个，以便在输入网卡之间保持公平，从而避免饥饿。但这可能可能需要增加网卡的内存容量，是吗？嗯，我不知道，我不知道多少，网卡可能有相当数量的，这个中断的问题是，这个活锁现象，在这个点以下，可能是在中断，如果数据包到达，网络线程将立即被唤醒，并将数据包拉出，在这里，在这个机器中，有太多的数据包到达，这个循环在轮询，而不是中断，数据包会丢失，我们知道，因为不同，这个输入率和输出率之间的差别，这都是丢弃的数据包，增加，我不认为增加，因为这些数据包无论如何都会被丢弃，增加网卡缓冲区可能不会有太大帮助。我不，我不认为网卡，目前还不清楚网卡需要更多的缓冲区。是的，有道理，是的，你只需要达到饱和所需的量就行了。所以，我想在他们的设计中，它们会拉出 5 个包，配额是 5 个包，所以网卡需要 5 个数据包缓冲，这是有道理的，但是可能，无论如何，还不清楚比这更多是否会更好。实际上，缓冲区的目的是吸收瞬时突发，整个数据包足够长让软件可以继续读取，但在这种情况下，我们不谈论瞬变现象，我们谈论的是持续性超载。所以就是这样，这就意味着来提供大量的缓冲没有更多功能。是的，理解了。我想我的问题与此有关，如果这里打开中断和关闭中断之间的区别是，它会是一样的，但它仍然会把东西放在队列上，如果它能做到的话，但它不会发出中断，但如果队列上没有位置，那么它就会丢弃。这个新方案中的中断例程从不查看数据包。哦，是的，我是说，你说过，你关闭了网卡的中断，对吧。是的，这里中断处理程序中发生的事情是，它禁用这个网卡上的中断，然后唤醒网络线程，这就是它所做的一切，然后返回。好的，我想我的问题是当中断禁用时，网卡是否仍然可以将数据包放在自己的缓冲区中？是的，网卡是独立的，所以它有内部缓冲，无论是否启用或禁用中断，当数据包到达时，唯一发生的事情是，网卡将数据包附加到它的数据包队列上，在它自己的内部内存中，这就是数据包到达时发生的所有事情。所以当网卡，在本论文的网卡中，不同的设计是非常不同的，但对于这个论文，他们的网卡从未进行过 DMA ，也从未接触过主机内存，它在自己的内存中保留了一个内部队列，如果主机愿意，可以读出数据包。好的，我明白了，如果没有内存的话，那么。丢弃。好的。所以在这个设计中，如果数据包是这些多余的数据包，它们被丢弃的位置是在网卡内，发生的是数据包或[]，如果我们处于过载状态，那么网卡队列将总是或几乎总是满的，所以，当数据包到达时，网卡队列将是满的，它将丢弃，网卡将丢弃数据包，而不会浪费机器上的任何 CPU 时间。好的，好的。事实上，它可以在不消耗 CPU 时间的情况下丢弃，是解释它如何避免活锁的一种方式。好的，好的，非常感谢。我有一个简短的问题，会不会有这样一种情况，在这种情况下， CPU 会拉出几个数据包，但是内部软件队列都是满的。哦，当然。是的，还有其他一些瓶颈，比如，假设这些传入数据包，其中一些需要传送到套接字上的某个应用程序，如果那个应用程序读取数据包速度不够快，那么套接字缓存导致，那个应用程序正在读取的套接字缓冲区将被填满，然后数据包可能在网络线程中丢弃，这也可能导致活锁，因为现在因为我们有相同的，活动锁出现的原因是，我们扩展了资源处理数据包，后来被丢弃，所以这是白费力气。如果应用程序没有读取它的，好的，我们可以获得活锁的一种方法是，随着负载的增加，也许我们使用 100% 的时间在网络线程上，为应该读取数据包的应用程序留出 0% 的时间，然后我们会再次得到活锁。但它不是中断活锁，它是网络处理活锁或其他什么，这个论文有关于这方面的描述，在第六部分的某个地方，他们谈到了网络，如果数据包被递送到本地应用，网络线程将查看那个应用的套接字队列，如果它变长了，它会关闭中断，并停止从网络接口拉出数据包，直到队列变短。这意味着网络线程将停止运行，给应用程序一个运行和处理数据包的机会，所以你可以在任何阶段得到活锁，如果你在多阶段处理方案中不小心的话。好的，有道理，谢谢。谢谢。谢谢。不客气。

### 以太网数据包格式



**控制流**

- 多个独立的角色（中断处理程序、线程）

- 边界处的队列

- 典型设置如下（有很多变化；实验室设置更简单）

  - - NIC 物理层（通常有多个 NIC！）

    - NIC 内部缓冲区

  - - NIC DMA 引擎

    - DMA RX 环

  - - rx 中断处理程序，将数据从 NIC 复制到软件输入队列

    - IP 输入队列

  - - IP / ARP / TCP / UDP 输入处理；PCB、ARP 和路由表

    - 套接字队列

  - - 应用程序

  - - TCP / UDP / IP 输出处理

    - IP 输出队列

  - - tx 中断处理程序，将数据从软件输出队列复制到 NIC

    - NIC DMA TX 环

  - - NIC

**注意：**

- 包缓冲区与分配器（见 net.h 中的 struct mbuf）
- 每一层在处理过程中解析、验证并剥离头部，发现问题时丢弃数据包
- 每一层在处理过程中会在前面添加头部

**为什么需要所有这些缓冲队列？**

- 吸收临时输入峰值，以避免在软件忙时丢弃数据包
- 在计算时保持输出 NIC 忙碌
- 允许独立的控制流（NIC 与网络线程与应用程序）

其他可能的安排，有时效果更好

- 例如，用户级堆栈
- 例如，直接用户访问 NIC（见 Intel 的 DPDK）
- 例如，轮询，如今天的论文所述

NIC 数据包缓冲

- 论文中的 NIC 内部缓冲数据包
  - 驱动程序软件必须从 RAM 中复制
- 实验室作业中的 NIC（Intel e1000）使用 DMA 从主机 RAM 读写
  - DMA 比软件复制循环更快
  - DMA 可以与计算任务同时进行
- 中断在有 DMA 时仍然有用
  - 提醒软件新到达的数据包
  - 告诉软件 NIC 完成发送，缓冲区可以释放

## 内核中的协议栈



## 过载行为



# 论文阅读：消除接受活锁

[Eliminating Receive Livelock in an Interrupt-driven Kernel, 1996](https://pdos.csail.mit.edu/6.828/2023/readings/mogul96usenix.pdf)

## 概要

大多数操作系统使用接口中断来调度网络任务。在低负载下，基于中断的系统能够提供较低的开销和良好的延迟，但在到达率较高时，系统性能会显著下降，除非采取措施防止多种病态现象。其中之一是接收活锁的各种形式，系统在这种情况下会一直处理中断，从而排斥其他必要任务。在极端情况下，系统无法将任何数据包传递给用户应用程序或输出。

为避免活锁及相关问题，操作系统必须像调度进程执行一样仔细地调度网络中断处理。我们修改了一个基于中断的网络实现，进行这样的调度，这消除了接收活锁问题，同时不降低系统性能的其他方面。我们展示了一些测量结果，证明了我们方法的成功。

## 1. 介绍

现代系统可以在几到几十微秒内响应中断；如果使用轮询实现相同的延迟，系统需要每秒轮询数万次，这会产生过多的开销。对于通用系统，基于中断的设计效果最好。

许多多媒体和客户端-服务器应用程序还具有另一种不利的特性：与传统的网络应用程序（如Telnet、FTP、电子邮件）不同，它们没有流量控制机制。一些多媒体应用需要恒定速率、低延迟的服务；基于RPC的客户端-服务器应用通常使用数据报式传输，而不是可靠的、流控的协议。需要注意的是，像磁盘这样的I/O设备只有在操作系统发出请求时才会生成中断，**因此它们本质上是流控的**，而网络接口则会生成未经请求的接收中断。

事件速率的提升和非流控协议的使用可能使主机陷入拥塞崩溃：一旦事件速率饱和系统，如果没有负反馈循环来控制源头，系统无法优雅地削减负载。如果主机在这种情况下仍然以满负荷运行。

基于中断的系统在过载情况下表现不佳。在中断级别执行的任务，按定义具有绝对优先级，超过所有其他任务。如果事件速率高到让系统全部时间都用于响应中断，那么其他任务将无法执行，系统吞吐量将降至零。我们称这种情况为**接收活锁(receive livelock)**：系统并没有死锁，但它无法在任何任务上取得进展。任何纯粹的基于中断的系统，使用**固定中断优先级**的设计，在输入过载的条件下都会遭遇接收活锁。

然而，我们不应轻易放弃基于中断设计的明显优势。相反，我们应将网络中断处理子系统的控制与操作系统的调度机制和策略相结合

## 2. 相关应用

- **基于主机的路由**， 虽然网络间路由传统上是使用专用的（通常是非中断驱动的）路由器系统完成的，但路由往往是使用更常规的主机完成的。几乎所有的互联网“防火墙”产品都使用 UNIX
- **被动网络监控**：网络管理员、开发人员和研究人员通常使用 UNIX 系统，并将其网络接口置于“混杂模式”下，监控局域网上的流量，用于调试或统计数据收集。
- **网络文件服务**：NFS 等协议的服务器通常是由 UNIX 系统构建的。
  这些应用（以及其他类似的，如 Web 服务器）都可能面临重载的、没有流量控制的负载风险。

本文其余部分集中讨论基于主机的路由问题

## 3. 调度网络任务的要求

理想情况下，通信子系统能够处理最坏情况下的输入负载而不会饱和（即不超出其处理能力），但现实中由于成本的限制，通常无法构建如此强大的系统。在在过载情况下，目标期望是实现可控和渐进的性能下降。

当终端系统处理大量网络流量时，其性能在很大程度上依赖于任务的调度方式。用于调度数据包的场景应该保证系统能够维持可接受的**吞吐量throughput**、合理的**延迟latency**和**抖动jitter**（延迟变化）、**公平fair**的资源分配，以及总体的系统**稳定性stability**，尤其是在系统过载时，不应产生过多的开销。

我们可以将吞吐量定义为系统将数据包交付到最终使用者的速率，我们期望设计良好的系统在达到“**最大无损接收速率(Maximum Loss Free Receive Rate, MLFRR)**”之前，能够跟上提供的负载，并且在更高负载下，吞吐量不应低于这个值。当然，有效的吞吐量不仅仅取决于成功接收数据包；系统还必须传输数据包。

许多应用（如分布式系统和交互式多媒体）往往比起高吞吐量，更依赖低延迟和低抖动的通信。即使在过载时，我们也希望**避免长队列**（这会增加延迟）和**突发性调度**（这会增加抖动）。当主机因收到大量网络数据包而过载时，它还必须继续处理其他任务，以保持系统对管理和控制请求的响应能力

## 4. 中断驱动调度及其后果

在本节中，我们首先描述中断驱动系统的特性，然后确定在网络输入过载下中断驱动系统产生的三类问题：

- 在过载下接收活锁（receive livelock）：只要输入过载持续，实际吞吐量将降为零
- 包传递或转发的**延迟**增加：系统在处理中断或后续包时，可能会延迟某个包的传递
- 包传输**饥饿Starvation**：即使CPU能够跟上输入负载，严格的优先级分配可能会阻止任何包的传输

### 4.1 中断驱动系统描述

网络输入优先处理任务的方式， 当包到达时，网络接口通过中断CPU来信号化这一事件。设备中断通常具有固定的中断优先级水平（IPL），并且会抢占所有在较低IPL下运行的任务；但不会抢占与其相同IPL下的任务。中断触发进入相应的网络设备驱动程序，设备驱动程序然后将包放入队列，并生成一个软件中断以进行进一步处理。软件中断的优先级较低，因此协议处理可以被后续中断抢占。不同IPL级别的步骤之间的队列提供了针对瞬时过载时丢包的隔离保护。

**操作系统的调度器并不参与这类活动，事实上它对此完全不知情**。

调度中断是一个代价高昂的操作，为了避免这种开销，网络设备驱动程序试图批处理中断，这将中断处理的开销分摊到多个包上。即使批处理，中断驱动系统在输入包过载时将花费大部分时间在设备IPL级别的代码上，也就是说，该设计给予了处理输入包绝对的优先级。

现代网络适配器可以在不需要主机干预的情况下接收多个连续的包，或者通过大量的缓冲，或者通过高度自主的DMA引擎。这使系统得以与网络隔离。过去给接收数据包的初步处理步骤赋予绝对优先级的理由不再那么重要了。

### 4.2 接受活锁

随着输入负载的增加，系统可能表现出三种行为：

- 在理想系统中，交付的吞吐量总是匹配输入负载。
- 在可实现的系统中，交付的吞吐量可以跟上输入负载，直到达到最大无丢失接收速率（MLFRR），在此之后吞吐量相对恒定。在超过MLFRR的负载下，系统仍然可以前进，但会丢弃一些输入数据，通常是在不同优先级的处理步骤之间的队列中丢包。
- 在易于出现接收活锁的系统中，随着输入负载的增加，超过MLFRR的负载下，吞吐量会下降。接收活锁发生在吞吐量降至零的点。

系统发生活锁（livelock）时，尽管系统投入了很多精力去部分处理接收到的数据包，但这些数据包最终都会被丢弃，因此所有的处理工作都白费了。接收中断批处理使情况变得稍微复杂一点。通过在重负载下提高系统效率，批处理可以提高MLFRR，但仅靠批处理无法防止活锁。

### 4.3 过载下的接收延迟

虽然通常认为中断驱动设计可以减少延迟，但它实际上可能会增加包传递的延迟。如果一组包快速突发到达，系统将对整个突发组执行链路层处理，而不是优先处理第一个包，因为链路层处理的优先级更高。因此，突发组的第一个包在链路层处理完成前不会传递给用户。第一个包的传递延迟几乎增加到接收整个突发组的时间。如果这个突发组包含多个独立的NFS RPC请求，这意味着服务器的磁盘会空闲，无法执行有用的工作

### 4.4 过载下的传输饥饿

在大多数系统中，包传输过程包括从输出队列中选择包，将其交给接口，等待接口发送包，然后释放相关缓冲区。包传输通常的优先级低于包接收。这种策略表面上看是合理的，因为它最小化了在突发到达的包超过可用缓冲空间时的丢包概率。然而，合理运行高层协议和应用程序需要传输处理取得足够的进展。

当系统长时间过载时，传输优先级的固定较低可能会导致吞吐量减少，甚至包传输完全停止。包可能在等待传输，但传输接口空闲。我们称这种情况为传输饥饿。

## 5. 通过更好的调度避免接受活锁

在本节中，提出几种避免接收活锁的技术。这些技术包括控制中断到达率的机制、基于轮询的机制，以及避免抢占的技术。

### 5.1 限制中断到达率

我们可以通过限制系统中中断的到达率来避免或延迟接收活锁。系统会检查中断处理是否占用了过多资源，如果是，暂时禁用中断。

系统可以通过以下方式推测是否即将发生活锁，以及有相应的办法。

1. 由于队列溢出而丢包。相应办法： 因为内部队列已满时，则表明应当禁用输入中断。还需要一个触发器来重新启用输入中断，以防止不必要的数据包丢失。可以在内部缓冲区空间变得可用时或计时器到期时重新启用中断
2. 高层协议或用户代码处理太慢了导致丢包。相应办法：在具有精细时钟寄存器的处理器上，计算出用户代码执行时间。如果这个总和（或一个运行平均值）超过了总经过时间的指定比例，内核就会禁用输入中断。
3. 在没有精细时钟的系统上，可以通过在每个时钟中断时采样CPU状态（时钟中断通常会抢占设备中断处理）来粗略模拟这种方法。如果系统发现自己正处于处理中断的状态中，它可以禁用中断几次时钟周期

### 5.2 使用轮询

限制中断率可以防止系统饱和，但是无法保证系统在处理数据包时的进展。也就是说，系统还必须公平地分配数据包处理资源，包括输入和输出处理之间的资源以及多个（网络）接口之间的资源。我们可以通过仔细轮询所有数据包事件源，使用**轮询调度round-robin**来提供公平性。

在纯轮询系统中，调度程序会调用设备驱动程序来“监听”接收到的数据包和传输完成事件。这将控制设备级别的处理量，并且可以公平地分配资源，从而避免活锁。然而，简单地以固定间隔轮询会增加数据包接收和传输的延迟。

我们采用了混合设计，其中系统仅在中断触发时轮询，而中断仅在轮询暂停期间发生。在负载较低时，数据包到达不可预测，我们使用中断以避免延迟。在负载较高时，我们知道数据包以或接近系统的饱和率到达，因此我们使用轮询来确保进展和公平性，只有在没有更多工作待处理时才重新启用中断

### 5.3 避免抢占

接收活锁发生是因为中断处理抢占了所有其他数据包处理。我们可以通过使higher-level packet processing不可抢占来解决这个问题。我们观察到这可以通过以下两种通用方法之一来实现：

- 在高IPL（中断优先级级别）下可几乎做所有事情，或者
- 在高IPL下几乎什么都不做。

按照第一种方法，我们修改4.2BSD设计，消除软件中断，使用轮询机制来检查事件，和在设备IPL下完成处理接收到的数据包。由于数据处理发生在设备IPL下，它不能被其他数据包到达所抢占，从而保证内核协议栈中不会发生活锁。我们仍然需要使用速率控制机制来确保用户级应用程序的进展。

第二种方法中，流程为

1. 当设备发生中断时，中断处理程序被调用
   - 任务是设置一个“需要服务”的标志，并只是系统某些设备需要服务
2. 中断处理程序调度一个轮询线程（这个线程尚未运行的话）
3. 轮询线程启动，它运行在系统中最低的中断优先级（零IPL），这意味着它的运行不会被其他中断打断。它的任务是
   - 检查“需要服务”的标志
   - 处理相应的设备
4. 一旦处理完所有的任务，他就会结束运行
5. 恢复中断

这两种方法都消除了在设备驱动程序和高层协议软件之间排队数据包的需要，尽管如果协议栈必须阻塞，接收到的数据包必须在稍后的时刻排队。（例如，这会发生在数据准备好交付给用户进程时，或者当接收到IP片段且其伴随片段尚未可用时。）

**5.4. 技术总结**

总之，我们通过以下方式避免活锁：

- 仅使用中断来启动轮询。
- 使用轮询以公平地分配资源给事件源。
- 当内部队列满或CPU使用限制表明其他重要任务待处理时，暂时禁用输入。
- 早期丢弃数据包，而不是晚期丢弃，以避免浪费工作。一旦决定接收一个数据包，我们尽力将其处理到完成。
- 通过重新启用中断在没有待处理工作时避免轮询开销并保持低延迟。
- 让接收接口缓冲突发数据包，以避免丢弃数据包。
- 消除IP输入队列及相关开销。

我们观察到，低效的代码往往会加剧接收活锁。积极的优化、"快速路径"设计和去除不必要的步骤都有助于延迟活锁的到来。

## 6. BSD路由器中的活锁问题

我们考虑了一个具体的例子，  IP 数据包路由器。(1) 实现最高可能的最大吞吐量；(2) 即使在过载情况下也保持高吞吐量；(3) 为用户模式任务分配足够的 CPU 周期；(4) 最小化延迟；以及 (5) 避免影响其他应用程序的性能。

### 6.1 测量方法

源主机以各种速率生成 IP/UDP 数据包，并通过路由器将它们发送到一个虚假的目的地址（我们通过在路由器的 ARP 表中插入一个虚假条目来欺骗路由器）。我们通过计算在给定时间内成功转发的数据包数量来测量路由器的性能，从而得出平均转发速率。

数据包生成器发送了 10000 个 UDP 数据包，每个数据包携带 4 字节的数据。该系统不会生成精确节奏的数据包流；报告的包速率是平均数，短期速率会有些波动。我们通过使用 "netstat" 程序（在路由器上， ``netstat -i``）在每次试验前后采样输出接口计数 ("Opkts") 来计算交付的数据包速率

### 6.2 未修改内核的测量

![image-20240909222418571](http://47.115.50.83:49153/i/66df05246ae53.png)

我们首先测量了未修改的操作系统的性能，如图 6-1 所示。每个标记表示一个试验。填充的圆圈表示基于内核的转发性能，空心方块表示使用 screend 程序的性能，该程序在一些防火墙中用于筛选不需要的数据包。

在这种情况下，screend 被配置为接受所有数据包。从这些测试中可以明显看出，使用 screend 时，路由器在速率超过 2000 个数据包/秒时表现不佳，在约 6000 个数据包/秒时完全发生活锁。即使没有 screend，路由器的峰值为 4700 个数据包/秒，可能会在接近以太网最大包速率（约 14,880 个数据包/秒）时发生活锁。

### 6.3 为什么在 4.2BSD 模型中会发生活锁

![截屏2024-09-09 22.27.55](http://47.115.50.83:49153/i/66df05f67b1d7.png)

4.2BSD 遵循了第 4.1 节中描述的模型，如图 6-2 所示。设备驱动程序运行在中断优先级级别 (IPL)比  IP 层通过软件中断的 高。驱动程序和 IP 代码之间的队列名为 "ipintrq"，每个输出接口都有自己的缓冲队列。所有队列都有长度限制，超出的数据包会被丢弃。该系统中的设备驱动程序实现了中断批处理，因此在高输入速率下实际处理的中断很少。

一旦输入速率超过设备驱动程序从接口中拉取新数据包并将其添加到 IP 输入队列的速率，IP 代码就不会运行。因此，它永远不会从其队列（ipintrq）中移除数据包，队列会填满，所有后续接收的数据包都被丢弃。系统的 CPU 资源被饱和，因为它在提高的 IPL 级别上投入了大量 CPU 时间处理每个数据包。这是愚蠢的

### 6.4 解决活锁问题

我们通过尽可能在内核线程中完成工作，而不是在中断处理程序中，并通过消除 IP 输入队列及其相关的队列操作和软件中断（或线程调度）来解决活锁问题。一旦决定从接收接口取出数据包，我们尽量避免在之后丢弃它，因为这代表了浪费的工作量。

我们改为让这个线程使用轮询技术来高效地进行轮询调度。轮询线程使用附加的启发式方法来帮助实现我们的性能目标。

0. 在boot阶段，修改后的接口驱动程序会向轮询系统注册自己，提供处理接收和发送数据包的回调程序，启用中断。

1. 当数据包到达时，接口驱动程序的中断处理程序被执行。
   - 但是几乎不做任何工作，它只是调度轮询线程（如果它尚未被调度）
   - 记录接口”需要服务“的状态
   - 从中断返回（注意，并没与重置设备的中断使能标志，因此系统在轮询线程处理所有待处理的数据包之前不会被额外的中断打扰志）
2. 当轮询线程被调度时，它会检查所有注册的设备，查看它们是否请求处理，并调用相应的回调程序来完成中断处理程序原本需要做的工作。
   - 接收数据包的回调程序直接调用 IP 输入处理例程，而不是将接收的数据包放在队列中以便稍后处理。
   - 如果系统落后，接口的输入缓冲区将暂时储存数据包，任何多余的数据包将在系统浪费资源之前由接口丢弃。
   - 轮询线程给回调程序分配处理数据包的**配额**。一旦回调程序用完配额，**它必须返回到轮询线程**。
   - 轮询线程在多个接口之间以及在每个接口上的输入和输出处理之间进行轮询，以防止单个输入流独占 CPU
3. 一旦接口上的所有数据包都被处理完，轮询线程调用驱动程序的中断使能回调
   - 确保在后续的数据包事件发生时触发中断

### 6.5 结果和分析

![截屏2024-09-09 22.59.09](http://47.115.50.83:49153/i/66df0d6fede97.png)

图6-3总结了未使用screend程序时，我们的更改带来的结果。图中展示了几种不同的内核配置，使用不同的标记符号进行表示。修改后的内核（用方形标记表示）稍微提高了MLFRR（最大丢包前接收率），并在较高的输入速率下避免了活锁。

修改后的内核可以配置为表现得像一个未修改的系统（用空心圆表示），但其性能似乎略逊于实际未修改的系统（用实心圆表示）。

### 6.6 调度启发式方法

图6-3显示，如果轮询线程不对回调程序设定配额（能处理的数据包数量），当输入速率超过MLFRR时，总吞吐量几乎下降到零（图中用菱形表示）。这种活锁的出现是因为，尽管数据包不再在IP输入队列中被丢弃，但它们仍在堆积，并最终在输出接口队列中被丢弃。由于无法保证输出接口的运行速度与输入接口相同，这种输出队列的拥堵是不可避免的。

> 为什么系统未能清空输出队列？

如果数据包到达得太快，输入处理回调程序无法完成其工作。这意味着轮询线程无法调用输出接口的处理回调程序，这阻止了释放发送缓冲描述符，以便进一步传输数据包。这类似于在4.4节中描述的传输饥饿问题。在没有配额的修改内核中，结果实际上更糟糕，因为在该系统中，由于输出队列空间不足，数据包被丢弃，而不是在IP输入队列中被丢弃。

#### 6.6.1 来自满队列的反馈

![image-20240909231326927](http://47.115.50.83:49153/i/image-20240909231326927.png)

当使用screend程序时，修改后的系统表现如何？图6-4比较了未修改的内核（实心圆）和多个修改后的内核。按前面描述修改的内核（用方形标记表示）的性能与未修改的内核几乎一样差。问题在于，由于screend运行在用户模式下，内核必须将数据包排队以交付给screend。当系统过载时，该队列会填满并导致数据包丢弃。screend无法运行来清空此队列，因为系统将其所有的周期都用于处理输入数据包。

为了解决这个问题，**我们检测到筛选队列变满时，会抑制进一步的输入处理（以及输入中断**），直到队列中有更多可用空间。结果如图6-4中的灰色方形标记所示：没有活锁，峰值吞吐量大幅提高。通过队列状态反馈，系统能够正确分配CPU资源以使数据包顺利通过整个系统，而不是在中间点被丢弃。

在这些实验中，轮询配额设置为10个数据包，筛选队列的限制为32个数据包，当队列达到75%满时，系统会抑制输入处理。当筛选队列减少到25%满时，重新启用输入处理。我们任意选择了这些高水位和低水位标记，适当的调优可能会有所帮助。我们还设置了一个超时（任意选择为一个时钟周期，约1毫秒）， 在超时后重新启用输入处理，以防screend程序挂起，这样不会无限期丢弃其他消费者的数据包。

同样的队列状态反馈技术也可以应用于系统中的其他队列，如接口输出队列、用于网络监控的数据包过滤器队列等。对于这些队列，反馈策略会更加复杂，因为可能很难确定输入处理负载是否确实阻碍了这些队列的进展。然而，由于screend通常作为系统上的唯一应用程序运行，因此筛选队列满了是一个明确的信号，表明输入数据包过多。



#### 6.6.2 配合数量的选择

![截屏2024-09-09 23.19.10](http://47.115.50.83:49153/i/66df11ffeb605.png)

为了在非screend配置中避免活锁，我们必须为每次回调处理的数据包数量设置配额，因此我们研究了随着配额变化系统吞吐量的变化情况。
图6-5显示了结果；**较小的配额表现更好。随着配额增加，活锁问题变得更加严重。**
然而，当使用screend时，队列状态反馈机制可以防止活锁，较小的配额会略微降低最大吞吐量（约5%）。我们认为，通过每次回调处理更多的数据包，系统可以更有效地摊销轮询的成本，但增加配额也可能增加每个数据包的最坏情况下的延迟。

一旦配额足够大，能够使筛选队列填满一波数据包时，反馈机制可能会掩盖任何潜在的改进空间。

![截屏2024-09-09 23.20.36](http://47.115.50.83:49153/i/66df12531e007.png)

图6-6显示了在使用screend时的测试结果。总之，无论是否使用screend的测试表明，在测试的硬件配置下，10到20个数据包的配额可以实现稳定且接近最优的行为。对于其他CPU和网络接口，适当的值可能会有所不同，因此这一参数应该是可调的。

总之，无论是否使用screend的测试表明，在测试的硬件配置下，10到20个数据包的配额可以实现稳定且接近最优的行为



## 7.  保证用户级进程的进展

第6.4节描述的轮询和队列状态反馈机制可以确保在输入超载时，数据包处理的所有必要阶段都能取得进展。然而，它们并没有考虑到其他活动的需求，因此用户级进程可能仍然会因CPU周期不足而被饿死。这会导致系统的用户界面无响应，并干扰后台维护任务（例如路由表维护）。

我们通过在修改后的路由器上运行一个计算密集型进程，并向该路由器发送最小尺寸的数据包洪流，验证了这一效应。路由器以全速转发这些数据包（即仿佛没有用户模式进程在消耗资源），但用户进程几乎没有任何进展。

由于问题的根源在于数据包输入处理子系统占用了过多的CPU，我们可以通过简单地测量处理接收数据包所消耗的CPU时间，并在超过某个阈值时禁用输入处理，从而缓解这一问题。

我们在实验使用的Alpha架构中，包含一个高分辨率且低开销的计数器寄存器。该寄存器会计数每条指令周期（在当前实现中），并且可以在一条指令内读取，而不会产生任何数据缓存未命中。其他现代的RISC架构也支持类似的计数器。我们测量了一个周期内处理数据包代码所使用的CPU周期总量（这个周期目前被任意选择为多个时钟周期，约为10毫秒，以匹配调度器的量子）。每个周期结束时，一个定时器功能会清除数据包处理代码使用的CPU周期总量。每次修改后的内核开始它的轮询循环时，会读取周期计数器，并在循环结束时再次读取它，以测量在该轮询循环期间处理输入和输出数据包所花费的周期数（配额机制确保这个间隔相对较短）。然后，这个数字会被添加到运行总计中，如果总数超过阈值，则立即禁用输入处理。

在当前周期结束时，定时器会重新启用输入处理。系统的空闲线程执行时也会重新启用输入中断，并清除当前计数总量。

通过将阈值调整为周期总数的某个比例，可以相当精确地控制处理数据包所占用的CPU时间。我们尚未为这种控制实现编程接口；在我们的测试中，我们只是简单地通过修改内核中的全局变量来代表分配给网络处理的百分比，而系统会自动将这个值转换为对应的周期数。

![image-20240909233300382](http://47.115.50.83:49153/i/66df153cab3bf.png)

图7显示了对于不同的周期阈值设置和各种输入速率情况下，一个计算密集型用户进程可以获得的CPU时间。曲线显示了随着输入速率的增加，系统行为相对稳定，但用户进程获得的CPU时间并没有达到阈值设置所暗示的水平。部分差异来源于系统开销；即使在没有输入负载的情况下，用户进程也只能获得大约94%的CPU周期。此外，周期限制机制抑制了数据包的输入处理，但没有抑制输出处理。在较高的输入速率下，在输入被禁止之前，输出队列会填满并消耗更多的CPU周期。

## 8. 总结和结论



在接收过载情况下表现不佳的系统无法提供一致的性能和良好的交互行为。活锁（livelock）从来不是应对过载的最佳解决方案。在本文中，我们展示了如何理解系统过载行为，并通过仔细调度数据包处理的时间来改善系统表现。

通过对UNIX系统的测量，我们证明了传统的中断驱动系统在过载情况下表现不佳，导致接收活锁以及发送饥饿问题。因为在这些系统中，随着数据包进入系统的深度增加，其处理优先级逐渐降低，过载时会出现过多的数据包丢失和无用的工作。这种病态现象不仅可能由长期的接收过载引起，也可能由短期的突发负载引起。

我们描述了一系列调度改进措施，帮助解决过载表现不佳的问题。这些改进包括：

- 限制中断到达速率，以减轻过载
- 通过轮询机制提供公平性
- 完整地处理接收到的数据包
- 明确调控用于数据包处理的CPU使用

我们的实验表明，这些调度机制提供了良好的过载处理表现，并消除了接收活锁问题。这些改进对专用系统和通用系统都具有帮助。
