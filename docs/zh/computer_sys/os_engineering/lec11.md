# Lec 11 调度

任何操作系统都可能会运行比计算机 CPU 数量更多的进程，因此需要一个计划来在进程之间共享 CPU。理想情况下，这种共享对用户进程是透明的。一种常见的方法是通过将多个进程多路复用到硬件 CPU 上，为每个进程提供虚拟 CPU 的假象。下面我们介绍xv6如何实现这种多路复用技术。

> 为什么要有线程和线程切换？

1. 人们希望计算机能够支持多任务处理
2. 简化程序结构，通过使用线程，可以优雅地分解复杂的程序，降低编程的复杂性
   - 比如实验0的素数筛选的例子
3. 多核并行加速，可以充分利用多核CPU
   - 可将 xv6 内核视为一个多核并行程序



[lec11.md](./lec11.md)

## 11.1 线程

线程可以简单理解为一个**串行执行的流程**，即程序一条指令接一条指令的顺序执行。每个线程都有自己的**状态**（因为我们想要保存下来，之后再恢复它），主要包括：

- 最重要的是，程序计数器： 记录当前执行哪条指令
- 还有当下微处理器的状态（这是支持执行所需的），与编译器的密切相关。
  - 寄存器：保存程序中的变量
  - 堆栈：用于存储函数调用记录和局部变量，反映该线程执行中的当前点

### 线程的交错执行

 如果你有 4 个 CPU ，很明显可以运行 4 个线程，每个 CPU 上运行一个线程，然后每个线程自动获得自己的程序计数器寄存器，程序计数器寄存器是与正在运行的 CPU 关联的，但是如果你有 4 个 CPU 和上千个线程，那么如何在核心上使用线程，不是一个好回答的问题。

一个主要策略是，每个 CPU 如何在不同线程之间切换，所以，如果我有一个 CPU 和上千个线程， xv6 在一段时间内运行一个线程，然后切换并放在一边，保存该线程的状态，并切换到第二个线程，执行一段时间，然后是第三个线程，等等，直到它执行了每个线程的一部分，然后，返回到第一个线程并继续执行。

一个线程系统的不同之处，或者线程系统实例的不同之处是，它们是否共享内存。所以这是很重要一点。一种可能是，你可以有一个单独的地址空间，许多线程在该地址空间中执行，它们可以看到彼此的变动，如果共享内存的一个线程修改了变量，那么其它共享该内存的线程会看到修改。所以，在线程共享内存的情况下，我们需要锁。

### 内核线程与用户线程

- **内核线程**：每个进程在 xv6 中都有一个内核线程，用于执行该进程的系统调用。所有内核线程共享同一个内核地址空间和内核内存。这意味着内核线程在操作系统内核中是共享内存的。
- **用户进程与用户线程**：
  - 每个用户进程拥有一个控制线程，用于执行该进程的用户指令。
  - 每个用户进程有自己独立的地址空间，因此不同用户进程之间不共享内存。
  - **xv6 用户进程**：每个用户进程仅有一个线程，不存在用户级线程的并行执行。因此，进程内部没有多线程共享内存的情况。
- **与 Linux 对比**：
  - **Linux** 支持用户级的多线程，即一个进程中可以有多个线程，且这些线程共享该进程的内存。这使得用户可以编写并行程序，并在多核系统上实现加速。
  - Linux 实现多线程的机制复杂度更高，需要跟踪每个进程的多个线程，而不仅仅是一个线程。
- 其他**多任务执行方法**：
  - 线程并不是唯一支持多任务的方式，还有 **事件驱动编程** 和 **状态机**，这些是非线程技术，用于在多任务之间共享一台计算机。
  - 尽管线程的效率并不总是最高的，但它通常是编写程序时最方便、对程序员最友好的方法。

### 线程系统的主要挑战

1. **线程切换与调度**：
   - **线程切换** 是指从一个线程的执行切换到另一个线程执行。
   - 这个过程称为 **调度**，决定哪个线程应该执行，并在不同线程间交替执行。
   - **调度器** 是负责这一任务的模块，在 xv6 中，每个 CPU 核心都有一个独立的调度器，负责管理核心上的线程切换。

2. **保存与恢复线程状态**：

   - 每次线程切换时，必须 **保存当前线程的状态**（包括寄存器、程序计数器等），以便稍后能恢复并继续执行该线程。

   - 线程切换需要确定哪些状态需要保存，保存到哪里，并在恢复时将状态准确地加载回来。

3. **处理计算密集型线程**：

   - **自愿让出 CPU**：通常，线程可以自愿暂停执行并让出 CPU（如通过系统调用）。这称为 **自愿切换**。

   - **强制收回控制权**：对于长时间运行的计算密集型线程，它们可能不会主动让出 CPU。因此，系统需要通过某种方式 **强制收回控制权**，定期暂停这些计算密集型线程并让其他线程有机会运行。这种方式通常由 **硬件定时器** 驱动，实现 **强制切换**。

#### 处理计算密集型线程

关键思想：定时器中断。 每个核心，每个 CPU 上都有一个硬件，它会产生周期性的中断，而 xv6 或任何操作系统将这些中断传送给内核，所以，即使我们在用户级运行一些循环，比如计算 $\pi $​的前 10 亿位，尽管如此，定时器中断还是会在某个时间点发生，可能是每 10 毫秒一次，将控制从该用户级代码转移到内核中的中断处理程序，这是内核获得控制权的第一步，用来在不同的用户级线程之间切换。

基本的方案是：在中断处理器中，所以我们有处理这些程序的内核中断。我们会看到，内核处理程序让出，这个名称是 yields ，内核处理程序自愿让出 CPU 给调度器，告诉调度器，你现在可以运行其他线程。而这个让出是一种线程切换的形式，它保存了当前线程的状态，这样以后就可以恢复。我们会在这里看到整个流程，实际上，你已经在这里看到了整个流程，因为它涉及到中断，你已经知道整个流程有些复杂，但是基本的想法是定时器中断将控制权交给内核，而内核自愿让出 CPU ，这个术语称为抢占调度。它的意思是，抢占的意思是，即使正在运行的代码不愿意，没有明确地让出 CPU ，定时器中断会夺走控制权，我们会让出给它，抢占式调度的反义词称为自愿式调度。有趣的事情是，抢占式调度在 xv6 等操作系统中的实现是定时器中断强制夺取 CPU ，然后内核自愿让出，切换到那个进程的线程。

> 这里出现一个问题， 怎么确定要让哪个线程上呢？ 

线程状态，xv6中有RUNNING、RUNNABLE，SLEEPING（表示线程等待某个 IO 事件，只有在 IO 事件发生之后运行）等。

这个定时器中断和 yield 所做的是，将一个运行时线程，即被定时器中断的线程，转换为一个可运行线程。RUNNING的线程的PC寄存器在执行它的 CPU 的硬件寄存器中，RUNNABLE没有，因为它现在没有关联CPU，因此，对于RUNNABLE状态，我们需要事先保存好所有 CPU 状态，而对于RUNNING状态的线程，我们需要复制CPU的内容，不是RAM，而是寄存器（也就是程序计数器和 CPU 寄存器），从CPU到内存的某个地方保存他们。作为再次运行该线程的步骤一部分，我们会看到程序计数器，保存的程序计数器寄存器被复制回 CPU ，到调度器决定运行线程的 CPU 的寄存器上。

### 用户线程与内核线程的切换机制

- **用户线程的组成**：
  - 用户程序计数器、用户寄存器、用户堆栈、用户内存。
  - 当程序运行时，它在用户空间中执行，并拥有自己的控制线程，处理用户指令。
- **系统调用或中断时的处理**：
  - 当程序进行系统调用或发生中断时，系统进入内核模式。
  - 用户级的寄存器和程序计数器被保存在 **trapframe** 中，随后内核线程被激活。
  - 切换到内核线程后，CPU 开始使用 **内核堆栈**。此时，用户寄存器不需要被恢复，因为用户线程已经暂停，正在执行的是内核线程。
- **内核线程执行与恢复**：
  - 内核线程执行系统调用或处理中断，之后可能会返回用户空间，恢复用户寄存器和程序计数器。
  - 如果只涉及当前进程，则直接返回用户空间；如果调度器决定切换到另一个进程，则需要进行内核线程间的切换。

### 进程切换机制

进程切换的高层次步骤：

1. **保存当前进程状态**：如果需要切换到另一个进程，首先保存当前进程的内核线程状态（如内核寄存器和内核堆栈）。
2. **调度器选择下一个进程**：调度器决定切换到另一个进程的内核线程。
3. **恢复新进程状态**：恢复下一个进程的内核寄存器和内核线程状态。
4. **返回用户空间**：当内核线程完成任务时，返回到用户空间并恢复该进程的用户寄存器和程序计数器。

#### 示例：进程间的切换

- 假设 `C 编译器` 需要从磁盘读取数据，它会发起系统调用并进入休眠状态，等待磁盘操作完成。

- 此时，调度器可以选择切换到另一个进程，比如 

  ```
  ls
  ```

  - 如果 `ls` 处于可运行状态，调度器会恢复 `ls` 的内核线程状态（包括内核寄存器和堆栈）。
  - `ls` 的内核线程继续执行，可能完成一个系统调用，并最终返回用户空间，恢复用户寄存器，然后继续执行 `ls` 程序。

### 重点总结

- xv6: 不会进行用户到用户的直接切换
  - 每次进程切换都涉及一个 **内核线程的切换**。
  - 切换是从一个进程的内核线程切换到另一个进程的内核线程，然后该进程返回用户空间，恢复用户寄存器并继续执行。一直是这种间接的策略



## 调度器

假设我们有进程一 P1 正在运行，而进程二 P2 是可运行的，但是现在没有运行，还有一些额外的细节，我们在 xv6 中有多个核心，假设我们有两个核心，这意味着在硬件层面上，我们有 CPU0 ，它是其中一个核心，还有 CPU1。更完整的故事是我们如何从执行用户空间到，在用户空间中执行的一个进程，到另一个可运行但没有运行的进程。）

1. **定时器中断与用户进程**

- 当用户空间中的进程 P1 正在运行时，定时器中断触发，强制将控制从用户进程切换到内核空间。
- xv6 使用 `trampoline` 代码保存进程 P1 的用户寄存器和 `trapframe`，随后执行 `usertrap`，它处理这个 trap 或中断。
- 系统调用或其他中断会执行一些内核代码（如 C 代码），使用进程的内核堆栈。

2. 进程让出 CPU

- 假设 P1 进入内核后，通过某种方式（如等待 I/O）决定让出 CPU，最终会调用 `switch` 函数。
- `switch` 保存当前进程的内核线程寄存器，切换到当前 CPU 的调度器线程。每个 CPU 有一个专门的调度器线程。
- 调度器线程的上下文包括堆栈指针等寄存器，`switch` 返回时，会从保存的寄存器恢复，并执行调度器函数。

3. **调度器选择下一个进程**

- 调度器函数通过遍历进程表查找下一个可运行的进程。
- 如果找到一个新进程（如 P2），调用 `switch` 切换到 P2 的上下文。P2 的寄存器恢复后，将开始执行其内核或用户代码。
- 如果没有找到合适的新进程，则可能会返回到 P1。

4. **上下文存储位置**

- 每个进程的内核寄存器状态存储在其进程结构体的 `p->context` 中，而 `trapframe` 只包含用户寄存器。
- 每个 CPU 的调度器上下文存储在 CPU 结构体中。
- 内核线程的上下文可以存储在多个地方，但 xv6 选择将用户寄存器与内核寄存器分开存储，以简化代码结构。

5. **用户进程的自愿让出**

- 用户进程不能直接调用让出 CPU，而是通过系统调用间接完成（例如读操作阻塞时）。等待 I/O 时，内核调用 `yield` 或 `sleep`，将当前进程置于等待状态，并切换到调度器线程。
- 这种让出在计时器中断或 I/O 操作中发生。

6. **每个 CPU 的调度器线程**

- 每个 CPU 都有一个独立的调度器线程及其堆栈，这些堆栈在引导阶段被设置。
- 调度器堆栈和上下文在内核引导时为每个核心初始化。

7. **上下文切换的通用流程**

- 上下文切换指的是从一个进程切换到另一个，包括保存当前线程的寄存器并恢复目标线程的寄存器。
- 在每次上下文切换中，调用 `switch` 函数，它保存当前线程的上下文并切换到另一个线程。

8. **线程与进程**

- 在 xv6 中，一个进程只能有一个线程（用户空间或内核空间）。尽管有时用“线程”来描述用户和内核代码的执行，但 xv6 并不支持真正的多线程，用户线程在 xv6 中要么执行用户级代码，要么执行内核级代码。

9. **进程结构体中的相关字段**

- **`trapframe`**：保存用户级寄存器的结构。
- **`context`**：保存内核线程的寄存器，在进程切换时使用。
- **内核堆栈**：用于保存当前执行的内核函数调用栈。
- **状态变量**：表示进程是否正在运行、可运行、休眠或未分配。
- **锁机制**：保护进程状态的修改，防止多个调度器线程同时操作同一进程。





> 第一部分与我所说的相同，定时器中断强制将控制从用户进程转移到内核，trampoline 代码保存进程一的用户寄存器和 trapframe ，然后执行 usertrap ，它知道该怎么处理这个 trap 或中断，系统调用，比如，所以有段时间，我们执行普通的内核 C 代码，在进程一的内核堆栈上。假设，进程一的内核代码决定让出 CPU ，它有一堆东西，我们可以看到细节，最终调用这个例程切换，是这个故事中的一个中心例程，switch 保存上下文，在上下文一中运行的内核线程的寄存器，所以有两组用户寄存器，用户寄存器， trapframe ，内核线程寄存器在上下文中。switch 从一个线程切换到另一个，但是实际上 xv6 的设计方式，用户线程，抱歉，在 CPU 上运行的内核线程可以切换的唯一位置是，那个 CPU 的调度器线程。所以我们甚至不能直接切换到另一个进程，只能切换到调度器线程，所以这里有一个，完整的线程装置专用于 CPU0 的调度器，因为我们使用的是 CPU ，这个 switch 会切换到调度器线程的之前保存的寄存器，假设是调度器 0 。在 CPU0 的调度器中，switch 会，通过恢复这些寄存器，由于寄存器包括堆栈指针，正如我们看到的， switch 的返回，将会返回到 CPU0 上的调度器函数，调度器函数会进行一些清理，使进程一进入休眠状态，然后它在进程表中查找另一个进程，一个可运行进程，如果它找到了一个，所以我们从这里下来，然后到调度器，如果调度器找到另一个要运行的进程，或者发现进程一是可运行的，并且仍然想要运行，找到进程一，没有别的进程想要运行，但不管怎样，调度器会再次调用 switch ，将上下文切换到进程二，在线程中， switch 再次保存自己的寄存器到自己的上下文中，它们也会是前序保存的上下文，当进程而停止时，这组寄存器将被恢复，进程二将使用前序调用，像进程一样，切换到调度器线程，当它停下来的时候，它调用 switch 返回无论系统调用还是中断，进程二结束，会有一个先前保存的进程二的 trapframe ，它将包含用户寄存器，也可以恢复或返回到用户空间。每个 CPU 都有一个完整的、单独的调度器线程，所以，它们也将被保存，CPU1 的调度器线程的上下文，以及在调度器一上运行的调度器循环，以及任何运行在 CPU1 上的进程，比如进程三或其他，当它决定让出 CPU 时，它将切换到 CPU 为它的调度器线程。好的，这里有一个问题，上下文存储在哪里，对于这些我刚才说的操作，保存的，事实上，对于线程切换，这些上下文，这些内核线程保存的寄存器都在进程结构体中，所以，任何内核线程只能有一组保存的内核寄存器。因为每个线程只在一个位置执行，它的上下文在某种程度上反映了它执行的位置，线程是单个控制线程，所以一个线程只需要一个寄存器的上下文，所以，它在进程结构体的 p->context 。而且每个调度器线程都有自己的上下文，不是在，没有与调度器线程关联的进程，调度器的上下文存储在核心的 CPU 结构体中。有一组 CPU 结构体，每个核心一个，每一个有一个上下文。为什么我们不能将寄存器包括在进程的 trapframe 中，实际上，这些寄存器可以存储在 trapframe 中，因为只有一个内核线程寄存器的存储集，对于进程，我们可以将它们保存在任何数据结构中，比如存在进程数据结构实例的一个元素，只有一个进程结构，有一个用于进程的 trapframe 结构，我们可以把寄存器保存在 trapframe 中。我是说，只是，可能是为了代码的简单性或清晰性，trapframe 只包含数据，进入和离开内核时需要的（数据）。content 结构由需要保存和恢复的内容组成，当内核线程和调度器线程之间切换时。好的，问题是，yield 是有内核调用的，所以在 xv6 中，用户线程没有真正直接的方式，用户线程让出 CPU 或切换，它是通过内核这种透明的完成，它会在内核觉得有必要发生的时候出现，如果有线程，有时候，你能猜到，一个系统调用可能让出，比如，如果进程在管道上进行读取，它知道在管道中没有需要等待读取的东西，那么读取就会阻塞，你可以预测读取阻塞，内核将运行其他进程，当我们等待数据出现在管道中时。所以，内核调用 yield 的时机，有两个主要的时机，一种是如果计时器中断，内核总会让出，基于这样的理论，我们应该交错执行所有这些进程，想要定期在定时器中断时运行，所处，定时器中断总是调用 yield ，当进程系统调用等待 IO 时，等待你输入下一次按键，读取控制台，但是你没有按键，然后等待 IO 调用的机器调用 yield ，调用 sleep ，我们下周会谈到。好的。好的，那么。我有一个问题。好的。如果是 sleep ，它是否会做大致相同的事，比如会是系统调用，保存 trapframe ，然后是一样的[图片]，但它只是，使进程在没有定时器中断的情况下进入内核，但是，是进程自己决定的吗？是的，所以这个进程有一个读取系统调用，这就是为什么它在内核中，而读取需要这个进程等待磁盘完成读取，或者等待数据出现在管道上，那么图跟这个是一样的，内核中调用系统调用，trapframe ，所有保存的用户寄存器，执行系统调用，系统会[]，需要等待磁盘完成读取某些内容，系统调用调用 sleep ，包含 switch ，保存进程上下文中的内核线程寄存器，切换到该当前 CPU 调度器，让其他线程运行，而这个线程等待磁盘读取完成。所以，我们现在谈论的一切，除了定时器中断，几乎都是一样的，如果发生的情况是我们使用系统调用，系统调用需要等待一些 IO ，并放弃 CPU 。对于今天的讨论，这两种情况几乎是一样的。好的，问题是每个 CPU 调度器都有自己的堆栈吗。是的，这里有一个堆栈，而这个调度器有单独的堆栈，对于 CPU1 的调度器。是的，这个调度器的堆栈也设置好了，实际上，所有这些东西，上下文，堆栈，调度器线程，设置与用户进程的设置方式不同。它们是在引导时设置的，如果查看 start.S 或 start.c ，你会看到每个核心的调度器线程的设置，堆栈在引导的汇编代码中非常早的位置，其中为每个 CPU 设置堆栈，在那个堆栈上，CPU 引导，然后运行它的调度器线程。好的，嗯。一条行话，当人们谈论上下文切换时，他们谈论的通常是这种行为，从一个线程切换到另一个，保存旧线程的一组寄存器，并且恢复要切换到的线程先前保存的寄存器，这就是上下文的意思，有时它也适用于完整的，一个用户进程切换到另一个，偶尔会看到在用户和内核之间切换的上下文，但对我们来说，我们大多数的意思是，从一个内核线程切换到调度器线程。只是一些片段信息，要记住的[][]，每个核心一次只做一件事，每个核心在任何时间只运行一个线程，它要么运行一些用户线程或一些内核线程，或核心的调度器线程，所以在任何时间，核心都不会做多件事，它只是在做一件事，正是这种切换在创造了一个假象，多个线程在核心上不同时间运行，类似地，每个线程运行在，要么在一个核心上运行，或它的状态已保存，然后我们从它切换走。所以，一个线程，线程不会在多个核心上运行，线程要么在一个核心上运行，要么根本没有运行，作为保存状态。另一个关于 xv6 设置的有趣的事情是，这些上下文保存内核线程寄存器，它们总是由调用 swtch 产生，所以，这些上下文总是关联指线程的状态，它在对 swtch 的调用中执行的。我们会看到的方式是，当我们从一个切换到另一个，并恢复目标线程的上下文，它要做的第一件事是从上一个调用返回到 swtch ，这些上下文总是保存状态，就像它在 swtch 里一样。好的。关于图表级别的情况，还有什么问题吗？我有一个问题，你一直使用线程这个术语，但在我看来，我们对 xv6 的实现，一个进程只有一个线程，所以，有没有可能一个进程可以有多个线程，还是我说错了。在 xv6 中，好的。我们在这里的说法是有一些让人困惑的地方，在 xv6 中，一个进程，一个进程执行用户级别的指令，或者执行内核中的指令，或者根本不执行，它的状态被保存到上下文和 trapframe 中。所以这就是实际情况，你怎么叫它，你想怎么叫都行，我不知道对这个结构有什么简单的解释，我们一直这样叫它，我一直说每个进程有两个线程，一个用户级线程和一个内核级线程，这是一个进程，有这样的限制，进程只会执行，或者在用户空间的内核中执行，或者在内核中中断系统调用中，但不能两个同时。理解了。是的，我为这件事的复杂性道歉。好的。好的，让我切换到代码，看看 xv6 代码。好的，首先，我展示一些我们一直在谈论的东西，我来看一下进程的结构。我们可以在进程结构中看到，很多东西我们已经讨论过，复习一下，这是 trapframe ，保存了用户级别寄存器。保存用户级寄存器的陷阱帧。这里有一个上下文，它保存内核线程寄存器，当切换到调度器线程。有一个指向这个进程的指针是内核堆栈，保存函数调用的位置，当我门在内核中执行时。这是状态变量，它记录该进程是否正在运行或是否可运行，或者处于休眠状态或根本没有分配。最后，这里有一个锁，保护我们看到的各种东西，目前，我们可以看到，至少保护了对状态变量的更改。比如，两个调度器线程，不会试图获取一个可运行的进程并同时运行它。这个锁做的事情之一就是防止这种情况发生。我要为你们运行一个简单的演示程序 spin 程序。我要用它来推动，创造一种可预测的情况，我们从一个线程切换到另一个线程，但这就是这个程序，spin 程序创建了两个进程，而且这两个过程都是永远计算的，在这里调用 fork，我创造了一个子程序，然后两个子程序永远在这个循环里，而每个进程打印一个字符，所以，我们可以看到它们的进展，但是它们不经常打印字符，而且它们从来不会故意放弃 CPU 。所以我们这里有两个计算密集型进程，为了让它们两个都跑起来，我将在单个 CPU 上运行它们，只有一个核心的 xv6 ，所以，为了让它们两个都能执行，有必要在这两个进程之间切换。让我使用 gdb 把 spin 程序启动起来，运行 spin 程序，你可以看到它在打印，其中一个进程打印正斜杠，另一个打印反斜杠，你可以看到，每隔一段时间，xv6 在它们之间切换，而且只有一个核心，就像我配置的那样，所以我们看到打印一堆正斜杠，然后很明显是计时器中断，将一个 CPU 切换到另一个进程，然后打印一会另一种斜杠。所以，我想要观察的是计时器中断的运行，所以我要在 trap 里放一个断点，尤其是在 trap 中的第 207 行，trap 中的 devintr 代码，表示我们在中断中，并且这是中断是由定时器中断引起的。所以我在这里设置一个断点，在 trap.c 第 207 行，继续[]我刚才触发的 trap ，因为定时器中断相当频繁，我们可以从得到，用户 trap 调用 devintr 的地方，来处理这个中断。我想离开 devintr ，回到用户 trap 中。因为，事实上我们没有，devintr 到定时器中断的代码没有什么东西。然而，一旦我们回到 usertrap ，我们可以从这一行看到，我们刚从 devintr 返回。有趣的是，我们接下来要做的，期待我们在这一行，我们期待这里调用 yield ，在这里你可以看到， devintr 返回了 2 ，返回的是 2 ，2 是设备号，我们很快就会看到这一点，因为哪个设备是 2 ，usertrap 要 yield 的，它获得 CPU 并允许运行进程，你很快就会看到这一点，同时，让我们看看中断发生时正在执行的是什么。我要打印 p ，变量 p 保存着指向当前进程结构 proc 的指针。好的，问题是什么让每个进程的内核线程不同。每个进程都有单独的内核线程，确实有两个不同的进程内核线程，因为多个可以在不同的核心上执行。一个是每个进程都有单独的内核堆栈，就是结构体 proc 的 kstack 所指向的，另一个是，早些时候，当 usertrap C 代码调用时，通过 trampoline ，当中断发生时。我们可以通过任何内核代码区分它们，可以通过调用 myproc 来判断当前 CPU 上正在运行哪些进程。这是另一个不同的地方，这允许内核代码区分，哪个进程内核线程正在执行，而 myproc 使用的是 tp 寄存器，你可能还记得，设置为包含当前核心 hartid 或核心编号，使用它来索引到一个结构数组，也就是说，对于调度器设置的每个核心，每当它切换进程以指示每个核心，哪个进程在那个核心上运行。这就是不同内核如何区分。好的，我要使用这个 p 的值，名称和 p 值以确定哪个进程正在运行，xv6 记得名字就是 spin 进程，像预期的一样，有两个进程，我想进程 id 分别是 3 和 4 。糟糕。我们现在再次执行进程 3 ，所以在切换之后，我们应该在进程 4 中，另一个 spin 进程，我们可以查看 trapframe 中保存的用户寄存器。这是 32 个寄存器，trampoline 代码保存用户状态，用户 ra 返回用户堆栈指针的地址，用户寄存器是十六进制 62 。这些都是我们在观察 trap 时熟悉的东西。你可能最感兴趣的是，trapframe 保存着用户程序计数器和值 62 ，如果我们关心，可以查看 spin.c 的汇编代码。糟糕，在 spin.asm 查找 62 ，我们可以看到有一个中断定时器，中断发生在 spin 无限循环中的加法指令期间，这并不令人惊讶。好的，回到刚才返回的 trap 代码，我要单步几次把我们带到，到达执行 yield 。而 yield 是进程中让出 CPU 的第一步，切换到调度器，让调度器选择进程中的另一个内核线程来运行。好的，让我们单步进入 yield ，现在我们在 yield ， yield 是。你有什么问题吗。不是。好的，我们在 yield 中，yield 做了几件事，它获取这个进程的锁，因为它要对这个进程进行一系列的修改，而且它不希望有任何其他的，实际上，在它放弃锁之前，这个进程的状态会是一致的，比如， yield 将进程的状态更改为可运行，这表明，这个进程没有在运行，但希望运行，但是这个进程是在运行的，我是说，我们正在运行这个进程，就是正在执行的这个进程的内核线程，所以，获取这个锁所做的事情之一是，让它，尽管我们修改状态为可运行，没有其他核心的调度线程会看到这个进程，因为使用了锁，（其他核心）看到它是可运行的，并试着运行它，当我们还在这个核心上运行它时，将会是一场灾难，在两个不同的核心上运行相同的进程，你知道进程只有一个堆栈，这意味着两个不同的核心在同一个堆栈上调用[子例程]，这是一个灾难。所以，我们使用了锁，yield 把状态改为可运行。这意味着，我们最终放弃了，当我们最终放弃 CPU ，切换到调度器进程，这个状态会保持在可运行状态，这样它就可以再次运行，毕竟这是一个计时器中断，中断了正在运行的用户级进程，它希望继续运行。我们把状态设置为可运行，让它可以再次运行，一旦调度器做了决定。然后，另一件事情是，yield 调用了这个调度器函数。所以，我要单步执行调度器函数。我会在这里展示整个过程。这个调度器几乎什么都不做，它做了一些检查，它做了一些可用性检查和 panic ，这个原因是，xv6 中的这段代码，在多年的时间中，一直是最容易出错的，有最多的意外，不愉快的意外，所以这里有很多可用性检查和 panic ，因为经常会有与这些代码相关的 bug 。好的，我会跳过这些可用性检查，然后继续到调用 swtch ，这个调用 swtch 是真正的操作发生的地方，这个调用 swtch ，将保存目前的内核线程寄存器到 p-> context 中，是当前进程保存内核线程上下文，保存寄存器，c->context ， c 是指向核心 CPU 结构的指针，CPU 结构体具有保存这个核心调度器线程的寄存器的上下文，我们将从这个线程切换，保存这个线程的状态，恢复核心调度器的线程状态，并继续运行这个核心的调度器线程。好的，让我们看看是，让我们快速看一下上下文，我们将切换到的，我可以得出，我不能直接打印 c->context ，但我知道 c 打印 cpus[0] ，因为我们在核心 0 上，只有一个核心，我可以打印它的上下文。这就是这个核心的调度器线程中保存的寄存器。特别感兴趣的是 ra ，因为 ra 寄存器是当前函数调用要返回的地方，当前函数调用将返回到，所以我们要切换调度器线程，它会返回，并返回到 ra 。我们可以找到，通过查看 kernel.asm ，找到那个返回地址。实际上，那是。你可以看到的，这个 x/i ，打印某个地址的指令，但它也会打印函数的名称标签，那些指令在这个函数里面，所以我们很快会回到调度器上，这正是你希望的。好的。我想看看 swtch 做了什么，关于调用 swtch 。所以我在 swtch 上设置一个断点，设置断点，因为这里有一堆设置代码，将上下文的值从这些结构中提取出来，我会跳过它。好的，现在，当到达 swtch 断点时，GDB 不会给我们看指令，但是我们可以查看 switch.S ，来查看即将执行的指令。正如你看到的，我们在第一条指令，将 ra 保存到 a0 指向的地址，你可能还记得在调用 swtch 中，第一个参数是当前线程的上下文，第二个参数是我们要切换到的线程的上下文，两个参数分别到 a0 和 a1 。所有保存都要通过寄存器 a0 的原因是，是因为我们在 a0 指向的内存中保存了一堆寄存器，是我们切换的线程的上下文，而加载从地址 a1 中加载，因为这是指向我们要切换到的线程的上下文的指针。好的，线程， swtch 保存寄存器，加载寄存器，从目标线程的上下文中，然后返回，这就是为什么 ra 很有趣，因为它返回 ra 指向的位置，即进入调度器。好的，这里有一个问题，你可能会注意到，虽然 swtch 保存了 ra sp 和很多 s 寄存器，却没有保存程序计数器，这里没有提到程序计数器，那是为什么。是不是因为程序计数器在函数任何时候调用时都是更新的。是的，程序计数器里没有实际的信息值，我们知道正在执行的是 swtch 。因此没有保存程序计数器，因为它有一个可预测的值，也就是这条指令，swtch 指令的地址，我们真正关心的是我们从哪里被调用，因为当我们切换回这个线程时，我们希望继续从调用点执行，它就是 ra ，保存着调用 swtch 指令的地址。所以是 ra 被保存在这里，而 ra 是再次执行的点。让 swtch 返回，我们甚至可以打印，ra ，糟糕。我们可以打印 ra ，我们还没有切换线程，还记得我们是从这个 sched 函数过来的，所以，如你所想的， ra 指向 sched 函数。另一个问题是，为什么 swtch 只保存了 14 个寄存器，我数了一下，它只保存和恢复 14 个寄存器，尽管 RISC-V 有 32 个寄存器，可用于，代码可以使用。为什么只保存了一半的寄存器？当调用 swtch 时，它是作为正常函数调用的，所以，任何调用 swtch 都会假设 swtch 可能会修改这些，以便那个函数将其保存在它的堆栈中，这意味着当我们从一个跳到另一个的时候，它会自动恢复调用者保存寄存器。完全正确，这个 swtch 是由 C 代码调用的，我们知道 C 编译器保存在当前堆栈上，任何调用者保存寄存器，包含编译器之后需要的值。而那些调用者保存寄存器包括，我想有 18 个，取决于你如何计算它们，有 15 到 18 个调用者保存寄存器，所以，我们在这里看到的寄存器是所有不是调用者保存的寄存器，编译器没有保证保存，但是仍然可以保存调用函数所需的值，所以当我们切换线程时，只需要保存被调用者保存的寄存器。好的。我想打印的最后一个东西是，我们保存和恢复了堆栈指针，当前堆栈指针，很难从这个值判断，这意味着，它是当前进程的内核堆栈，我不知道你是否记得，但是是由虚拟内存系统在高内存映射的。好的，我们要保存当前的寄存器，从调度器线程的上下文中恢复寄存器，我不想执行每一个加载或保存，所以我要跳过，所有这 14 个保存和 14 个加载，直接到返回指令。好的，所以我们执行了 swtch 中的所有代码，除了返回，在我们返回之前，我们再次打印感兴趣的寄存器，看看我们在哪里。所以堆栈指针，现在它有一个不同的值，堆栈指针现在指向内存中的堆栈零区域，这是在引导序列中非常早的位置，在 start.S 中放置堆栈，以便它可以调用第一个 C 函数，所以回到这个 CPU 的初始引导堆栈，它恰好就是调度器运行的地方。好的，我们对程序计数器也很感兴趣，我们在 swtch 中，因为我们还没有返回，并且现在 ra 寄存器指向调度器，因为我们已经加载，我们已经恢复了之前由调度器线程保存的寄存器集。实际上，我们现在在调度器线程中，如果我执行 where ，返回结果跟上次执行完全不一样了，它现在是调用 swtch ，但是现在我们调用 swtch ，要切换到过去某个时间调度器，调度器很久以前就在引导期间运行了，是在引导过程中所调用的最后一件事。所以，我要执行一条指令，从 swtch 返回到调度器。现在我们在这个内核的调度器中，看看完整的代码，这是调度器代码，这个函数叫做 scheduler ，现在我们在 CPU 调度器线程中执行，我们在，我们从之前的调用返回到 swtch ，它是调度器在之前创建的，当它决定开始运行这一进程时，pid 3 ，它是被中断的 spin 进程，所以现在这个 swtch 进程 id 是 3 ，是 spin 调用的 swtch ，但这次返回的不是之前的 swtch ，之前的 swtch 还没有返回，仍保存在进程 id 3 堆栈和上下文中，只是从先前的 swtch 返回。好的，在调度器中发生的事情，已停止运行这个进程，所以你可以忘掉我们所做的各种事情，在运行这个进程的过程中，我们希望[忽略] c->proc 等于零，意味着我们忘了这个。我们不再在这个核心中运行这个进程，所以我们不想让任何人对此感到困惑，让我把每个核心进程指针设置为零，接下来发生的事情是，你记得 yield ，获取这个进程的锁，因为它不想任何其他核心调度器看到这个进程并可能运行它，直到这个进程完全进入休眠状态。我们现在已经完成了从这个进程的切换，这样我们可以释放对刚刚让出的进程的锁，这就是 release ，在这一点上，我们还在调度器中，如果还有另一个核心，在这时，其他核心调度器可以找到这个进程，因为它是可运行的，并运行它。不过，没关系，因为我们已经完全保存了它的寄存器，不再在那个进程堆栈上执行，因为现在在这个核心的调度器堆栈上执行，所以，如果其他核心决定运行这个进程，也没什么问题。好的，但是这里没有其他核心，所以在这个演示中并没有发生。实际上，我想花点时间再谈一下 p->lock ，p->lock 做了几件事，从调度器的角度来看，它做了两件事，一个是让出 CPU ，涉及多个步骤，我们必须设置状态为可运行的，将状态从运行中改为可运行，我们将寄存器保存在让出进程的上下文中，现在我们必须停止使用让出进程的堆栈，至少有三个步骤，需要时间来做这些步骤让出 CPU ，所以锁做的一件事是，正如我提到的，阻止任何其他核心调度器看到我们的进程，直到所有三个步骤完成之前，所以锁是把这些步骤变成原子性的，从其他核心的角度来看，它们都发生，或者它们都不发生。当我们开始运行一个进程时，p->lock 也具有类似的保护功能，我们将进程的状态设置为运行中，当我们开始执行进程时，我们把它的寄存器从它的进程上下文转移到 RISC-V 寄存器中，但是，如果在进程中间发生中断，中断会看到进程处于一种奇怪的状态，比如状态标记为运行中，但是没有完成将寄存器从上下文移动到 RISC-V 寄存器，那将是一场灾难，如果发生定时器中断，因为我们可能会离开这个过程，在它恢复寄存器之前。从这个进程中离开，会保存未初始化的 RISC-V 寄存器到进程的上下文，覆盖它的真正的寄存器。所以，实际上，我们也希望启动一个进程具有原子性，在这种情况下，持有一个锁，在切换到进程的过程中持有 p->lock ，为了防止其他核心看到这个进程，还会在切换到那个线程的[]持续时间内关闭中断，防止定时器中断看到，只切换到一半的进程。好的。所以我们在调度器中，我们在调度器的循环中执行，调度器中的循环，依次查看所有进程，以找到一个要运行的进程，在这种情况下，我们知道还有另一个进程，因为，还有另一个我们 fork 的 spin 进程，但是有很多东西要检查，我将跳过扫描进程表，直接调到到调度器找到下一个进程的点，所以我要把断点放在第 474 行，在那里它找到了一个要运行的新进程。[]在这里，调度器扫描进程表，并找到另一个要运行的进程。它会调用那个进程运行，你可以看第 468 行，它获取了那个进程锁，所以现在它有权做不同的步骤，切换到那个进程，在第 473 行，它将进程状态设置为运行中，现在是第 474 行，我们在 CPU 结构中记录 CPU 正在执行的进程，然后调用 swtch 来保存调度器的寄存器，并恢复目标处理器的寄存器，你可以看到找到了什么进程，通过查看新的进程名称，当然它是 spin ，它的进程 id 现在是 4 ，以前是 3 ，现在是 4 。我们已经将状态设置为运行中，所以状态是运行中。我们可以看到这个线程将切换到哪里，在调用 swtch 的第 475 行，打印上下文保存的寄存器，那么 ra 在哪里，我们要调用 swtch ，但是正如我们知道的， swtch 返回，当它返回时，返回到恢复的 ra ，所以我们关心的是 ra 指向哪里，我们可以通过以下方式找出。糟糕。使用 x/i ，它返回 ra 指向某个点，这并不令人惊讶，假设另一个 spin 进程由于定时器中断而挂起，我们知道调用 sched ，调用 swtch 。好了，现在要调用 swtch 了，让我再次打开 swtch 代码。实际上在 swtch 中仍然，仍然在调度器上下文中。我要再次执行所有 swtch 的指令，这次，从调度器切换到新进程。我们跳过 28 个加载和保存。说服我们，要回到 sched 了，既然我们要返回 sched 而不是 scheduler ，我们现在必须在进程的内核线程中，而不再是调度器线程，事实上，如果我们查看 backtrace ，我们有一个 usertrap 调用，这一定是定时器中断，在过去的某个时候，正如我们所看到的，调用 yield 和 sched ，但它是定时器中断另一个进程，不是在我们最初观察的进程中。好的，有什么问题吗，我要停止一步步介绍代码了，关于我们看过的任何材料，有什么问题吗。不好意思，如果它是这样，比如这个[]，然后我们会看到 ra 会指向某个地方比如睡眠之类的，是吗？嗯，是的。好的，我们在这里看到的 where ，会包含一些系统调用实现函数，并且调用 sleep ，我想这是，简单回答你的问题，是的，如果我们停止执行这个进程，因为不是定时器中断的原因，swtch 将返回到某些系统调用代码，而不是 sched ，我认为 sleep 可能调用 sched ，所以。backtrace 看起来会不一样，我想会包含 sched ，我只选择了一种方式，因为定时器中断而产生的进程切换。但是你也可以切换等待用户 IO ，等待其他进程执行比如写入管道的操作。好的，有一件事你可能会注意到，调度器调用 swtch ，这里 swtch 返回的是什么，但是我们是从一个不同的调用 swtch 返回，而不是调度器创建的，我们是从这个进程很久以前调用 swtch 返回的。所以，这可能有点令人困惑，但是，这就是线程切换[]的工作原理。另一个需要注意的事情是，我们正在查看的代码，这个 swtch 代码，是线程切换的核心。你要切换线程所做的就是，保存寄存器和恢复寄存器，线程有比寄存器多的状态，它们在堆里有变量和东西，无论什么，所有其他状态都在内存中，不会受到干扰，我们没有做任何事情来干扰这些线程堆栈，比如堆里的值，所以微处理器的寄存器实际上是唯一一种易失性状态，需要保存和恢复才能进行线程切换，内存堆栈中的所有内容，仍会在内存中原封不动，所以，它不需要显式地保存和恢复。我们只保存和恢复微处理器 CPU 寄存器，因为我们想为新线程重新使用这些寄存器，覆盖它们包含的任何值。所以，这就是为什么我们必须保存旧线程的寄存器。那么，其他处理器的状态呢，我不知道我们使用的 RISC-V 处理器有没有其他标志，但我知道有些 x86 英特尔芯片有浮点单元状态或类似的东西，在 RISC-V 中有没有这个？你的观点在其他微处理器如 x86 上得到了很好的[理解]，切换的细节略有不同，因为它们在不同的状态有不同的寄存器，所以这是非常依赖于 RISC-V 的代码，对于其他处理器，切换程序可能看起来不同，比如可能必须保存浮点寄存器，RISC-V 使用的是通用寄存器。实际上，我不确定它对浮点数有什么作用，但是内核不使用浮点，所以不用担心它，但是，是的，这完全依赖于微处理器。一个关于定时器中断的问题。所以听起来像是所有调度工作的核心是，有一个定时器中断，如果它出现故障会发生什么？这里会有一个定时器中断。所以，我知道，好的，用户进程的抢占式调度能起作用的原因是，用户进程总是在打开中断的情况下执行，xv6 确保在返回用户空间之前启用中断，这意味着如果在用户空间中执行，可以发生定时器中断，所以用户进程不可能，如果在用户空间，定时器中断就会发生，当时间到来的时候。所以，在内核中有一点微妙之处，内核有时会关闭中断，比如，当你获取锁的时候，中断将被关闭，直到你释放它。所以。所以，如果内核中有一些 bug ，如果内核关闭中断，没有再打开它们，内核中的代码不会放弃 CPU ，从来没有调用 sleep ，或者因为任何其他原因放弃 CPU ，那么会发生计时器中断，这意味着，这个内核代码，可能永远不会放弃 CPU ，但是，据我们所知， xv6 []，所以它总是重新打开中断，或者，如果在 xv6 中有关闭中断的代码，它必须重新打开中断，所以定时器中断可以在内核中发生，我们可以从这个内核线程切换，或者代码返回到用户空间，内核代码返回到用户空间，我们相信不会出现这种情况，内核代码在中断关闭的情况下永远循环。我知道了，我的问题更多的是关于，所以我假设中断来自某个硬件，如果那个硬件出现故障。不。没关系，那是你的电脑坏了，你应该买一台新的。好的。我是说，这是一个合理的问题，你的计算机里有 100 亿个晶体管，的确，有时硬件会有 bug ，但这超出了我们的范围。我是说，如果你计算 1+1 ，而电脑给出 3，那么你遇到深层次的问题，xv6 不能帮你解决。所以，我们假设计算机是可以工作的。唯一一次，软件可以，我是说，有时软件会试图弥补硬件错误，比如，如果你通过网络发送数据包，总是发送校验和，如果网络硬件翻转了一个位，出现故障翻转了一个位，那么你可以纠正这个，但是对于计算机里的东西，人们往往不会，它只是，人们不会试图让软件来弥补硬件错误。我有个问题，为什么在 trampoline. ，在 swtch 中，我们用汇编语言编写代码，这是因为，是不是因为我们要确保这件事的发生，所以我们不能用 C 写，因为我们需要，[感觉]这些事情发生。是的，是的。是的，我们当然希望这个确切的顺序发生，C 很难访问像 ra 或 sp 这样的东西，当然，在 C 语言中，也没有办法使用 ra 寄存器访问修改堆栈指针，这些东西不能在普通的 C 中看到，你能在 C 中看到它的唯一方式是，在 C 代码中嵌入汇编语言指令，所以我们可以把这些汇编指令嵌入到 C 函数中，但是，这就等同于一件事了。我们是在低于 C 的级别下操作的，所以我们不能在这里使用 C 。我有个问题，关于当线程完成执行，假设发生在用户空间，当我们调用 exec 系统调用，它也结束了进程，在内核空间的线程，但是如果线程在新的定时器中断发生之前结束，它是否还是，比如 CPU 还在被那个线程获取，或者结束那个线程并开始一个新的，在新的定时器中断之前。是的,线程让出 CPU ，exec 让出 CPU ，所以实际上有很多点，尽管我一直在用定时器中断来讨论，事实上，在几乎所有的情况下，xv6 在线程之间切换，不是由于定时器中断，因为有些系统调用在等待某些东西，决定需要放弃 CPU ，比如， exec 执行各种操作，然后调用 yield 并放弃 CPU ，它就是这么做的，独立去做，与定时器中断无关。是的。好了，这节课的时间到了，我想下周我会继续讨论这个问题，但是我现在很乐意回答更多的问题，如果人们有问题的话。那么，让我们说操作系统，实际上，我采用了线程实现，比如，你希望在多个 CPU 上运行一个进程的多个线程，这必须由操作系统来处理，不能在用户空间中处理，是吗？这种切换是怎么工作的，是每个线程都变成了一个进程，比如，在所有现有线程中循环，或者，因为每个 CPU 仍然会切换，即使一个进程提供八个核心，它还会在这些 CPU 之间切换，以及其他一些进程，而且我们也不想，在同一个 CPU 上的切换一个线程和另一个线程，或者我们希望，我不知道。等等，我能不能。我不确定问题是什么。是的，我想，你能解释一下这是怎么发生的吗？抱歉，什么是怎么发生的。假设每个进程有多个线程，它们可以在不同的 CPU 上运行，我们如何处理这些。比如， Linux 支持每个进程多个线程，在 Linux 中，是一个很复杂的实现，也许最简单的解释是，几乎 Linux 中的每个线程都是一个完整的进程，给定进程的线程，我们称之为特定进程的线程，本质上是共享内存的独立进程，所以 Linux 在某种程度上将执行线程的概念从地址空间中分离出来，你可以把它们独立起来，如果在一个进程中有两个线程，它基本上是两个进程共享一个地址空间，从那时起，调度与 xv6 对各个进程所做的没有什么不同。我明白了，然后，有没有什么东西，比如用户必须指定，每个线程要固定到一个 CPU 上，或者操作系统如何确保同一进程的不同线程不会在同一核心上运行，因为这是不是违反了目的，或者不是，我猜的，我不知道。这个其实跟 xv6 很像，就是，这里有四个核心，Linux 只会为这四个核心中找到四个东西，如果没有太多的事情发生，那么可能会是同一进程的四个线程，或者如果有一百个用户登录到 Athena 机器，也许是每个线程来自多个不同的进程，没有一个固定答案，或者内核为每个核心找到要做的事情，然后那个核心做那件事。好的，理解了。如果你想仔细测量，有一种方法可以将线程固定在核心上，但是人们只有在做一些奇怪的事情时才会这么做。所以是共享虚拟表。你能再说一遍吗？虚拟内存。所以说哪些线程有相同的页表。是的，如果你在 Linux 上，在一个进程中创建两个线程，然后你就有了两个线程。我不知道它们是否共享完全相同的页表，或者它们的页表是相同的，两个中的一个。有没有原因让它们分开，如果你手动映射内存，或者。我不知道 Linux 是哪种。好的，我还有一个关于一个小细节的问题，根据我的理解，当你调用 swtch 时，从一个调用切换到另一个，所以当你第一次调用 swtch 时，必须人为地创建返回的端点，是吧。是的。因为你不能随便跳到任何代码。是的，你想知道那个伪造在哪里，那个上下文是在哪里创造的。我猜可能是在进程创建的地方，我不知道。是的，可能是 userinit ，没有使用 allocproc 。我也不知道。有一个叫做 fork trap 的东西。是的，看这个，有 forkret ，好的，所以 allocproc ，在引导时的第一个进程或 fork 时调用，allocproc 为新进程设置上下文的关键元素，它设置了新进程的上下文，大多数寄存器是什么并不重要，但重要的是 ra 是什么，因为第一个 swtch ，那个进程会返回到 ra ，那个进程需要使用自己的堆栈，所以 ra 和 sp 设置，是伪造的，所以，第一个 swtch 或进程可以正常工作。所以，如果我理解正确的话，当这个 swtch 发生时，它会开始执行 forkret 内部的第一条指令，forkret 调用 swtch ，再返回。是的，从 swtch 返回跳到 forkret 的开头。好的，有意思，我们有没有调用 forkret ，还是总是这样，我想总是这样发生的。我不认为有什么东西调用了 forkret ，因为，是的，它只在第一个进程运行时以这种奇怪的方式执行。它的工作实际上是释放调度器占用的锁，然后返回，然后这个 usertrapret ，当然，也是伪造的。它是，就像是从 trap 返回，trapframe 也是伪造的，调到用户代码的第一个指令。但是 trapframe ，它又是一样，不需要初始化任何寄存器，因为就像我们要从头开始，所以你不需要假设任何事。是的，程序计数器我认为是。是的。它需要初始化为零，我不知道还有什么，也许就是它。它们可能，如果我们调用它们不正确，因为如果我们已经进行了调用，那么就会设置程序计数器。是的，只会在这里发生，是因为 fork 复制了程序计数器，用户程序计数器，所以我们唯一不做 fork 的是，对于第一个进程，在那里它明显的欺骗性的。噢。而且堆栈指针。哦，是的。也需要设置。是的，因为它是 EPC 不是 PC ，就是那个要被 trampoline 换掉的。是的。哦，我明白了。因为真正的程序计数器会在 trampoline 中，但是我们要把它切换，跳到那里。是的。我能不能问一下，你能不能回到 allocproc 。我觉得这里，哦，不是，抱歉，是 forkret ，那里有一些东西我想只发生在第一个进程。对于第一个调用，我不太确定发生了什么事。我们看看，文件系统需要初始化，特别是一些内容需要从磁盘上读取，为了使文件系统运行，比如有一种叫做超级块，用来描述文件系统有多大，以及文件系统中各种内容的位置，还有一个崩溃恢复日志，需要重播，以便从之前的崩溃中恢复过来，如果有的话。但是为了在文件系统中执行任何操作，你需要等待磁盘操作完成，但是 xv6 的工作方式，你只能在进程的上下文中执行文件系统代码，为了比如等待 IO ，所以文件系统的初始化必须推迟，直到我们有一个进程在运行。这发生在 forkret 的第一个进程中。我明白了。我猜我们稍后会对此有更多了解。是的，不是关于这个烂摊子，而是关于文件系统是如何工作的。好的，谢谢，抱歉占用您时间了。谢谢你的回答。抱歉，是不是在初始化个进程，当这个东西被执行的时候……

## Code:  调度

现在讨论如何通过调度器在不同进程的内核线程之间进行切换。每个 CPU 上，调度器以一个特殊线程的形式存在，这些线程运行``scheduler``函数。调度函数负责选择下一个要运行的进程。想要放弃 CPU 的进程必须获取自己的进程锁 `p->lock`，释放它持有的其他锁，更新自己的状态（`p->state`），然后调用 `sched`。你可以在 `yield`（见 `kernel/proc.c:496`）、`sleep` 和 `exit` 中看到这个过程。`sched` 会再次检查这些要求（见 `kernel/proc.c:480-485`），然后检查一个推论：由于持有锁，中断应当被禁用。最后，`sched` 调用 `swtch`，将当前上下文保存在 `p->context` 中，并切换到调度器的上下文 `cpu->scheduler`。`swtch` 返回时是在调度器的堆栈上，就像调度器的 `swtch` 已经返回一样。调度器继续其循环，找到一个进程并切换到它，然后循环重复。

我们刚刚看到，xv6 在调用 `swtch` 时持有 `p->lock`：`swtch` 的调用者必须已经持有该锁，锁的控制权会传递给切换后的代码。这个习惯在锁的使用中是很不寻常的；通常情况下，获取锁的线程也负责释放锁，这样更容易确保正确性。对于上下文切换，打破这一惯例是必要的，因为 `p->lock` 保护着进程状态和上下文字段上的不变性，而在执行 `swtch` 时这些不变性并不成立。一个例子是，如果在 `swtch` 执行时 `p->lock` 没有被持有，另一个 CPU 可能在 `yield` 将进程状态设置为 `RUNNABLE` 后但在 `swtch` 停止使用其内核堆栈之前决定运行该进程。结果是两个 CPU 会同时运行在同一个堆栈上，造成混乱。

内核线程放弃其 CPU 的唯一地点是在 `sched` 中，并且它总是切换到调度器中的同一位置，而调度器几乎总是切换回之前调用过 `sched` 的某个内核线程。因此，如果你打印出 xv6 切换线程的行号，你会观察到如下简单的模式：`kernel/proc.c:456`、`kernel/proc.c:490`、`kernel/proc.c:456`、`kernel/proc.c:490`，如此往复。那些通过线程切换有意转移控制权的过程有时被称为协程；在这个例子中，`sched` 和 `scheduler` 是彼此的协程。

有一种情况调度器的 `swtch` 调用不会返回到 `sched`。`allocproc` 会将新进程的 `ra` 寄存器设置为 `forkret`（见 `kernel/proc.c:508`），因此其第一次 `swtch` 会“返回”到该函数的开头。`forkret` 的作用是释放 `p->lock`；否则，由于新进程需要像从 `fork` 返回那样返回到用户空间，它可以直接从 `usertrapret` 开始。

`Scheduler`（见 `kernel/proc.c:438`）运行一个循环：找到一个进程，运行它直到它放弃 CPU，然后重复。调度器会遍历进程表，查找一个可运行的进程，也就是状态为 `RUNNABLE` 的进程。一旦找到进程，它会设置每个 CPU 的当前进程变量 `c->proc`，将该进程标记为 `RUNNING`，然后调用 `swtch` 开始运行它（见 `kernel/proc.c:451-456`）。

可以认为调度代码的结构是它强制执行了每个进程的一组不变性，并在这些不变性不成立时持有 `p->lock`。其中一个不变性是：如果一个进程处于 `RUNNING` 状态，计时器中断的 `yield` 必须能够安全地从该进程切换走；这意味着 CPU 的寄存器必须持有该进程的寄存器值（即 `swtch` 还没有将它们移动到上下文中），并且 `c->proc` 必须指向该进程。另一个不变性是：如果一个进程是 `RUNNABLE`，那么空闲 CPU 的调度器必须能够安全地运行它；这意味着 `p->context` 必须保存进程的寄存器（即它们实际上不在真实的寄存器中），没有 CPU 在该进程的内核堆栈上执行，并且没有 CPU 的 `c->proc` 指向该进程。请注意，当持有 `p->lock` 时，这些属性通常不成立。

保持上述不变性的原因是，xv6 经常在一个线程中获取 `p->lock`，而在另一个线程中释放它，例如在 `yield` 中获取并在 `scheduler` 中释放。一旦 `yield` 开始修改正在运行进程的状态以使其变为 `RUNNABLE`，该锁必须保持持有状态，直到不变性恢复：最早的正确释放时机是在调度器（运行在自己的堆栈上）清除 `c->proc` 之后。同样，一旦调度器开始将一个 `RUNNABLE` 的进程转换为 `RUNNING`，该锁不能被释放，直到内核线程完全运行（例如在 `yield` 中的 `swtch` 之后）。

## Code： mycpu和myproc

xv6经常需要指向当前进程的 `proc` 结构体的指针。在单处理器系统中，可以使用一个全局变量指向当前的 `proc`。但在多核机器上，这种方法不可行，因为每个核心执行不同的进程。解决这个问题的办法是利用每个核心都有自己的一组寄存器，我们可以使用其中一个寄存器来帮助查找每核的相关信息。

xv6 为每个 CPU 维护一个 `struct cpu`（见 `kernel/proc.h:22`），该结构记录了当前在该 CPU 上运行的进程，CPU 调度线程的保存的寄存器，嵌套自旋锁计数（用于管理中断禁用）。函数 `mycpu`（见 `kernel/proc.c:72`）返回指向当前 CPU 的 `struct cpu` 的指针。RISC-V 给每个 CPU 分配一个`` hartid``。xv6 确保每个 CPU 的`` hartid`` 存储在该 CPU 的 tp 寄存器中，这样 `mycpu` 可以使用 tp 来索引 `cpu` 结构体数组，找到正确的结构体。

确保一个 CPU 的 tp 始终保存 CPU 的 hartid 是有一定复杂性的。`mstart` 在 CPU 启动序列的早期设置 tp 寄存器，仍在机器模式下（见 `kernel/start.c:51`）。`usertrapret` 将 tp 保存到 trampoline 页面，因为用户进程可能会修改 tp。最后，`uservec` 在从用户空间进入内核时恢复保存的 tp（见 `kernel/trampoline.S:70`）。

编译器保证不会使用 tp 寄存器。虽然如果 xv6 可以随时询问 RISC-V 硬件当前的 hartid 会更方便，但 RISC-V 只允许在机器模式下这样做，而不能在特权模式下。

`cpuid` 和 `mycpu` 的返回值是脆弱的：如果计时器中断导致线程让步并转移到不同的 CPU，之前返回的值可能不再正确。为避免这个问题，xv6 要求调用者禁用中断，并在使用返回的 `struct cpu` 后再启用中断。

函数 `myproc`（见 `kernel/proc.c:80`）返回当前 CPU 上运行的进程的 `struct proc` 指针。`myproc` 禁用中断，调用 `mycpu`，从 `struct cpu` 中提取当前进程指针（`c->proc`），然后重新启用中断。即使中断启用，`myproc` 的返回值也是安全的：如果计时器中断将调用进程移到不同的 CPU，它的 `struct proc` 指针仍会保持不变。

## 定时器中断

Xv6 使用定时器中断来维护当前时间的概念，并在计算密集型进程之间进行切换。定时器中断来自连接到每个 RISC-V CPU 的时钟硬件。Xv6 会对每个 CPU 的时钟硬件进行编程，使其周期性地中断 CPU。

在 `start.c` (kernel/start.c:53) 中的代码设置了一些控制位，允许管理模式下访问定时器控制寄存器，然后请求第一个定时器中断。时间控制寄存器包含一个计数，硬件以稳定的速率递增该计数，作为当前时间的概念。`stimecmp` 寄存器包含 CPU 将在某个时间点触发定时器中断的时间；将 `stimecmp` 设置为当前时间加上 x 将安排在未来 x 时间单位后发生中断。在 qemu 的 RISC-V 仿真中，1000000 个时间单位大约相当于 0.1 秒。

定时器中断与其他设备中断一样，通过 `usertrap` 或 `kerneltrap` 以及 `devintr` 来到达。定时器中断时，`scause` 的低位设置为 5；`trap.c` 中的 `devintr` 检测到这种情况并调用 `clockintr` (kernel/trap.c:164)。后者函数递增 `ticks`，让内核能够跟踪时间的流逝。时间递增仅发生在一个 CPU 上，以避免多个 CPU 导致时间加速。`clockintr` 唤醒任何通过 `sleep` 系统调用等待的进程，并通过写入 `stimecmp` 来安排下一次定时器中断。

对于定时器中断，`devintr` 返回 2，以便指示 `kerneltrap` 或 `usertrap` 调用 `yield`，从而使 CPU 能够在可运行进程之间进行多路复用。内核代码可以被定时器中断打断，并通过 `yield` 强制进行上下文切换，这也是 `usertrap` 早期代码在启用中断前小心保存 `sepc` 等状态的原因之一。这些上下文切换意味着内核代码必须编写得足够谨慎，以应对可能在没有预警的情况下从一个 CPU 切换到另一个 CPU 的情况。



## 现代操作系统

某些计算机应用要求系统必须在限定时间内做出响应。例如，在安全关键系统中，错过时限可能会导致灾难。Xv6 不适用于硬实时环境。**硬实时操作系统通常是与应用程序链接的库，允许分析确定最坏情况下的响应时间**。Xv6 也不适用于软实时应用（偶尔错过时限是可以接受的），因为 Xv6 的调度程序过于简单，并且它的内核代码路径中有时会长时间禁用中断。



xv6调度器实现了一种简单的调度策略，即依次运行每个进程。这种策略称为轮转调度（round robin）。实际的操作系统实现了更复杂的策略，例如允许进程具有优先级。其理念是调度器在可运行的进程中，优先选择高优先级的进程，而不是低优先级的进程。这些策略可能会迅速变得复杂，因为通常存在相互冲突的目标：例如，操作系统可能还想保证公平性和高吞吐量。此外，复杂的策略可能会导致意外的交互问题，如优先级倒置和队列阻塞。优先级倒置可能发生在低优先级和高优先级的进程都使用某个特定锁时，低优先级进程获得锁后，可能会阻止高优先级进程继续执行。当多个高优先级进程都在等待一个低优先级进程释放共享锁时，就可能形成一个长时间的队列阻塞，一旦队列形成，它可能会持续很长时间。为了避免这些问题，复杂的调度器需要额外的机制。