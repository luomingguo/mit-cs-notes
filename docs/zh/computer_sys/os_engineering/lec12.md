# Lec 12 协调



## 总览

- 复习Xv6线程切换的两个要点
- 序列协调
  - sleep & wakeup
  - 丢失wakeup问题
- 终止



## 1. 复习

### 1.1 线程switch

要从一个线程切换到另一个线程，通常从内核线程到调度器线程，它总是在预先获取这个进程的锁。

一个进程想要休眠，tarangchu

> 为什么切换需要维护锁？

```
 yield()                     scheduler()
 -------                     -----------
 acquire(&p->lock);
 p->state = RUNNABLE;
 swtch();
                             swtch();
                             release(&p->lock);
```





>是 xv6 当有东西调用 swtch ，要从一个线程切换到另一个线程，通常从内核线程到调度器线程，它总是在预先获取这个进程的锁。所以进程获取这个锁调用 swtch ，它总是切换到调度器，而调度器释放那个锁。事实上，顺序是这样的，或者一个进程因为某种原因想要休眠，它让出 CPU 等待某些东西，它会获取自己的锁，设置自己的状态，这里有很多状态，我们上周看到的，将状态从运行中设置为可运行，然后调用 swtch ，或者调用 sched 来调用 swtch 。这个 swtch 是将线程切换到调度器线程，与之前调用 swtch 一样，这个调度器线程返回。然后调度器线程释放这个进程的锁，让出CPU 。这样做的原因，只是重复一下，是因为，获取进程上的锁会阻止不同核心的调度器，在这个时间点上查看，然后说，这个线程是可运行的，并试图运行它，因为每个其他核心也许运行一个调度器循环，它不断地在进程表上循环，寻找可运行的线程，所以它们中的任何一个都可能看到这个线程是可运行的，如果我们没有获取进程锁，所以我们获得进程锁意味着，其他调度器，在查看进程状态之前，必须取得它的锁。所以我们知道这次获得之后，没有其他线程会查看该锁。我们不能在这里放弃锁，我们不能在调用 swtch 之前放弃锁，也就是说，这个线程获取锁，但它不能放弃它，因为如果我们这么做了，那么在我们放弃之后，某个其他核心的调度器可能会开始运行这个进程，即使那个线程当前正在这个核心上运行。让两个核心运行相同的线程，使用相同的堆栈，会造成瞬间的崩溃。所以，进程获取锁，在调用 swtch 时释放它，不同的线程，即调度器线程释放锁，在这个线程完全停止使用其自己的堆栈之后，这时另一个内核的调度器运行这个线程是可以的，因为线程现在不再运行，并且已经放弃了处理器。好的，这是很重要的一点，它几分钟后会出来，这是诸多限制之一，在 sleep wakeup 协同机制的设计上。关于 swtch 持有 p->lock ，有什么问题吗？是的，如果。是的。当我们有多个核心时，它们能看到相同的锁的唯一原因是，因为只有一个共享的物理内存系统，是吗？这是正确的。那么，有没有这样的实现，其中有奇怪的文件系统，可以保证这一逆境，这样我们就可以使用锁。如果你买两台计算机，那么它们不会共享内存，对吧，所以，一种方式是如果你购买两台计算机，这样它们就不会共享内存，我们就不会有任何这些问题了。只是从处理器芯片的角度来看，如今，单个处理器芯片上总是有多个核心，所以，硬件被构建为让多个核心共享相同的内存系统。我明白了，谢谢。好的，这是一点。还有一点，我还没有提到，与一个事实有关，在 xv6 中，进程在调用 swtch 时不允许持有任何其他锁。进程调用 swtch 时需要持有 p->lock ，但当它调用 swtch 时，禁止持有任何其他锁，所以这是另一个重要的限制，关于很多东西的设计，包括休眠是如何工作的。让我来描述一下这件事的理由，这是当你调用 swtch 时，没有其他的锁。这个方案说明了为什么这条规则必须强制执行，这是一条规则，如果你扩展，你是开发 xv6 内核的程序员，必须遵守这条规则以及其他一些规则。那么这条规则的正确性，假设我们有进程一，有进程一的内核线程，它会获取一些锁，这并不稀奇，只是一些锁，可能它正在使用磁盘或使用 uart 控制台，并且获取一些锁。假设它发生了，通过调用 swtch 或 yield 或 sched 或其他方法放弃了 CPU ，但是仍然持有这把锁。所以现在 p1 持有锁，持有锁，但是并没有运行。想象一下，我们在一台只有一个核心的机器上。所以只有一个核心，进程一调用 swtch ，转移调度器，调度器看到进程二的内核线程在等待运行，所以调度器切换进程，开始运行进程二，切换到进程二，假设进程二，不管出于什么原因，或许它还想使用磁盘或 uart 之类的东西，它对同一个锁调用 acquire ，所以我们第二次获取这把锁，这是同一把锁。当然，锁已经持有了，所以这个 acquire 拿不到它，这些是自旋锁，所以，这会导致，进程二内部的 acquire 会在一个循环中，旋转等待锁被释放。而且 acquire 不会返回，既然 acquire 不会返回，进程二不能，没有机会，即使它愿意在完成的时候让出 CPU ，它没有机会这样做，因为 acquire 在锁释放之前不会返回。但是唯一能释放锁的方法是，如果进程一恢复执行，并且可能稍后，它是正确的，没有严重的 bug ，它会打算释放这个锁，但这还没有发生，因为它调用了 swtch ，而进程二正在旋转，等待这个锁，所以这是一个死锁，这会导致系统冻结。我描述这个的上下文是，一台只有一个 CPU ，一个核心，你可以想象，使用多个锁将导致相同类型的死锁，在一台有多个核心的机器上。所以，在 xv6 中有一个普遍的限制，不能在 swtch 时持有锁。对于这条规则，有什么问题吗？聊天中有个问题，罗伯特。能否有一个定时器中断，切换到 p1 来解决死锁？好吧，那么，确实是这样，所以所有这些东西都运行在内核中，acquire release 都是内核代码，不是运行在用户空间中，但确实定时器中断可能会发生，而且 xv6 允许定时器中断发生，在运行内核代码，系统调用代码时，事实上，如果你查看内核陷阱代码，或者不管在 trap.c 里的什么，你会看到，如果发生定时器中断，当 xv6 执行内核时，它会调用 yield 。所以如果定时器中断发生，当我们在这里运行 acquire 时，那么我们就会得救，因为我们调用 yield ，返回到这里，希望 p1 恢复并最终释放锁，然而，由于上一节课已经解释过的原因，acquire 在开始等待锁之前关闭中断，出于其他原因，不能承受在我们持有锁的同时发生中断，因为那会导致另一种死锁。它是中断处理程序，需要获取锁，这个 acquire 可能是刚刚获得的，所以，如果你查看 xv6 中的 acquire 代码，你会看到它做的第一件事就是关闭中断，然后旋转你可能会想，为什么它不是旋转，然后关闭中断，原因是这将允许短时间内发生，锁被持有，但是中断没有被禁用，再一次，如果这个时间段发生设备中断，可能会导致死锁。所以，不幸的是，这是另一个要求我们关闭中断，当我们旋转等待锁时，防止定时器中断关闭，并且防止了进程二回到进程一。这是个好问题。好的，另一个问题，我可以重复一下如何避免死锁吗？在 xv6 中通过禁止这个来避免死锁，xv6 代码不允许获取 p->lock 以外的任何锁，然后调用 swtch ，如果你看一下 swtch 的代码，在 swtch 里有一些检查，检查除了 p->lock 之外没有锁被持有。所以问题是这个代码，如果它发生在 xv6 内核中，将是非法的。很容易造成死锁，所以这是被禁止的。这回答了这个问题吗？好的。还有其他问题吗？好的，记住这条规则，以及预先在 swtch 中持有 p->lock ，因为它们会再次出现，在我们讨论睡眠和唤醒是如何工作的时候。好的，新主题，协调，就是 sleep/wakeup 。我们听说过很多关于锁的事，锁在某些情况下是很棒的，不同的线程不必担心，而不必担心或考虑其他线程在做什么，我们持有锁并共享数据，这意味着我们不必担心这种可能性，其他的锁是或不是使用这些数据，因为锁导致一次只发生一件事，但是当你编写线程化代码时，也有某些情况，你需要等待特定的事件，你希望不同的线程进行交互。比如，假设我们有管道，我们有读取者和写入者，如果我正在读取管道，而当前在管道中没有要读取的内容，我希望能够等待任何其他进程将数据写入管道，我想等待管道不是空的事件。同样的，如果我正在读取磁盘，或正在写入磁盘，如果我在读取磁盘，然后我希望告诉磁盘控制器，请读取磁盘上的某个块，可能需要很多毫秒，在磁盘完成读取完成之前需要很长时间，特别是如果它必须寻找和旋转，正在读取的进程需要等待这个特定事件，我们希望等待磁盘读取完成。同样地，你可能已经注意到，当你编程的时候， unix 程序可以进行 wait 系统调用。而 wait 所做的是，它使调用进程等待，直到它的任意子进程退出，所以，这里我们故意让父进程等待，等待某种由另一个进程引起的事件。所以，这些都是进程需要等待事件的情况，通常是 IO 或另一进程，表示发生一些特殊的事情。协调是一种工具，帮助我们解决这类问题，或者实现这些要求，而且协调是很基础的，就像锁一样，协调是另一个基本工具，用于编写线程化程序，并且它总是出现。好的，那么，我们怎么让一个进程或一个线程等待这种事件，一种可能的非常简单的方法是，就是忙等待循环，你可以想象。我们马上就会把它当作一个坏想法扔掉，但是假设我们想要从管道中读取，我们在管道读取函数中编写了一个循环，也就是 while 管道缓冲器是空的，我们什么都不会做，现在我们在这个循环中的旋转，直到另一个线程或另一个核心写入缓冲区，使其不为空，然后这个循环就会结束，然后我们会返回数据，或者无论我们如何处理这些数据。所以，你可以想象这样编写代码，实际上有一点，可能像这样有很少的代码，如果你知道你等待的事情极有可能发生，十分之一微秒，这种等待可能是最好的方式，所以，对于一些设备硬件通常是这样完成的。当你要求设备硬件执行某些操作时，你知道它总是会在很短的时间内完成这项任务，只是在一个短的循环里，可能是正确的选择。但是，如果这可能需要很多毫秒长的时间，或者你不知道时间有多长，可能需要 10 分钟，在任何进程写入管道之前，那么我们就不想在那里旋转，浪费 CPU 时间，它可以用来找到更多的 π 的数字或其他有用的东西，相反，我们想放弃 CPU 。我们想要像 swtch 一样放弃 CPU ，但在我们关心的事件发生时重新获得 CPU ，这种协调是一种技术，放弃 CPU ，直到条件出现，我们等待的事件发生。再一次， sleep/wakeup ，有多种不同的协调原语，在很多年中，人们发明的。xv6 与许多 Unix 的风格相同，使用一种 sleep/wakeup 的东西。好的，在这样的背景下，我想切换到查看 xv6 的代码。好的，我刚分享了我的屏幕，如果屏幕共享起作用了，请让我知道。好的，我已经为这次课程做了准备，重写了 UART 驱动程序中的一些代码，这是 xv6 用来从控制台读写字符的串行驱动程序，所以我写了这个 uartwrite 函数，当像 shell 这样的程序打印提示符或产生任何其他输出时，会使用一个 write 系统调用，在我稍微修改的 xv6 版本中，write 系统调用以 uartwrite 的调用结束，在 uart 驱动中，将这个循环中的字符逐个写入 uart 硬件。这是以一种经典的设备驱动程序风格编写的。你会在许多设备驱动程序中看到这样的代码。好的，这里发生的一件事是，uart 硬件一次只能接受一个字符进行传输，所以这段代码需要查看的，通常你有很多想要写入的字符，它可以向 uart 硬件写入字符，它需要等待 uart 硬件是的，我已经把那个字符发送完了，我准备好接受一个新的字符了，然后驱动可以写入一个新的，下一个要输出的的新的字符，因为这个硬件运行起来非常慢，可能每秒只有一千个字符，我们在字符之间等待的时间可能非常长，以毫秒为单位，在现代计算机上是很长的时间，可能是十亿个，百万个机器周期，这可以做大量的工作，所以，我们不希望只是旋转，等待 uart 完成每个字符的发送。我们想要有更好的方式，事实上，像大多数操作系统一样， xv6 有更好的方式，uart 硬件在完成发送每个字符后引发中断，所以我们不仅仅有这个写入例程，我们将看到 uart 驱动程序有一个中断例程，我认为会调用 trap.c ，当 uart 硬件发出中断通知已完成时，中断例程检查 uart ，读取 uart 硬件中的一个内存映射寄存器，查找表示已经完成发送的标志，应该是 LSR_TX_IDLE 标志，如果设置了这个标志，那么中断例程在内存中设置该标志，然后调用这个 wakeup 调用，这将调用 uartwrite ，不管 uartwrite 是哪个线程，从这里的睡眠中恢复过来，并尝试发送新字符。所以流程是这样的，如果线程需要等待某些东西，在这里，我们需要等待 uart 硬件能够接受新字符，通常调用 sleep ，并等待特定条件，当条件满足时，实现该条件的代码调用 wakeup 。所以这些 sleep 和 wakeup 是成对的。sleep 会查看实现情况，sleep 会做很多事情，然后调用 swtch 来放弃 CPU 。需要注意的一件事是，wakeup 和 sleep 必须以某种方式联系在一起，就是我们调用 wakeup ，我们只想唤醒等待特定事件的线程，我们[意识到]已经发生，所以 sleep 和 wakeup 都有这个参数，它被称为睡眠通道，这里 wakeup 支持同样的值，我们传给 sleep 的值，sleep 和 wakeup 不会真的查看，不在乎这些是什么，它们在这里只取 64 位的值，它们不在乎它们是什么，唯一发生的事情是，当我们在特定的睡眠通道，等待通道上睡眠时，我们希望 wakeup 传递与此相同的值，表示它想要唤醒哪些睡眠者。关于这个接口，有什么问题吗？好的。所以有一个问题需要确认，进程不是对每个写入的字符唤醒。好的，让我看看，在这个为了演示而改写的驱动程序中，每个字符都有一个中断，所以 uartwrite 的工作方式是对于每个字符，在这个 while 中，对于缓冲区中要写入的每个字符，我们在这个循环中等待，直到 uart 准备好接受另一个字符，我们再写入一个字符，我们把这个完成标志设置为 0 ，返回，在 sleep 中等待，知道完成标志为 1 。然后，在 uart 发送完这个字符之后，它会中断，并且中断例程完成标志设置为 1 ，并进行唤醒，所以，这里有一个 sleep wakeup ，对于每个字符的循环的迭代中，uart 实际上有能力发送一些数量，比如 4 个或 16 个或更多字符，所以，更高效的驱动程序在这个循环的每次迭代中向 uart 传递 16 个字符，每 16 个字符有一个中断，并且更高速的设备，比如以太网驱动器，通常每个中断接收多得多的字节。好的。好的，这只是接口概况的展示。sleep 和 wakeup 很好用，它们好用的一个原因是，它们相对灵活，sleep wakeup 并不关心 uart 是什么。是的，你可以。你不必告诉 sleep 你在等什么，不必告诉 wakeup 发生了什么事，你只需要使用匹配的睡眠通道 64 位值。然而， sleep 接口有一个有趣的属性，我们要在这里传递一个锁，第二个参数，这个锁参数，为什么 sleep 需要第二个参数，背后有一个很大的故事。我会解释这是怎么回事，但更高层次的画面是，这似乎是不可能的，设计一种完全不知道你要等待的是什么的 sleep 。很难写出一种通用的 sleep ，它只是在睡眠，等待一些特定的事件，这很危险，我们稍后会看到，称为 lost wakeups ，每种协调机制，必须以某种方式处理这种问题。而 sleep 接口，我们必须传入一个锁，是一种有点丑陋的实现，以某种方式泄漏到接口中，我稍后会解释。好的，我会解释，有一个问题，为什么我们需要完成标志和睡眠通道？我会抓住这个问题，在五分钟或十分钟后解释这个问题。好的，在解释为什么 sleep 要用这个锁之前，我想讨论一下，如果我们有一个更简单的 sleep 会有什么影响，它不需要额外的锁参数。所以。好的，那么。这里的主题是 lost wakeups ，这是现在要描述的问题。所以，假设接口只是sleep 在任意的通道上，没有第二个参数，在没有第二自变量的该任意信道值上，这个不能正常工作，所以我把这叫做 broken_sleep 。你可以想象，如果我们不是更清楚地知道这一点，这样的 sleep 可以简单地将进程状态设置为这个特殊的休眠值，表示不想继续运行了，我在等待一个特定的事件，如果你看一下 sleep 的 xv6 实现，你会看到它还做了其他事情，我们需要记录这个特殊的睡眠通道值，这样将来的 wakeup 调用就可以意识到我们在等待的东西，wakeup 为我们唤醒，你可以想象 sleep 被打破，它只做了这些，我想你也必须获取这个进程的锁。当然，然后[] wakeup 。这是关于 wakeup 是如何工作的，我们想唤醒所有的线程，它们正在等待，使用了特定的信道值调用 sleep ，所以我们只是，for each p 和进程表，如果状态是睡眠状态，它睡眠在我们要唤醒的信道上，然后将状态设置为可运行的，实际上模块[]，这就是 wakeup 所做的。好吧，这个替补的[]会更好，如果 sleep 和 wakeup 都这么简单。让我来演示一下，让我来演示一下。在我回到 xv6 代码之前，让我来概述一下，你将如何在 uart 驱动程序中使用这个 sleep 和 wakeup ，在某种程度上，是我们已经看到的情况的重复，但是使用这个稍微简单一点的接口。你会有。驱动完成这个，有完成标志，然后 uartwrite ，对于缓冲区中的每个字符，然后检查完成标志，还没有完成，就去睡觉，然后传递一个信道，不管是 tx_chan 还是别的，其实并不重要。好的，然后我们将 c 发送给 uart ，并将 done 设置为 0 。然后，中断例程将其完成标志设置为真，当然还有 wakeup 。所以，这就是我们如何使用这种简化的 broken_sleep 。好的，这其实是 broken_sleep 。这里，缺少的是锁，所以，实际上，两个例程，写入例程和中断例程的都必须使用锁。其中一个原因是，这个完成标志，任何时候我们共享数据时，我们都需要对共享数据加锁，另一个原因是，实际上我没有把它放在这里，但是中断例程和写入例程都需要访问 uart 硬件本身，通常这是一个错误，除非你非常非常聪明，让两个线程同时读写内存映射硬件寄存器是错误的，所以，我们需要在这两个子例程中都设置一个锁，为了避免对完成标志竞争访问和对硬件的竞争访问。所以问题是我们应该把锁放在哪里。在中断例程中很容易做到，我们在开始就加锁，不管是什么锁，我想它叫 uart_tx_lock 或别的什么，然后我们在最后解锁。所以中断例程只是获取一个锁并释放它，难题是如何在 uart 例程中放置锁。一种可能是，uartwrite 可以持有的锁对于发送每个字符的整个序列。所以，我们可以在这里获取锁，在这里释放锁。所以，加锁和解锁在处理每个字符的开头和结尾，那么为什么这肯定行不通呢。其中一个原因是我们走出这个循环的唯一方法是，中断例程将完成标志设置为 1 ，但是如果我们在整个序列中持有这个锁，中断例程也需要锁，所以它会在这里旋转等待锁，因为我们持有锁，并且在设置完成之前不会释放它，但是完成标志只能在中断例程获得锁时才能设置，所以我们不能简单地持有锁在发送每个字符的整个序列中。好的，那么另一种可能是，这里的令人讨厌的问题是uartwrite 持有锁，在它希望中断进程发生的时候，就在这里，我们需要中断例程执行的唯一时间是，就在这一点上，其他时间持有锁也没有问题，所以另一种可能是在开始时获得锁，因为我们需要保护对这个共享变量 done 的访问，但在调用 sleep 前释放它，这给了中断例程机会执行并将完成标志设置为 1 ，然后我们会在 sleep 返回后重新获取它。然后我们回到开始，再检查一遍，我们又拿到了锁。好的，让我修改驱动程序来做这件事。我们会看到，好的。看看后果是什么。好的。所以我们所说的 sleep ，我们想要，你可以看到，这个代码所做的，在开始时获取锁，并在结束时释放它，并且中断例程也获取和释放。建议是我们要做两件事，一个是，我们要探索为什么我的 broken_sleep 方法，它只使用一个参数，为什么不能工作。所以为了正确地进行使用锁，我们调用 broken_sleep ，但是我们在这里释放锁，并在 sleep 返回后重新获得它。这个 broken_sleep 所做的就是，我写在白板上的东西，也就是，它将状态设置为睡眠，并将信道设置为 tx_chan 参数，然后调用 swtch 。好的，让我们看看会发生什么。哦，看看这个，真的成功了，所以 init 正在打印，它是 init 启动消息，它成功地写下了几个字符，但是似乎卡住了。如果我输入一个字符，我输入一个句号，通过输入句号，输出重新启动了，也许我运行 ls 会产生更多的输出，糟糕， ls 也输出几个字符，然后就停了，但是如果我输入一些东西，我输入 x ，将重新启动 ls ，它将继续运行。所以。你觉得这是怎么回事？所以你想提出一个理论。这个问题肯定与我刚刚更改的代码有关，那么发生了什么。好的，这里的问题是，我的新代码释放了锁，在这里释放了锁，然后在这里，中断发生了，因为一旦你释放锁，首先，会重新启用中断，所以，在这个 CPU 上，中断可能发生，这是一台多核心机器，所以中断可以在任何核心上进行，所以几乎可以肯定，在这我标记的这一点上发生的是，在其他核心上， UART 中断正在执行，它在 acquire 上，等待其他核心上的锁。所以当我释放了锁，另一个核心将获得锁，会看到 uart 已经完成了字符的发送，它将把这个传输完成标志设置为 1 ，太棒了，然后它使用 tx_chan 调用 wakeup 。这也很好，除了，因为写入线程仍在释放和 broken_sleep 之间执行，写线程还没有进入睡眠，所以，中断例程调用的 wakeup并没有唤醒任何东西，因为没有什么东西睡眠在那个信道上。然后，写入线程将继续调用这个 broken_sleep这将将状态设置为睡眠，并设置睡眠信道，但是中断已经发生，并且 wakeup 已经被调用，所以这个睡眠，什么都不会唤醒，因为唤醒已经发生了。这就是所谓的丢失唤醒问题。关于为什么或如何发生这种情况，有什么问题吗？是的，是不是一直都是这样，一旦东西丢失了，一旦 wakeup 丢失了，在下一次，所有被缓冲的东西都会被丢弃。好的，这完全取决于发生了事情的细节。在这种情况下，它只是，这是一种意外，我输入某些内容会导致输出恢复，我输入会导致输出得到修复，原因是 uart 只有一种中断，它调用相同的中断例程，无论是信号输入还是信号完成输出，所以，当我输入内容时，会调用 uart 中断例程，而 uart 将其作为输入已经到达的信号，但实际上 xv6 中的中断例程，注意到这段代码需要写入，如果 uart 准备好发送另一个字符，它总是调用 wakeup ，虽然刚才可能已经调用过 wakeup 了，所以这在某种程度上是偶然的，我输入字符导致重启，有时候，如果有丢失的唤醒，幸运地话，有时它们会以这种方式修复自己，而有时它们不会，如果 uart 具有分开的接收和发送中断例程，那就没有办法摆脱这件事了。这回答了你的问题吗？好的，谢谢。好的。所以。好的，请继续。tx_done 位的作用是什么？哦， tx_done 位，你是说这个标志 tx_done ，这是中断例程与 uartwrite 通信的一种方式。先前传输的字符已经完成，uartwrite 可以继续传输下一个字符，所以它是一小段，中断例程到 uartwrite 的一个小通信标志。因为如果它会睡眠，然后，当它醒来时，它会知道，可能是 uart 中断唤醒了它，所以可能已经设置了 tx_done 位，但如果我们没有它，所以我想我是说，wakeup 应该知道它是由 uart 中断引起的。好的，这是你提问的另一种方式，为什么这里会有个 while 循环，而不是仅仅。好的，我想回答了我的问题，我想问题的答案是，因为 uart 中断有两个目的。好的。是的，一般说来，你的问题的答案是更笼统的回答的一个具体例子，事实证明这不是实际的，让睡眠和唤醒保证精确，也就是说，如果 sleep 返回，那么可以肯定，无论你等待的是什么都已经发生了。举个例子，假设我们有两个进程，它们同时试图写入 uart ，它们都在 uartwrite ，它们可以，因为在一个写完一个字符之后，它会休眠，释放锁，然后另一个可以进入循环并等待，直到 uart 不忙。它们可能最终都会睡眠，并且当 uart 中发生中断时，可以再接受一个字符，它们都会被唤醒，但只有一个能真正写入这个字符。这个 while 循环。事实上，我相信在 xv6 中的每个 sleep 都会出现 while 循环，就是因为这个问题，你可能醒来了，但实际上有人拿走了你等待的东西，所以你得再睡眠一次，这种情况随处可见。好的。谢谢。好的。我有个问题。好的。所以看起来我们只看到一次丢失的唤醒，因为只要我们按下键输入一个字符，剩下的输出就都出来了，我们不是应该看到多个丢失的唤醒吗，为什么没有再发生一次。哦，它是这样的，让我来运行这个，我要运行 readme ，我要执行 cat README ，这是几千字节，所以，糟糕，我已经输入一些东西了。我想我们已经有一个字符，我要输入一个句号，我们又多了几个字符，然后它又卡住了。我要再输入一个句号，另外几个字符。每次我输入句号，会引起输入的中断，然后唤醒这个进程，它可以再写出几个字符，然后再挂起来。另一个丢失的唤醒。是的，错过了，有道理。是的，所以我输入句号，每次丢失的唤醒都会得到几个字符，因为丢失的唤醒需要这个巧合，中断已经发生了，并且等待获取锁，正是这样，我们经常会遇到这样的巧合，但并不总是。好的。好的，我们的目标是避免这个丢失唤醒的问题，通过消除这个窗口，在 release(&uart_tx_lock) 之间，我们必须释放它，因为中断需要这个锁，所以我们必须释放锁。但是，我们想要消除这个窗口在释放锁和进程将自己标记为睡眠中，所以中断的 wakeup 可以看到进程处于睡眠状态，真正唤醒它，而不会丢失唤醒。所以我们要消除这个窗口。要做到这一点，会使睡眠接口变得更复杂，所以，回到原来可以工作的 sleep ，我称为可工作的 sleep ，人们解决这个问题的方式是就是 sleep 需要，即使 sleep 并不知道你在等什么，它需要你在等待一些东西，另外，还需要一个锁，保护你等待的任何东西。所以它需要有一个睡眠状态，它并不知道，睡眠状态是 tx_done 等于 1 ，sleep 不知道睡眠状态是什么，但是它需要有一个锁，保护睡眠状态，也就是 uart_tx_lock ，当你检查状态时，是持有锁的，你一直持有锁直到调用 sleep ，然后把锁传递给 sleep 。在接口层面上， sleep 保证的是它会原子地使进程睡眠并释放锁，作为一种，至少是一对不可分割的行动，至少对于 wakeup 来说是这样的。所以， wakeup 不会再看到这种情况，你释放了锁，但是进程没有睡眠，sleep 释放锁，进入睡眠状态是原子的。这里的规则是必须有一个条件，必须有一个保护条件的锁，当你调用 sleep 时，必须持有锁，你必须把锁传递给 sleep ，另外，当你调用 wakeup 时，也必须持有锁。当你调用 wakeup 时，需要持有这个条件锁，所以这些都是程序员最好遵守的规则，如果想要使用 sleep 和 wakeup 编写正确的代码。好的。让我们来看看 sleep 和 wakeup ，试着发现它们是如何使用这个额外信息和这些规则，以避免丢失的唤醒。首先我想看一下 wakeup ，wakeup 并不令人惊讶，它遍历整个进程表，它锁定每个进程，还记得吗，在进程加锁之后，你不能查看加锁进程的状态，它锁定每个进程，如果进程处于睡眠状态，而且睡眠的信道跟传递给 wakeup 的信道一样，然后 wakeup 将进程的状态更改为可运行，然后释放进程的锁。这里没有什么令人惊讶的地方。我们忽略我的 broken_sleep ，而是查看原来的 sleep ，这里是 sleep 的实现，现在，有这个新的 lock 参数。所以我们知道 sleep 必须释放条件锁，这是它的第二个参数，我们知道它必须释放它，因为中断例程必须能够获取，所以我们知道在 sleep 中某个地方释放锁，实际上，这是那个释放锁的地方，当然，我们会担心，在我们在这一点解锁之后，可能会调用 wakeup 并且唤醒该进程，所以为了唤醒这个进程，不过，我们还没有标记为睡眠，所以我们不能让 wakeup 执行。糟糕。让 wakeup 在这个 release 之后执行，即使我们已经释放。所以为了避免这种情况发生，在释放条件锁之前，sleep 会获取将要睡眠的进程的锁。如果你还记得， wakeup 必须在持有条件锁的情况下调用，并且它获取，如果要唤醒进程，它首先必须等待获取进程锁。所以在这段时间里，在 uartwrite 检查状态之前，当我们在这里调用 sched 时，这个线程始终持有条件锁和 p->lock 中的一个。我回到 uart ，想强调这一点，uartwrite 在这里获取状态锁，并将状态锁一直持有到它调用 sleep ，所以它获得状态锁，在持有条件锁的情况下检查，在持有状态锁的情况下调用 sleep ，所以 wakeup 现在什么都做不了，因为它甚至不允许调用 wakeup ，直到调用者拥有条件锁，所以 wakeup 肯定不是现在执行，我们还持有锁，我们调用 sleep ，sleep 释放了条件锁，但首先获取进程锁。如果你还记得，糟糕，调用 wakeup 使用条件锁，在我们释放它之后。抱歉，在我们释放条件之后，wakeup 可以被调用，但是 wakeup 不会看到这个进程，直到它拥有我们持有的进程锁。好的，所以 wakeup 仍然没有被执行。获取进程锁，释放条件锁，在持有进程锁的同时，把进程标记为睡眠在这个信道上，然后调用 sched ，它会调用 swtch ，我们仍然持有进程锁，所以 wakeup 没有做任何事情，如果你还记得，从这个线程切换到调度程序线程，调度器例程在调用 swtch 返回后，释放最近运行的进程锁。所以，在这里，在调度器中时，wakeup 可以获取这个进程的 p->lock ，注意到它睡眠在这个信道上，并将它的状态设置为唤醒。所以我们保证了规则的效果，当你调用 sleep 时，必须持有条件锁，sleep 知道这个锁，并且只有在获取 p->lock 之后才释放它，而 wakeup 需要持有这两个锁，为了[考虑]这个进程，意味着我们不会再失去一个 wakeup ，所以我们解决了丢失唤醒的问题。我意识到有些[关联]，对于这里的事情，有什么问题吗？好的，请随时提问。好的，我们看到这个案例其中使用 sleep 和 wakeup ，并避免了丢失唤醒，在 xv6 中还有很多其他的版本，特别是，我们在等待的条件，是发生中断，表明硬件已经准备好做下一件事，也有一些时候内核代码调用 sleep为了等待其他的线程做一些事情，在概念上并没有什么不同，但可能感觉有点不同，例如，在管道代码中，如果你查看 piperead ，这里有一堆东西，你可以忽略，但是管道中的的读系统调用，最终调用 piperead ，这里有一把锁保护管道，而这就是条件锁，piperead 需要等待，直到有数据缓冲在管道中，那个条件是数据就绪，是 nwrite 大于 nread ，也就是写入的字节多于读取的字节，当这不是真的， piperead 在 sleep 上，等待那种条件，然后传入这个管道锁，条件锁到 sleep 中，为了防止丢失唤醒。所以你可能丢失唤醒的原因是，在不同的核心上，在文件的上面一点，在另一个核心上，可能有其他线程，刚刚调用了 pipewrite ，它会将字节添加到管道缓冲区，最后在 piperead 等待的信道上调用 wakeup ，我们想要避免读取者检查的风险，注意到这里没有字节可读，并且调用 sleep ，我们不想让另一个核心上的 pipewrite 在这里睡眠，增加字节，唤醒我们，在我们进入睡眠之前，会是一次丢失的唤醒。这把锁，sleep 很小心地释放它，防止写入者睡眠，在检查条件和睡眠之间，因为写入者也必须获得锁。对于你们来说，有人问到关于包裹 sleep 和 while 循环的问题，pipereed 和 pipewrite 都是在一段时间内睡眠的例子，比如，这是 piperead 等待数据出现的循环，等待管道缓冲器为非空，这个 sleep 被包裹在一个循环中，再说一次，原因是，完全可以有多个进程读取相同的管道，所以，如果写入着向管道写入一个字节，那里只有一个字节，写入者会调用 wakeup ，这将唤醒所有正在读取该管道的多个进程，但是管道中只有一个字节，所以其中一个进程会首先被唤醒，它会首先从睡眠中醒来。这提醒了我，关于 sleep ，还有一件重要的事情我忘了说，这是 sleep 做的最后一件事，我们来看一下，这里是，抱歉，这里是 sleep 的结尾，sleep 做的最后一件事是获取条件锁，所以，你必须在持有条件锁的情况下调用 sleep ，sleep 在返回之前重新获取它。好的，这意味着，如果有一群读者，有一个字节被写入管道，有一群读者被唤醒，其中一个会成功，其中一个线程成功获取这个锁，其他线程会等待并睡眠，旋转等待锁，一个幸运的睡眠进程将会返回，它会回到这个检查上，p->nwrite 比 nread 大 1 ，所以有数据可读，它会跳出循环，将读取一个字节，现在缓冲区里什么都没有了，释放锁，然后返回，现在下一个线程被唤醒，它的 sleep 能够重新获得状态锁，它的 sleep 将返回，重新检查循环条件，但是现在 nread 等于 nwrite ，所以这个线程和任何其他等待的线程将返回睡眠状态。这里再次强调，为什么几乎每次调用 sleep ，都必须包裹在一个重新检查条件的循环中。关于管道使用 sleep ，有什么问题吗？好的， sleep wakeup 接口规则有点复杂。因为你需要透露睡眠时在等待什么，你必须告诉锁，并遵守一些规则，这有时是令人讨厌的。另一方面， sleep 和 wakeup 是相当灵活的，部分原因是，它们不需要真正理解条件本身，只需要一个条件，一个锁，还有其他一些级别稍高的方案，还有其他的协调，比如 semaphore ，你从今天的阅读材料中读到的，其中接口不是那么复杂，比如信号量，你不必告诉信号量有关锁的信息，并且信号量的调用者不必担心丢失唤醒，信号量的内部实现，就像你们在今天的阅读材料中看到的，信号量的内部实现担心丢失唤醒。因为接口专用于这些递增递减计数器，处理丢失唤醒的需要不会显示到接口中。所以信号量稍微简单一些，尽管它们不那么普遍，如果你没有一个计数，你不是在等待计数，那么信号量不一定会对你非常有帮助。所以，我说 sleep 和 wakeup 会更普遍一些。好的。所以有了 sleep 和 wakeup 的经验，我想谈谈 xv6 面临的另一种挑战，它与 sleep 和 wakeup 有关，那就是如何关闭线程。每个线程系统，线程最终都需要退出，我们需要清理它们的状态，释放它们的堆栈，在 xv6 上，当一个进程退出时，我们需要释放它的用户内存，释放它的页表，释放它的 trapframe ，将进程表中的插槽标记为可重用。所以，典型的清理要求是，一堆东西需要释放，当线程退出或被杀死时。但是这里有两个大问题，与线程退出或杀死线程有关，其一，我们不能仅仅伸手，我们通常不能伸手，单方面摧毁另一个线程，问题是，其他线程可能正在另一个核心上执行，并使用其堆栈，可能要将寄存器保存在结构上下文中或其他什么，另一个线程，如果它在内核中，可能持有锁，它可能正在对内核数据结构进行一些复杂的更新，如果我们只是简单地强行中止它，并停止，杀死在它的[轨道]上，那么它可能已经完成了对一些内核数据的部分更新，但我们在它完成之前强行中止了。所以，我们不能承受这些事情发生。另一个问题是，即使线程调用 exit ，不是决定要杀死，而是自己决定要退出，它具有在执行时使用的资源，比如它的堆栈，和在进程表中的插槽，当它还在执行时，它可能不能释放还在使用的资源。所以我们需要一种方法让线程，对于最后几个资源，释放对执行至关重要的东西，即使线程需要它们，即使只是执行任何代码。好的，记住这两个问题，xv6 实际上有两个东西，关于停止线程或进程，一个是退出，另一个是杀死。那么，我们先来看一下退出。我会查看 proc.c 中用于退出的代码，这是 exit 系统调用所调用的。退出最终要做的，从外部接口上了解，它会释放进程内存和页表，关闭打开的文件，我们也知道有一个 wait 调用，可能是父进程创造的，exit 最终也必须唤醒父进程，所以我们要在 exit 代码中查看这些东西。所以你可以看到一些这样的东西，比如 exit 关闭打开的文件，这可能非常复杂，因为文件系统关闭文件，涉及到比如引用计数的东西。我们还没到那一步，但我们会看到，这需要相当多的工作。但是当你调用 exit 时，进程会继续并关闭它自己的文件。它也会做一些类似的事情，它具有当前工作目录的记录，就是当你是使用 cd 时会变的目录，它需要在将该引用释放到文件系统中。然后，还有其他一些讨厌的事情发生，这与事实有关，如果进程退出，但它有自己的子进程，这些子进程由初始进程继承，因为事实证明，我们几分钟后就会看到，每个退出的进程，都有来自父进程的对应的等待，这完成了退出过程中的一些步骤，所以如果我退出，我的子进程需要等待，我是它们的父进程，我不会等待它们，因为我已经退出了。所以这里有一段代码，exit 重新给退出进程的子进程设置父进程，这样它们的父进程就是 init ，它的进程 ID 是 1 。最后，一个进程唤醒它的父进程，可能在睡眠并等待，并将自己的状态设置为僵尸状态，我们还没有，但我们稍后会看到，这个进程还没有完全释放所有的资源，所以它没有准备好重新使用，我们想要在一个位置结束，这个进程所有的状态可以由 fork 重用，被另一个不相关的 fork 使用。但我们还没到那一步，稍后我们会看到为什么，我们把状态设为僵尸状态，然后我们还没做完，我们还没有释放这个进程，我们只是跳到调度器中，还没有完全释放我们的资源。好的。所以这里的故事还在继续，这里僵尸进程不会运行，调度器仅运行可运行的进程，这个进程没有释放，因为这是状态等于未使用，但它肯定不会再运行了，因为在僵尸状态中，所以对于调度器，现在运行的是其他东西。我们从关于退出和等待的描述中知道，在 Unix 中，如果进程退出，那么它的父进程调用 wait ，wait 将返回信号通知父进程，它的一个子进程退出了，所以我们可以查看 wait 的实现。wait 的实现也是一个大的循环，但是当进程调用 wait 时，它扫描进程表，查找父进程为当前进程的进程，特别是查找这样的进程，它的父进程是当前进程并且处于僵尸状态，那就是它们走到这一步，在退出或即将完成退出。所以现在我们发现这调用 wait 系统调用，找到已退出的子进程。我不知道你是否记得 exit 可以返回这个 32 位的退出状态，让父进程，剩下的代码可以收集这些信息，然后是父进程调用 freproc ，完成了最后一部，释放进程的资源。所以我们来看一下 freproc ，这是最后一次关闭，这是很难处理的，如果退出进程在执行时被释放，它冻结 trapframe ，冻结用户页表，如果我们释放内核堆栈，现有进程的内核堆栈也将在这里释放，但是由于堆栈保护页，我们没有释放内核堆栈，无论何时释放内核堆栈，我们都不会免费，但是这些东西在退出时都是一种潜在的痛苦，当正在运行的退出进程由父进程释放时。这里需要注意的一件事是，wait 不仅是为了父进程方便，它们想知道子进程什么时候退出，wait 实际上是退出进程的关键部分，在 Unix 中，这是一个需要，wait 对应于每个退出的进程，这就是为什么当进程退出其子进程时，某种程度上给了 init ，它们成为了 init 的子进程，当 init 在循环中调用 wait 时，因为每个进程都要等待，让父进程可以调用 freproc 并完成资源释放，然后当它完全完成的时候，如果父进程完成释放所有退出进程资源，将子进程状态设置为未使用，现在 fork 或将来调用 fork 可以重用该进程槽。有什么问题吗？哦，我有个关于 sleep 的问题，所以在 sleep 中，我们。是的，当我们，哦，抱歉，不是 sleep ，而是 exit 。我想说的是 exit ，是的。是的，为什么我们，为什么我们要在重新分配父进程前[抓住]最初的父进程，这是必须的吗。这是[]代码，处理一种可能性，进程和父进程同时退出的情况。还有一些，尽管最初的，不关心进程退出，它的父进程等待它，一切都很好，但它可能是进程退出的父进程，并且它的父进程同时退出，所以当我们试图唤醒父进程，告诉它我们已经退出时，那个父进程已经退出了。这里有很多这样的代码，我觉得我一年前是明白的，但现在不再明白了，是关于处理这种罕见的父进程和子进程同时退出的情况。如果不是因为这个，那么这一切都会非常简单，进程有一个父进程，它只需要在这里唤醒它的父进程。而且，我们不会，它是对于父进程和子进程同时退出的。子进程可以唤醒父进程。好的，我明白了，谢谢。抱歉，我不能解释更多了。我有一个简短的问题，为什么我们要在唤醒父进程之后将进程状态设置为僵尸状态，我们想在此之前这么做。哦，是的，是因为我们获得了，现有进程获取了自己进程锁，父进程不能看到这个进程。我们在这里获得了自己的锁，然后我们调用 sched ，父进程需要等待子进程的锁，也就是 p->lock ，这意味着在这个获取和我们调用 sched 之后，调度器线程释放 p->lock 之间，在这块代码中，父进程不能看到这个进程。好的。所以这些东西的顺序并不重要，事实上，如果我们没有锁，也有可能其他的。在大多数情况下，如果我们不持有锁，任何顺序都不会起作用。不管怎样，是的，因为我们有一把锁，顺序无关紧要，因为父进程看不到。理解了。好的。好的，我想强调的技巧是，在很大程度上，子进程没有，最终，子进程并没有释放所有的资源，因为它不能，因为它仍在执行，取而代之的是一些其他线程，也就是父线程来释放执行所需的资源，所以这是一种技巧，让我们极大地简化了退出。好的，我最后要看的是 kill ，你知道 kill 系统调用，在 Unix 中，可以调用 kill 系统调用，传递另一个进程的进程 id ，目标是，导致另一个进程，目标进程停止执行。如果我们一不小心，这有风险，特别是如果内核中执行的其他进程，这个风险可能在几分钟前就提到了，你想杀死的进程的内核线程，可能正在更新一些东西，比如，更新文件系统，创建文件，我们不能直接杀死它，因为这将使一些多步骤操作只完成一半。现在，我们知道 kill 不能真正停止目标进程，事实上，在 xv6 中，在 Unix 中也是如此，kill 几乎不会做什么事情，它扫描进程表，查找目标进程 id ，只是在进程的进程结构中设置这个标志，而且，如果它处于睡眠状态，调用到可运行，从睡眠中唤醒，它所做的就是设置这个标志，它不会停止另一个进程的执行或任何类似的操作。kill 是非常温和的，游戏是，目标进程在内核代码中，在那里会安全地停止执行，另一个进程检查它自己的终止标志，如果设置了，其他进程会自动退出，调用 exit 函数，你可以在 trap.c 上看到其中的一些点，实际上，你可以在 trap.c 中看到所有的点，所以，如果我们在 usertrap 中，在实际执行系统调用之前，如果进程已被终止，或者如果在这个进程调用了 kill ，然后进程调用 exit 并离开。这是内核中的一个点，没有持有任何锁，它没有在做任何事情，所以，进程是安全的，只需退出并调用 exit 。在 usertrap 末尾有一个类似的检查。所以，在系统调用之后，进程还会检查它是否被终止，实际上，这段代码会执行，即使进程被中断打断，比如，定时器中断关闭，然后，这段代码会执行，我们会看到这个进程已被终止，然后这个进程将会退出。所以，这意味着，kill 的意思是，kill 的作用不是直接停止其他进程，而更像是，如果用户空间中的其他进程，然后下一次它进行系统调用时，它会退出，或者下一次定时器中断发生时，当目标进程执行其他用户代码时，如果它被定时器中断或其他中断中断，那么它也会退出。所以，这里可能会有明显的延迟，在一个进程调用 kill 和另一个进程实际退出。事情就是这样的。虽然有一个中间问题，如果进程没有在自己的用户空间，而是在进行系统调用中，它被杀死，我们需要做些什么特别的事情吗？可能会出现这种情况的原因是，假设进程正在从控制台读取，读取你输入的下一个字符，你可能知道明天都不会输入另一个字符。这是可以的，如果你终止进程，它实际上在明天之前就消失了。所以，在 xv6 的很多点上。当进程在睡眠中，xv6 会安排，如果它在内核中睡眠时被杀死，它实际上会退出。让我来展示一下这个机制，首先要看的就是 kill 本身。你可以看到，如果目标进程处于睡眠状态，然后 kill 会将状态设置为可运行，这意味着即使它调用了 sleep ，调度器现在也会运行它，它会简单地从睡眠中恢复过来。所以让我们来看看一个真正重要的地方，在管道代码中，如果进程处于睡眠状态，等待读取管道，它被杀死， kill 设置从睡眠到可运行，回到这个循环的顶部，如果在管道中没有数据，在管道中仍然没有数据，现在， piperead 会检查进程是否已被终止，如果它已经被杀死，不是再次睡眠，piperead 会返回一个错误，并不重要，但我们要回到的是这个系统调用，因为我们在系统调用中，管道最终返回系统调用，然后 usertrap 再次检查 p->killed ，现在可以退出了。所以，对于 sleep 来说，我们知道[逃脱]是可以的，当它被杀死，这些循环检查 killed 标志，但也有 sleep 这样是不可以的，对一个要退出的进程，如果它在睡眠状态下被杀死。比如，如果某个进程正在更新磁盘上的文件系统，要创建新文件，现在不是时候，即使它在睡眠等待磁盘，对于一个进程来说，现在不是退出的时候，因为它已经被杀死，我们要完成完整的文件系统操作，只有在那时进程才可以退出，所以你可以看到这个，我们已经看过了，但我将在磁盘驱动程序中展示。睡眠循环的一个例子，不会检查 killed ，看那里,所以在这里，这是 sleep ，进程等待磁盘完成读取磁盘块，它绝对不会检查 killed ，因为它想要完成它，也许在创建文件的过程中，涉及多个磁盘读取和写入，想要完成整个文件系统操作，整个系统调用，然后才检查 p->killed 并退出。有什么问题吗？我有一个问题，为什么 kill 允许以这种方式，为什么进程不会杀死所有其他的进程，你可以自己运行。你知道，如果你在麻省理工学院的 Athena 分时机器上这样做，可能会把你赶出学校。是的，但是为什么允许这样做呢？在 xv6 允许，因为 xv6 是一个玩具操作系统，它只是在 xv6 中，任何与权限有关的东西都不存在，在 Linux 或真正的操作系统中，每个进程都有一个用户 id ，对应于执行该进程的人类用户，一些系统调用使用进程的用户 id检查允许哪些进程执行此操作，所以在 Linux 中，你会在这里看到一个额外的检查，就是调用进程必须具有相同的用户 id ，与想要杀死的进程一样，否则是不允许的。而且，至少在分时背景下，我们有多个用户，我们不希望他们杀死彼此的进程，这或多或少可以防止人们，使其难以杀死其他人的进程。好的，我明白了，谢谢。init 进程是否会退出？让我找找。是的，如果 fork 失败，退出，然而，这个问题的真正答案是否定的，其目的是 init 永远不会退出，它们只是在这个循环中，一遍又一遍地调用 wait ，如果 init 退出，我认为这是系统中的致命错误，系统将崩溃。内核中的某个地方有代码，可能在 exit 中。检查一下 exit 。是的，在这里， exit ，在 exit 的开头，如果当前进程是是 init 进程，那就是一个 panic 。因为，这个系统最终会陷入停顿，如果没有 init ，因为这样就没有什么东西收集，没有什么东西调用 wait 对于那些退出的进程，并且没有任何东西可以完成进程的释放，慢慢地，我们的进程耗尽了，那就是另一个错误了。我们必须有 init 。所以真正的答案是不，它确实不能被允许退出，最好不要退出。好的。还有其他问题吗？这是另一个问题，所以，我们在这节课上还没有谈到太多，但看看关闭操作系统会发生什么或需要发生什么。被拔掉电源。好的。好的。是的。是的，这很复杂，这取决于你运行的是什么，如果你在，如果机器当前什么都不做，好的，这个问题的部分答案是，文件系统最终，因为文件系统是永久性的，文件系统会从一个重启到另一个重启。我们需要让文件系统保持良好的状态，所以，如果我们正在对文件系统进行某些更新，比如创建文件，然后我们想要关闭系统或者停电之类的，我们绝对需要一个策略来确保，尽管我们正在对文件系统进行一些复杂的更新，我们不会把它留在文件系统中，我们不会在磁盘文件系统结构中公开任何损坏的不变量，因为文件实际上只是一种在磁盘上的数据结构。所以有很多这样的，一些机制来确保，如果你关机了或者停电了或者谁知道会发生什么，我们可以恢复磁盘上的文件系统。对于其他，不是为了这个，那么，你是不是要做一些特殊的事情来关机，取决于你正在运行什么进程，如果你正在运行某个重要的服务器，一台数据库服务器，许多其他计算机依赖，并从网络上访问，答案可能是你不被允许关机，因为你提供的服务对很多其他计算机都很重要。如果你的计算机没有执行任何操作。那你就可以把它关掉，它会停止执行，没有什么可做的，真的，也许你问题的答案是，如果你想关闭计算机，确保文件系统状态良好，然后停止执行指令，就好了。好的。有道理，是的，谢谢。还要别的吗？哦，抱歉，我还有一个问题，那么这个信号量接口是什么。关于 p 和 v 。所以，只有这两个函数？嗯，是的。是的， xv6 实际上没有信号量，但是书中的信号量，我想它只是 p 和 v ，信号量对象有两个方法 p 和 v 。好的，我明白了，谢谢。好的。还要别的吗？好的。



## 现代操作系统

睡眠（sleep）和唤醒（wakeup）是一种简单而有效的同步方法，但还有许多其他方法。它们的第一个挑战是避免本章开头提到的“丢失唤醒”问题。原始Unix内核的sleep通过禁用中断来解决这个问题，因为Unix运行在单CPU系统上。而xv6运行在多处理器上，因此它在sleep中添加了一个显式锁。FreeBSD的msleep采取了相同的方法。Plan 9的sleep使用一个回调函数，它在进入睡眠之前调度锁被持有时运行；该函数作为对睡眠条件的最后检查，以避免丢失唤醒。Linux内核的sleep使用了一个显式的进程队列，称为等待队列（wait queue），而不是等待通道（wait channel）；该队列有自己的内部锁。

扫描整个进程集以唤醒所有进程是低效的。更好的解决方案是将sleep和wakeup中的chan替换为一种数据结构，用于保存睡眠在该结构上的进程列表，如Linux的等待队列。Plan 9的sleep和wakeup将该结构称为会合点（rendezvous point）或Rendez。许多线程库将相同的结构称为条件变量（condition variable）；在这种上下文中，sleep和wakeup操作被称为等待（wait）和信号（signal）。所有这些机制都有相似的特点：睡眠条件通过某种锁进行保护，并在进入睡眠时原子性地释放锁。

wakeup的实现会唤醒所有在特定通道上等待的进程，而可能有许多进程在等待该通道。操作系统将调度所有这些进程，它们将争相检查睡眠条件。表现出这种行为的进程有时被称为惊群效应（thundering herd），最好避免这种情况。大多数条件变量有两个唤醒原语：signal，用于唤醒一个进程；broadcast，用于唤醒所有等待的进程。

信号量（semaphores）通常用于同步。计数通常对应于管道缓冲区中的可用字节数或进程拥有的僵尸子进程数。使用显式计数作为抽象的一部分可以避免“丢失唤醒”问题：有一个显式的计数表示发生了多少次唤醒。计数还可以避免虚假唤醒和惊群效应的问题。

终止进程并清理它们在xv6中引入了很多复杂性。在大多数操作系统中，这一过程更加复杂，因为受害进程可能在内核中深度休眠，解开其调用栈需要小心处理，因为调用栈中的每个函数可能需要执行一些清理工作。一些编程语言通过提供异常机制来简化这一过程，但C语言没有。此外，还有其他事件可能会导致正在睡眠的进程被唤醒，即使它正在等待的事件尚未发生。例如，当一个Unix进程正在睡眠时，另一个进程可能向它发送信号。在这种情况下，进程将从中断的系统调用中返回值-1，并将错误代码设置为EINTR。应用程序可以检查这些值并决定接下来的操作。xv6不支持信号，因此这种复杂性不会出现。

xv6对kill的支持并不完全令人满意：有些睡眠循环可能应该检查p->killed。一个相关的问题是，即使对于那些检查p->killed的睡眠循环，在sleep和kill之间仍然存在竞争条件；kill可能在受害进程检查p->killed之后但在调用sleep之前设置p->killed并尝试唤醒受害进程。如果发生这种问题，受害进程不会注意到p->killed，直到它等待的条件发生。这可能需要很长时间，甚至可能永远不会发生（例如，如果受害进程正在等待来自控制台的输入，但用户没有输入任何内容）。

一个真正的操作系统会使用显式的空闲列表以常量时间找到空闲的proc结构，而不是像allocproc那样使用线性时间扫描；xv6为了简单性使用了线性扫描。