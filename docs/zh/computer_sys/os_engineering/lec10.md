# Lec 10 锁

这节课，我们将关注内核和操作系统中的锁。

## 总览

- 为什么要使用锁？
- 锁的抽象
  - 不同角度理解锁
- 什么时候需要用锁？
- 锁是否可以自动的？
- 锁的问题
  - 死锁
  - 模块化
  - 性能
- 示例：URAT， CAS



## 为什么使用锁？

应用程序希望使用多核处理器来实现并行加速，因此内核必须处理**并行的系统调用**，从而需要并行访问内核数据（如缓冲区缓存、进程等）。锁可以帮助正确共享数据，但也可能限制并行加速。

![image-20240915023949091](http://47.115.50.83:49153/i/66e5d893ec9ee.png)

从 2000 年开始，时钟频率并没有继续增加。基本上是停滞不前（3.3GHz左右）。所以，核心的单线程性能，也基本达到了极限，停滞不前。而另一方面，晶体管数量还在随时间增长，所以，如果你不能使用晶体管使单个核心运行得更快，唯一的选择是使用多个处理器核，处理器核的数量越来越多。所以，应用程序需要更高的性能，就需要避免竞态条件，这正是锁的作用。

## 锁的抽象

锁可以序列化进程的执行，两个进入临界区间的进程，只有一个可以成功，另一个在第一个完成后运行临界区间，所以这里没有并行。

典型地， 锁是一个对象，结构体``struct lock``，提供了``acquire(lock)``和``release(lock)``，每个锁保护一个共享数据结构，因此一般程序而言，会有很多锁。

- 如果多个线程同时调用 `acquire(l)`，只有一个线程会立刻返回，其他线程会等待 `release()` 才能继续执行。

- 一个程序通常有大量数据和大量锁。如果不同的线程使用不同的数据，它们可能会持有不同的锁，这样就可以并行执行，完成更多工作。

- 需要注意的是，锁 `l` 并不专门与数据 `x` 绑定，程序员需要为锁与数据之间的对应关系制定计划。

### 大内核锁 vs. 细粒度锁

如果内核只有一个锁，通常称为**大内核锁**，那么内核中每个系统调用都会被串行化，系统调用 1 获取大内核锁，做它想做的事情，然后释放大内核锁，然后返回用户空间。然后第二个系统调用运行，所以我们有并行运行的程序，并行运行大量的系统调用，所有的系统调用都是串行运行的。而如果两个系统调用使用两个不同的锁，我们说细粒度锁，那么它们实际上可以完全并行运行，没有任何串行化。

> [!IMPORTANT]
>
> 锁并不是自动完成的，一切都由开发者决定的，将锁与数据结构关联，并确保适当的 acquire release 在那里

## 什么时候需要用锁？

### 一条保守的指导原则

**你有两个进程，两个进程访问一个共享数据结构。其中一个是写入者，这意味着它对共享数据结构做修改。那么你需要对这个数据结构使用锁。**

所以这是一条保守的规则，或者说这是一个严格的规则。有一种编程风格称为无锁编程（Lock-Free Programming），比如RCU等，难度很大。 



## 锁是否可以自动的？

如果我们每次共享数据结构，那么共享数据结构中的操作就需要锁，我们应该在每个操作中把锁和数据结构关联起来，实际上，它在数据结构上执行，来获取或释放锁。也许编程语言可以为每个数据对象关联一个锁，编译器在每次使用时自动添加 `acquire/release`，这样程序员就不会忘记加锁。

但是这种方法通常太过死板。例如：

```python
if present(table1, key1):
  add(table2, key1)
```

另一个线程可能会在当前线程有机会向 `table2` 添加之前，移除 `key1`，这个故事告诉我们，需要显式控制锁的持有时间段，以避免在中间状态时出现问题



### 示例： rename系统调用

假设``rename("d1/x", "d2/y")

如果我们遵循严格的规则，我们对 d1 加锁，删除 x ，然后释放 d1 的锁。然后，我们做数据的第二部分，对 d2 加锁，添加 y ，然后释放 d2 。然后我们就完成了。这是一种假设的方案。假如我们已经完成了第一步，还没有完成第二步。另一个进程会观察到文件丢失了。所以正确的解决方案是，我们需要的是，在重命名开始前，先锁定 d1 和 d2 ，然后擦除并添加，然后释放 d1 和 d2 的锁。这是一种方法。另外一种就是大内核锁。

### 锁的三种角度解释

你可以用其中一种解释来构建思考问题的方式。

1. 避免更新丢失。一种非常低级的方式
2. 使多步操作成为原子操作。
3. （真正的作用）维护一个不变量。不变量共享它在保护的数据结构。我们在 ``acquire ``之前，视为持有不变量。当我们获得锁，并进行一些操作时，暂时违反不变量，直到我们``release``锁。
   - 考虑一下 freelist 的例子，不变量是空闲指针指向下一个指针，并且所有的空闲页面都在一个列表上

## 锁的问题

### 死锁

如果我们获取锁顺序不当会导致死锁。

解决方案是，如果你有多把锁，那你就得对锁排序，所有操作都必须按照这个顺序来获取锁。所以如果你是一名系统设计师，你必须决定所有锁对象的全局顺序，比如，在这种情况下，你可能会说 d1 应该总是在 d2 之前，这就意味着，当我们执行``rename``时，规则是我们总是先获取更小数字的目录，在获取更高数字的目录之前。这将确保全局顺序，这种情况就不会发生。

### 模块化

考虑一下锁的顺序，一定是全局的。所以，如果我们有一个模块 m1 调用了模块 m2 的方法。根据遵守我们的锁规则，从 f 和 g 获取的所有锁，需要是在某种全局顺序，m1和m2的调用关系，意味着，就锁而言， **m2 的内部部件必须对 m1 可见**。所以 **m1 可以确保，以适当的方式调用 m2** 。在某种程度上，它会违背一些抽象的理念。如果做的好的话， m1 不需要知道关于 m2 实现的任何信息。因此，当你设计一个更大的系统时，、模块化变得更加复杂。

### 性能

我们想要性能，你需要拆分数据结构，所以如果你有一个大内核锁，这将使你的性能限于在单个 CPU 上，如果你想要具有多个 CPU 扩展的性能，你就得拆分。你需要拆分数据结构。最好的拆分，它并不明显，或者是一个挑战。比如，如果你将锁与每个目录相关联，如果你将锁与每个 inode 相关联，是否将锁与每个进程相关联。如果你需要修改，重新设计锁规则，你必须确保，你仍然保持着不变量，是内核想要保持的。

如果你拆分锁，你还必须重写代码。你也可能需要重写代码。所以事实证明，你应该重构内核的一部分，或者你的程序的一部分，为了获得更好的性能，通过拆分数据结构或引入更多的锁，有很多工作要做，你要仔细考虑，想要维持的范围，你必须要写代码，一般说来，这是一项繁重的工作，它们并不容易。所以这是比较负面的观点，因为我们想要更好的性能，这需要更多的锁。但这是一项繁重的工作。

对于这个问题，有一种**普遍的做法**是，从粗粒度的锁开始逐步细分，然后测量。不管是什么应用程序运行在内核上，如果它们利用了多个核心，观察得到了加速，你就完成了，你的锁设计已经够好了，如果没有加快速度，这意味着某些锁被争用，多个进程尝试获取相同的锁，因此它们被串行化了，所以，得不到加速。

这里的重点是，你要以这些**测量**来指导，因为可能是这种情况，一些使用粗粒度锁的模块不是经常并行调用，所以，没有必要对它重新设计，因为重新设计有很多工作要做，这也会使代码的条理变得复杂，不重新设计这些是一个更好的选择。所以，一般说来，一条好的规则是，**从粗粒度的锁开始，测量这些锁中的一个是否出现争用，然后重新设计系统的这一部分，你就会得到更好的并行性**。

## 示例：URAT， CAS

UART 只有一个锁，你可以认为这是一个可用的粗粒度锁。这个锁保护 UART 传输缓冲区，以及写指针和读指针。这些数据结构具有某种不变性，从读到写，任何在 R 和 W 之间的字符都是要发送的，任何在 W 和 R 之间的东西都是空插槽。这个锁帮助我们维持这个不变量。

所以，我们回到代码。让我们看一下 acquire 。这里是 uartputc ，uartputc 做的第一件事是，获取锁，然后插入一个字符，如果缓冲区中有位置，将字符插入缓冲区，开始打印，然后释放锁，所以，如果两个进程同时调用 uartputc ，那么锁将会确保，第一个进程中的一个字符出现在第一个插槽中，然后第二个进程的字符进入下一个插槽，它们不会最终进入同一个插槽。所以这是一个明显的例子，锁可以帮助我们避免竞态条件，否则，第二个进程可能会覆盖第一个进程的字符，这是一部分。然后，我们看看。我们之前看过一点，我们查看 start ，我们看到有更多的事情。这个，我们看到，如果缓冲区不是，如果缓冲器不为空，那么我们知道有一些字符，正在进行或正在发送。你知道锁可以确保，我们不会覆盖其中的任何一个，所以任何队列尾端的东西，是由 UART 本身处理的。所以，尾端正在处理。我们确保，通过获取锁，我们不会在这个方面修改或干扰。最后，更多的事情是，对 UART 寄存器的写入，比如 THR 寄存器，这里只有一个锁确保，记得 uartstart 在持有锁的情况下调用，确保只有一个写入者到 THR 寄存器。所以，另一方面锁确保的是，硬件寄存器有一个写入者。好的?还有一件有趣的事，我想稍微谈一下，那就是 UART 硬件完成后，会有一个中断。如你所知，我们注意到在 uartstart 之前，我们有调用者，它是获取锁的调用者，来确保，我们不会有多个[]写入写寄存器。UART 中断本身可以与调用 printf 的其他进程并行运行，所以，有一些实际的 printf 运行在 CPU0 上，而在 CPU1 上接受 UART 中断，因为它什么都没做，所以，它可以在任何时间点中断。它将会调用 uartstart 。它必须是这种情况，我们希望确保硬件寄存器中只有一个写入者，或者保护传输缓冲区的不变性，我们必须获取锁。所以，在 xv6 中，中断可以运行驱动的下半部，可以在不同的处理器上真正的同时运行，和驱动的上半部一起，所以，中断函数也需要锁。实际上，在这种情况下，需要在 UART 中的锁，然后调用 uartstart ，然后释放锁。稍后，我会回到这里，因为实现锁比较困难，在这种方式下，这是正确的。你应该担心的事情是，稍后我会讲一下，等到了那里我再讲。好的，关于这个，有什么问题，一个简单的使用锁和 UART 的例子。好的，让我来讲一下，让我来讲一下实现锁。所以，规范是只有一个进程可以获取锁，在任何时间点锁持有者都不会超过一个。我们想要了解，如何实现锁，以保证规范的方式。我会先写一个错误的锁，让我们理解这里的挑战是什么，或者错误的 acquire 。这样，我们知道挑战是如何实现 acquire 。这是我的错误的那个，acquire struct lock *l ，它所做是，在它后面，有一个无限循环 while(1) ，如果 l->locked 是零，表示没有人持有它。然后，调用者应该获取锁，那么，我们把 l->locked 设为 1 ，在这个时刻我们拿到了锁，我们可以返回，什么事情都不用做，并结束循环。如果我们没拿到锁，因为 locked 是 1 ，这意味着有其他人持有锁，我们只需要继续旋转，等待一遍又一遍地循环，直到在某一时刻，锁持有者调用 release ，它会将 locked 设置为零。那么这个实现的问题是什么？我想两个进程可能同时读到它没有被锁。是的，没错。所以，这里有一个竞态条件，竞态条件在这里。我们可以有两个 CPU 进来。我们看这个时序图，你知道 CPU1 ， CPU0 ， CPU1 ，这是语句 A，这是语句 B ，CPU1 到达语句 A ，CPU0 和 CPU1 都到达语句 A ，所以，它们都看到 locked 是零，然后，它们都执行了 B 。好的，这里它们看到 locked 是 0 ，这个也看到的是 locked 是 0 。所以，它们都执行语句 B ，它们都获得了锁，那么这个函数违反了规范。这能理解吗？所以，要解决这个问题，并得到一个正确的实现，有多种方式可以做到，但最常见的方式是依靠特殊的硬件指令。这条硬件指令所做的就是，它做这个测试，并原子的设置。所以，解决这个问题的方法是硬件的 testandset 支持。你可以这样想，在 RISC-V 上，这条指令是 amoswap ，是我们会用到的。基本上可以归结为测试和设置，硬件保证，你使用这三个参数，地址，寄存器一 r1 和寄存器二，寄存器1 R1和寄存器2本质上硬件所做的是，它锁定这个地址，如果你愿意，我们稍后会更多的讨论锁定地址。它（把值）放入一个临时变量，那个地址中的值。然后，将 r1 的值放到地址中。然后把原始地址的值放入，tmp 中的原始值放入 r2 ，然后释放锁，并返回。在这个锁里，你能保证这个测试，测试结果会返回到 r2 ，而设置是原子发生的。所以这是一条硬件指令，大多数处理器都有这样的硬件指令。因为这是一种方便的实现锁的方法。所以我们要做的是，我们减少软件锁实现原子性，而是硬件锁实现。处理器可能会使用不同的方式实现这个，所以，指令集本身有一份说明书，它并没有说明它是如何实现的，它非常依赖于实际的实现。显然取决于内存系统的工作方式。比如，如果多个处理器共享单个内存控制器，对内存进行读写，那么内存控制器可以支持该操作，允许在一个特定的地址设置锁，然后让一个处理器做两个操作或三条指令，然后解锁，因为所有处理器的读写都通过这个内存控制器，内存控制器可以对锁进行排序。如果处理器中的内存位于共享总线上，通常是总线仲裁器可以做这个，总线仲裁器支持以原子方式执行两条内存操作。如果处理器有缓存，那么它通常是高速缓存一致性协议的一部分，高速缓存一致性协议将确保，如果有写入者，缓存行会持有我们要更新的值，最终在一个单独的高速缓存中，然后，处理器控制有两个操作的单个缓存行的锁。所以，这个的实现，可以用多种不同的方式来完成，但理论上来说，所做的是，你锁定地址，读取原始值，保存新值，然后返回值。这能理解吗？看看我们如何使用这条指令，让我们看看 xv6 中 acquire 和 release 的实现，它还会揭露一些其他有趣的细节。让我先来介绍一下 spinlock.h ，spinlock.h ，如你所见，它非常简单，它有这个 locked 标记，和我们的伪代码一样，还有其他两个东西用来调试，即锁的名称，和当前持有锁的 CPU 。这主要是打印出调试消息，比如，获取相同的 CPU 。好的，让我们来看看它的实现。我们从 acquire 开始。我们先看看这个循环，这实际上是一种测试和设置循环，就是我刚才谈到的，实际上，在 C 标准中，定义了这些原子操作之一，C 标准种有一个函数，也就是 \_\_sync_lock_test_and_set ，指定了我刚才描述的行为。然后，每个处理器都需要实现该行为，由于大多数处理器具有测试和设置硬件指令，这是实现[]的一个合理而直接的方案，实际上，如果我们看看 kernel.asm ，我们可以查看汇编指令，了解 RISC-V 处理器的确切功能。所以。这是我们的汇编结构 acquire ，这是 amoswap 指令。所以你可以看到，amoswap 调用寄存器 a5 ，并且输入和输出也是 a5 ，与整个地址一样长，如果不相等，我们就返回，否则我们会回到，再检查一遍我说的是对的，将 a4 移动到[]，如果不等于，则到 +0x22 。好吧，这有点难计算，但基本上在一种情况下，我们[扩展]，在另一种情况下，我们向后分支。所以，可能看 C 代码更容易。所以让我们到这里，那么会发生什么呢，如果锁没有被持有，lk->locked 的值是多少，lk->locked 会是零，所以我们称之为测试和设置，将会发生的是，我们在 locked 中写入 1 ，但是返回之前的值。所以如果之前的值是零，那我们就好了，因为这意味着没有人拿着锁，我们就结束了这个 while 循环。现在，我们假设锁的值是 1 ，所以可能锁已经上锁，这条指令会做什么，它会读取旧值，把它放在一边，在这种情况下，是 1 ，然后在那个位置写入一个新的 1 。这不会改变任何事情，因为它已经被锁上了。函数将会返回 1 ，表明之前的持有，它已经被锁上了，所以在这种情况下，它不等于零，它会旋转，一直旋转，直到 locked 重新设置为 0 ，而这发生在 release 中。对于这个，有什么问题吗？没有问题？好的。所以现在，让我们看看对应的 release 操作，这里是 release 操作，如果你再看一下 kernel.asm 。那个指令，所以让我们看看 release ，可能在这里后面，这是 release 。所以， release 也使用了这个 amoswap 指令，把 0 放入 s1 。所以这保证的是，这个原子更新 lk->locked ，使用原子操作将 0 写入 lk->locked 。你们中的许多人问到，为什么不直接使用 store ，store 指令来写入 0 。有没有人，能猜猜为什么，为什么那可能行不通，或者问题可能是什么。因为某个其他进程可能正在将 1 写入锁，或者，不，或者再写一个 0 ，但不是这样的，对吧。是啊，可能是对的，可能有两个进程或两个 CPU 同时写入 l->locked ，但我认为真正的问题是，对很多人来说，我也经常这么想，就是你要做一条 store 指令，这是一个原子操作。并不总是这样的，比如，如果你，这取决于架构的实现，比如，如果缓存[]协议工作，或者缓存系统使用缓存行工作，其中缓存行可能大于整数，通常大于整数，那么真正发生的是，第一个操作是加载缓存行，然后是更新缓存行，所以， store 指令里面有两个微操作，你可能会得到错误的结果。所以，为了避免必须了解任何硬件实现，以及整数运算是否是原子的，或者写入 64 位内存值是原子操作，我们使用 RISC-V 操作，它可以保证原子执行。这能理解吗？是的。好的，为了[扩展]一下，amoswap 不是唯一存在的指令，这是 RISC-V 手册，在列表中，是所有的原子操作，所以有 AMOAND, AMOOR ，还有 AMOMAX, AMOMIN ，它们都可以在原子操作中读写一个值。好的?好的，还有其他几件事我想指出，在这个实现中。让我重新回到 acquire 。所以， acquire 函数首先做的是，它关闭了中断。最好能理解为什么是这样，现在，我回到 UART 示例代码。你可以稍微想一想这个。所以我们要考虑一下这个情况，是 acquire 可能没有正确实现，没有关闭中断，所以思考这个问题的方法是，如果我们到 uartputc ，我们假设 uartputc 运行，并且获得了锁，但是不关闭中断，会发生什么。给大家几秒钟考虑一下，如果你有想法或者为什么它可能是错误的，可以直接说。或许，它可以被中断，因为时钟，然后其他事情发生了，它也需要打印一些其他的东西，它试图再做一次 uartputc ，但是锁已经被拿走了。这是一种可能的情况，有一个更直接的例子。我们假设 uartputc 得到了锁，而 UART 正忙于传输一些字符。那么当 UART 完成了传输字符，它要做什么。它会导致中断，并且 uartintr 运行，uartintr 会做什么，它获取同一把锁，uartputc 已经持有了，那么，这里会发生什么，如果只有一个 CPU ，没有其他 CPU 可以运行这个中断。我们现在死锁了，因为当前 CPU 持有 uartputc 的锁，然后，后面传入的中断，它做的第一件事就是，获取已经持有的锁。实际上，在 xv6 的情况下，你会得到一个 panic ，因为相同的 CPU ，正在再次尝试获取相同的锁。所以，获得自旋锁，处理了两种不同类型的并发，其一，它们是两个不同 CPU 之间的并发，而且我们必须保证，中断函数运行在不同的 CPU 上，一般我们不会重新安排传输缓冲区，但是如果它们在同一个 CPU 上运行，我们要确保它仍然是原子的，它不会被中断，所以，我们在 acquire 中关闭了中断。它们只有在 release 结束时才会再次打开，当锁被释放的时候，在这一点上，再次看到，接收中断，因为锁不会再被释放，不是 acquire 持有它，不再持有。这能理解吗？好的，在这个实现中还有一件更微妙的事情，我想说一下，而且我们需要处理。那就是内存排序。比如，你认为锁是，比如， acquire locked 是 1 ，也许我们有一个临界区，x<-x+1 ，然后， release 将 locked 设置为 0 。所以，考虑一下指令方案，它是在特定的 CPU 上执行的，这些是正在执行的指令。现在，如果代码仅仅是顺序的。编译器或处理器可能会重新排序指令，为了获得更好的性能，比如，如果它是顺序流，可以把这个指令移到后面去吗？我们不会改变单一执行流程的正确性。你知道，不是很清楚，因为锁是完全独立的，与它无关，如果是顺序执行就完全没问题，将 x 移动到 locked 0 之后。所以，在单一的串行执行中，这是对的。实际上，处理器都是这样做的，它们[]地执行任务，我们期望执行指令，这会导致这些指令重新排序。编译器也会这样做，可能优化一些代码路径，并且重新排序指令，结果是相同的串行执行。但是，很明显在并行执行的过程中这是一场灾难，因为如果获取了锁，这是我们的 release ，我们所做的，我们将临界区间移出了 acquire 区间，这完全是错误的。所以，这在并发执行中是错误的。所以，为了禁止或告诉编译器和硬件不要这么做，有一个叫做内存屏障的东西，或者某个 synchronize ，这个指令表示，在这个点之前的加载或保存，不允许移动到这一点之后。所以 release 有这个， acquire 也有这个，比如，这个 x<-x+1 ，如果这个更新是在 acquire 之后和 release 之前，x<-x+1 必须留在，这个内存同步点之前。所以不会，不会有内存重排的问题，这就是 \_\_sync_synchronize 在这的原因，无论是在 release 还是在 acquire 。这能理解吗？我有一个问题。是不是按照惯例，这个[]的开始，所以，我想编译器可以搞清楚，在获取锁之前，有一条指令，它可以被移动到锁释放之后。这会发生吗，或者它会遇到屏障。在这种情况下，acquire 是一个屏障，release 也是一个屏障，所以，任何在 locked 设置为 1 之前发生的事情，会在它之前发生，永远不会超过那个指令，这就是屏障，如果你愿意的话，这个是屏障一，这是屏障二。所以，这意味着在这之前的任何指令，留在这里，在之间的指令，会发生在 acquire 和 release 之间，之后的指令，将留在 release 之后。好的。好的?好的，我的时间快到了，所以让我来总结一下。所以，对于锁，锁对正确性有好处，但可能会对性能造成不良影响。这是一种失望的局面，因为我们使用锁的一个原因是，为了在并行执行过程中保持正确，但是锁实际上限制了并行执行。这是一个或两个锁使编程复杂化。你会在一些实验里体验到，从现在开始，我们将要做的事情，我们会看到锁一直会出现，这至少会给我们一些思考，有必要理解为什么锁在那里，以及它们保护的是什么。如果你做并行编程，这是一种内在的，你需要使用锁。所以，如果你想避免锁带来的复杂性，你可以做几件事，不要分享它，除非必须这么做。如果你没有共享数据结构，这些竞态条件就不会发生，所以这里你不需要锁，你不需要这种复杂的编程。但是，通常你会有一些共享的数据结构，你会需要锁，我认为要做的事情是从粗粒度开始，然后在需要的时候转向细粒度，基于你的测量，你要确保锁真的争用，在你开始重新设计之前。最后，也可以使用竞争检测器。这些竞争检测器工具会发现问题或竞态条件，因为你把锁放错了，或者你将 acquire 和 release 放错了，而实际上，你还有竞争。好的，这是一个关于锁的快速介绍，我们将会谈论更多关于锁的话题，在本学期的剩余课程中，它会出现，最后，我们将讨论有关无锁编程的问题，看看在内核中是如何做到这一点的。好的，我在这里停止，所以，任何必须去别的地方的人可以去，但是，如果你还有什么问题，请随时提出来。我们在聊天中有个问题，fence 指令是不是没有必要，因为 amoswap 指令可以具有获取释放顺序。是的，好的，所以有两件事，sync 指令同时为编译器和硬件。是的，我马上要超过工作时间了，但我认为还有更多的问题。如何只为编译器做这件事呢？编译器知道正在编译的是哪个体系结构，所以我们知道它什么时候必须确保合适的栅栏，无论运行在哪种体系结构上，不管是哪种内存一致性模型。所以这会引起更复杂的讨论，就是每个硬件都有一个内存模型，编译器决定，给特定架构的内存模型，它可以做什么，不能做什么。我想我的问题是，fence 指令只有在这种情况是没有必要的，如果你调用 amoswap.w.rl ，放在 sync 中，这有一个 sync ，但是编译器排序，然后。是的。内存排序和无序。是的。机器也使用 fence 指令，fence 指令只在执行 .rl 的情况下是没有必要的，看起来它不会检测到这一点，那么你怎么做的，所以编译器最终强制排序，但你已经使用[]指令覆盖了它。是的，好问题，更复杂的需求实现会是，比如 RISC-V 专门的 acquire 和 release 实现，可能比我们做的事情更复杂，使用 fence 指令是一个粗粒度的实现。但是这有点复杂，是的，如果你对这个感兴趣，RISC-V 的内存模型非常复杂，如果你看一下操作手册中的非特权指令，有一整章来专门介绍内存排序，告诉你应该做什么。在特定情况下，编译器应该怎么做。所以，你的意思是编译器会注意到这一事实，我们只是把汇编指令放在这里，而且它本身不会对任何内存访问进行重新排序。抱歉， synchronize ，这个同步库函数是库函数，它可以通过不同的方式实现。这是一种实现。并且库函数由编译器提供。但是编译器有没有进行优化的选项，它可以移动加载和存储指令。是的，编译器有。那么如何在不使用 fence 指令时防止这种情况发生，我想知道这个。我想我说的是，也许我想说的是，synchronized 告诉编译器和硬件，但是编译器可以实现不同的 \_\_sync_synchronize ，它知道它不能移动东西，但它没有 RISC-V 上的发布和 fence 指令，它知道它以一种特殊的方式在 RISC-V 上运行。但是 RISC-V 内存模型还不够吗，机器可以重组东西，所以你是否需要 acquire ，整个[点]都有。好的，还有比 \_\_sync_synchronize 更复杂的接口，这给了编译器编写者更多的自由，我们可以得到编译器，并将编译器和处理器解耦。比如，我想这里有一个标记可以传入，有一个 release 一致性同步。我不知道细节，我不知道，但你可以查一下这个，这是粗粒度接口，更细粒度的接口，让程序员有更多的控制权。好的，谢谢。我有一个问题，如果有多个线程和一个处理器，[争论]的方式与我们对多处理器的争论是否相同。你能重复一下问题吗，只是确认一下。所以我们没有，我想我们并没有谈到多线程，我们主要讨论的是多 CPU ，所以对于多线程来说，我想解决方案与拥有多个 CPU 时的解决方案相同，比如，那里有没有同样的参数。或多或少，至少在理论上是正确的思考方式，所以你有多个线程，但是只有一个 CPU ，还是这样情况，你希望确保，以原子方式执行某些内核代码序列。所以，你仍然需要有一个临界区间的概念。你可能不需要显式的锁或释放，但是你确实需要一种打开和关闭中断的方法，在一段代码中。所以，如果你查看较旧的操作系统内核，它们通常在内核中没有真正的锁获取，因为它们假设在一个处理器上运行，但是它们确实有像锁这样的东西，要打开和关闭中断。好的，我明白了，我的另一个问题是，在带有 UART 图片和缓冲区的的幻灯片上。是的，是不是，是的，就是那个，是不是一直都是这样的，读在后面，我不明白这个。是的，好的。所以这个到显示器，这是字符序列，要到显示器。写入者增加更多字符，所以，写入者走这条路，读取者跟随写入者，因为你不能打印尚未放入缓冲区的字符。UART 把东西给显示器，开始将这个插槽中的第一个字符放到显示器上，同时， printf 进入，它们在这里放更多的字符，所以写指针在这里，然后当显示了一个字符，然后 UART 将这个指针向上移动，以显示下一个字符。UART 总是有点落后于写入者，直到它赶上，然后 r 和 w 相同，在这一点上，意味着缓冲区中不再有字符。哦，好的，我明白了。好的，非常感谢。不用谢。还有什么问题吗？只有我们在这里。好的。所有人，再见。