An icon-only square control for header and toolbar chrome; never for a primary action in content.

    <IconButton icon="search" label="搜索" />
    <IconButton icon="list" label="目录" active />
    <IconButton icon="x" label="关闭" variant="outline" size="sm" />

Ghost is the default; solid is reserved for one affordance per view. On touch surfaces bump to size="lg" so the target reaches 44px with padding.
