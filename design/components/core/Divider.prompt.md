A rule; pick the variant by meaning - hairline separates, dashed route connects, gold marks a chapter break.

    <Divider />
    <Divider variant="route" label="航线" />
    <Divider variant="gold" />

Gold rules appear at most twice per page. Vertical dividers only inside a header or meta row, never as a column separator (use the grid gap).
