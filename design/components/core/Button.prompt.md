The action control; use exactly one \`primary\` per view and reach for \`secondary\`/\`ghost\`/\`quiet\` for everything else.

    <Button>继续阅读</Button>
    <Button variant="secondary" icon="list">目录</Button>
    <Button variant="quiet" iconRight="arrow-right">全部领域</Button>

Labels are 2-4 Chinese characters, sentence case in Latin. Never gold-filled (gold is punctuation only), never full-width outside a form or mobile sheet. Hover darkens the fill and does not lift; press scales to .985.
