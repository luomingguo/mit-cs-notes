The 264px left rail listing 领域 and their 课程; group with tracked uppercase section titles.

    <SidebarNav active="justice" onSelect={setView} sections={[
      { title: '领域', items: [{ value: 'pp', label: '政治哲学', icon: 'map', count: 4 }] }
    ]} />

Active rows get an inset 2px ocean bar plus heading-colour text - never a filled blue block. Counts are mono at 11px.
