The brand lockup - badge plus name; use it in headers, footers, and the top of any deck slide.

    <Logo href="/" size={38} src="../../assets/logo-archipelago-badge-256.png" />
    <Logo size={64} showName={false} />           {/* badge alone */}
    <Logo tone="inverse" size={40} />             {/* on navy */}

Never recolour, crop, or rotate the badge, and never place it inside a button. Pass \`src\` whenever the page is not at the project root. The name is set in Playfair Display because no wordmark file exists yet.
