A checkbox for independent options - filters, "只看未读", settings.

    <Checkbox label="只看有反向链接的讲义" defaultChecked />

Square with 2px radius (radio is the only circle in the system). Label is sans, not serif.
