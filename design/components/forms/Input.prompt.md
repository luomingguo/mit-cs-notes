A text field; also the site search when given \`icon="search"\`.

    <Input label="搜索" icon="search" placeholder="搜索讲义、概念、课程" suffix="⌘K" />
    <Input label="邮箱" error="这个地址看起来不完整" />

Fields have an inset shadow (paper well) and a 1px border that goes ocean on focus with a 3px focus ring. Labels are sans 11px tracked uppercase; placeholders are full sentences in Chinese without a trailing period.
