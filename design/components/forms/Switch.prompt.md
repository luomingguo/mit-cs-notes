A toggle whose effect is immediate - show backlinks, dark sea mode, wide measure.

    <Switch label="显示反向链接" defaultChecked />

Off track is sand, on track is ocean; the knob slides with --ease-tide and never bounces. If the change needs a save button, use Checkbox instead.
