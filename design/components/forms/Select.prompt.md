A native select in brand chrome; use it for 4+ options (below that use Tabs or radios).

    <Select label="排序" options={['按课程顺序','最近更新','引用最多']} />

Stays native on purpose - no custom popover in this system.
