Multi-line prose input - notes, summaries, annotations; set in the serif face, unlike every other control.

    <Textarea label="摘要" rows={3} placeholder="一到两句话说明这篇在讲什么" />

Only vertical resize. Do not use it for code (use a mono-font variant via \`style\`).
