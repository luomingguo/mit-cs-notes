A 领域 tile rendered on the night sea; use these in the atlas grid on the home page, not inside a paper card.

    <DomainCard name="政治哲学" latin="Political philosophy"
      summary="从社会契约到分配正义：这片海域关心的是权力凭什么正当。"
      concepts={['无知之幕','自然状态']} courseCount={4} noteCount={46} href="/d/pp" />

It sets \`data-theme="deep"\` on itself, so nested components flip automatically. Concepts are dashed pills (sea lanes), not solid tags. Two to four per row; never mix with paper cards in the same grid.
